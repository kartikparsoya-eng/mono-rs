---
phase: 29
plan: 3
wave: 2
depends_on: [29.1, 29.2]
files_modified:
  - packages/zero-cache/src/services/view-syncer/pipeline-driver.test.ts
  - packages/zero-cache/src/services/view-syncer/fuzz-ivm.test.ts
autonomous: true
requirements_addressed: [COR-03, COR-04, COR-05]
---

# Plan 29.3: Missing Test Scenarios

<objective>
Add test coverage for Take/Limit queries (hydration + advance), NOT EXISTS advance, and implement the fuzz fan-out test.todo placeholder. All new tests must pass under `ZERO_DUAL_EXEC=strict` to validate the dual-exec infrastructure from Plans 29.1 and 29.2.
</objective>

## Task 1: Add Take/Limit hydration test

<read_first>

- `pipeline-driver.test.ts` — study existing test structure, especially how `createPipelineDriver()`, `addQuery()`, and `changes()` are used
- Find how queries with `limit` are represented in the AST (search for `limit` in pipeline-driver.ts and the test file)
- Look at existing `whereExists` tests for the pattern to follow
- `pipeline-driver.ts:992` — `limit: ast.limit` confirms limit is passed through to Rust config
  </read_first>

<action>
Add a new `describe('queries with limit')` block in `pipeline-driver.test.ts` containing:

**Test: "hydration with limit returns correct subset"**

1. Set up a table with 5+ rows (e.g., `issues` with ids 1-5, each with a priority field)
2. Create a query with `ORDER BY priority ASC` and `LIMIT 3`
3. Call `addQuery()` and collect hydration changes
4. Assert exactly 3 rows are returned
5. Assert they are the 3 rows with lowest priority values

Follow the existing test patterns exactly — use the same `createPipelineDriver()` helper, same table setup style, same assertion patterns.
</action>

<acceptance_criteria>

- Test creates a query with `limit` in the AST
- Hydration returns exactly `limit` rows
- Rows are the correct subset based on ordering
- Test passes with `ZERO_DUAL_EXEC=strict`
  </acceptance_criteria>

## Task 2: Add Take/Limit advance test

<read_first>

- Existing advance tests in `pipeline-driver.test.ts` — study how `pipelines.advance()` is called after mutations
- How the test harness applies changes (inserts/updates/deletes) between advance calls
  </read_first>

<action>
Add within the same `describe('queries with limit')` block:

**Test: "advance maintains limit window when new row enters top-N"**

1. Start with 5 rows, query with `ORDER BY priority ASC LIMIT 3`
2. Hydrate — get rows with priorities [1, 2, 3]
3. Insert a new row with priority 0 (should enter the top-3 window)
4. Advance — expect: ADD for priority-0 row, REMOVE for priority-3 row (it falls out of the window)

**Test: "advance maintains limit window when top-N row is deleted"**

1. Start with 5 rows, query with `ORDER BY priority ASC LIMIT 3`
2. Hydrate — get rows with priorities [1, 2, 3]
3. Delete the row with priority 1
4. Advance — expect: REMOVE for priority-1 row, ADD for priority-4 row (it enters the window)
   </action>

<acceptance_criteria>

- Advance correctly adds/removes rows to maintain the limit window
- Both insert-into-window and delete-from-window scenarios covered
- Tests pass with `ZERO_DUAL_EXEC=strict`
  </acceptance_criteria>

## Task 3: Add NOT EXISTS advance test

<read_first>

- Existing `whereExists` tests in `pipeline-driver.test.ts` — find the hydration tests
- Understand how EXISTS/NOT EXISTS conditions are represented in the AST
- Look at how the test sets up related tables for subquery conditions
  </read_first>

<action>
Add a `describe('NOT EXISTS advance')` block:

**Test: "advance when NOT EXISTS condition changes"**

1. Set up two tables: `issues` and `comments`
2. Query: `SELECT * FROM issues WHERE NOT EXISTS (SELECT 1 FROM comments WHERE comments.issue_id = issues.id)`
3. Hydrate with some issues having comments and some not — only uncommented issues appear
4. Insert a comment for an issue that previously had none
5. Advance — expect: REMOVE for that issue (it now has a comment, so NOT EXISTS is false)
6. Delete all comments for an issue that previously had comments
7. Advance — expect: ADD for that issue (NOT EXISTS is now true)
   </action>

<acceptance_criteria>

- NOT EXISTS condition correctly evaluated during advance
- Adding a related row causes parent to be removed from results
- Removing all related rows causes parent to be added to results
- Tests pass with `ZERO_DUAL_EXEC=strict`
  </acceptance_criteria>

## Task 4: Implement fuzz fan-out test

<read_first>

- `fuzz-ivm.test.ts:1-50` — understand the test file structure, imports, and helpers
- `fuzz-ivm.test.ts:338-343` — the todo placeholder
- `dual-executor.ts:149-236` — `compareChanges` function
- Understand what `rustFanOutFn` does and how it's called in pipeline-driver.ts
  </read_first>

<action>
Replace the `test.todo(...)` at line 342 with an actual test implementation:

**Test: "generate random tables, filter predicates, and changes"**

1. Generate N random iterations (e.g., 50) where each iteration:
   a. Creates a random table schema (2-5 columns, random types from string/number/boolean)
   b. Generates 5-20 random rows conforming to the schema
   c. Generates 1-5 random filter predicates (simple comparisons: =, !=, <, >)
   d. Generates 1-10 random changes (inserts, updates, deletes)
2. For each iteration, run the changes through both:
   a. A TS-side filter evaluation (apply each change, check if row matches all predicates)
   b. The `compareChanges` function from dual-executor to verify format compatibility
3. Assert no crashes and that the comparison infrastructure handles all generated shapes

Keep the test focused on the comparison infrastructure rather than actual Rust fan-out (which requires native bindings). The goal is to verify that `compareChanges` handles diverse change shapes correctly.

Use a seeded random number generator for reproducibility (e.g., simple LCG or use the test index as seed).
</action>

<acceptance_criteria>

- The `test.todo` is replaced with a working test
- Test generates random table schemas, rows, predicates, and changes
- Test uses `compareChanges` to verify comparison infrastructure
- Test is deterministic (seeded RNG)
- Test passes reliably
  </acceptance_criteria>

## Task 5: Verify all tests pass under `ZERO_DUAL_EXEC=strict`

<read_first>

- Confirm the test runner supports env var override
  </read_first>

<action>
Run the full pipeline-driver test suite with `ZERO_DUAL_EXEC=strict`:

```bash
ZERO_DUAL_EXEC=strict npm --workspace=zero-cache run test -- pipeline-driver
```

And the fuzz test:

```bash
npm --workspace=zero-cache run test -- fuzz-ivm
```

Fix any failures. If Rust native bindings are not available in the test environment (causing Rust paths to fail), ensure the dual-exec code gracefully handles this — the comparison should be skipped if the Rust path throws, not crash the test.
</action>

<acceptance_criteria>

- All pipeline-driver tests pass with `ZERO_DUAL_EXEC=strict`
- All fuzz-ivm tests pass
- `npm --workspace=zero-cache run check-types` passes
- `npm --workspace=zero-cache run lint` passes
  </acceptance_criteria>

<verification>

## must_haves

- `npm --workspace=zero-cache run test -- pipeline-driver` passes
- `npm --workspace=zero-cache run test -- fuzz-ivm` passes
- `ZERO_DUAL_EXEC=strict npm --workspace=zero-cache run test -- pipeline-driver` passes
- `npm --workspace=zero-cache run check-types` passes
- `npm --workspace=zero-cache run lint` passes
- At least 2 limit/take tests exist (hydration + advance)
- At least 1 NOT EXISTS advance test exists
- The fuzz-ivm todo is replaced with a working test
  </verification>

<must_haves>

- Limit/Take hydration test: correct subset returned
- Limit/Take advance tests: window maintenance on insert and delete
- NOT EXISTS advance test: condition change triggers add/remove
- Fuzz fan-out test: replaces todo with deterministic random test
- All tests pass under `ZERO_DUAL_EXEC=strict`
  </must_haves>
