---
phase: 34-differential-fuzz-schema-extension
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - tools/ivm-parity/arb-ast.ts
  - tools/ivm-parity/random-ast-fuzz.ts
  - tools/ivm-parity/harness-fuzz.ts
  - tools/ivm-parity/timing-probe.md
autonomous: true
requirements: [FUZZ-01]
tags: [phase-34, fuzz, fast-check, timing-probe, wave-1]

must_haves:
  truths:
    - "fast-check generator covers full operator surface — filter, join, exists, or-exists, take, skip, related[], correlated subquery — per FUZZ-01 spec."
    - "Targeted arbitraries guarantee generation of B1, B2, B3 trigger shapes per CONTEXT D-08."
    - "Timing probe (100 iterations) measures cost-per-AST and projects 1k iteration total; result recorded in timing-probe.md."
    - "Projected 1k total under 150 seconds; if not, mitigations applied (BATCH_SIZE=60, mutation pruning) before 34-07 verification."
    - "Default shrinker produces minimal counterexamples for at least one synthetic divergence (validated via FORCE_DIVERGENCE=1 mode)."
  artifacts:
    - path: "tools/ivm-parity/arb-ast.ts"
      provides: "complete fast-check arbitraries — arbAst covers all operators per FUZZ-01"
      min_lines: 150
    - path: "tools/ivm-parity/random-ast-fuzz.ts"
      provides: "production-ready driver with FUZZ_NUM_RUNS / FUZZ_SEED / WAVE_0_SKELETON / FORCE_DIVERGENCE env support"
    - path: "tools/ivm-parity/harness-fuzz.ts"
      provides: "BatchedRunner with BATCH_SIZE configurable; mutation pruning if intersection-empty"
    - path: "tools/ivm-parity/timing-probe.md"
      provides: "recorded timing of 100-iteration probe, projection to 1k, mitigation log"
  key_links:
    - from: "tools/ivm-parity/random-ast-fuzz.ts"
      to: "tools/ivm-parity/parity-allowlist.json"
      via: "isAllowed() filter"
      pattern: "parity-allowlist"
    - from: "tools/ivm-parity/arb-ast.ts"
      to: "tools/ivm-parity/zero-schema.ts"
      via: "adaptSchema runtime read"
      pattern: "adaptSchema"
---

<objective>
Promote the Wave 0 fast-check skeleton to production quality: full operator-surface generator, targeted arbitraries that guarantee B1/B2/B3 shape coverage per D-08, and a timing probe that validates the under-2-minute / 1k iteration CI budget per D-05 BEFORE FUZZ-02 schema extension lands.

Purpose: Per D-19, Track 1 develops in parallel with Track 2 against the existing schema. Per RESEARCH section "CI Budget Compliance", the 2-minute budget is tight (existing 221s for 1084 ASTs in `harness-advance-coverage.ts`); a 100-iteration probe must project under 150s total before the schema grows in 34-04.

Output:
  - `arb-ast.ts` extended with arbStart, arbOrderBy, arbRelated, arbLimit, arbB1Targeted, arbB2Targeted, arbB3Targeted.
  - `random-ast-fuzz.ts` with FORCE_DIVERGENCE=1 mode that injects a known-divergent AST (flip:true CSQ from Phase 35 deferred B7) to validate the harness can SEE divergences.
  - `harness-fuzz.ts` with BATCH_SIZE configurable plus mutation pruning (skip MUTATIONS that don't touch any AST's tables in the batch).
  - `timing-probe.md` recording probe results plus mitigation log.
</objective>

<execution_context>
@/Users/kartik.parsoya/Documents/Zero/mono-rs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kartik.parsoya/Documents/Zero/mono-rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-differential-fuzz-schema-extension/34-CONTEXT.md
@.planning/phases/34-differential-fuzz-schema-extension/34-RESEARCH.md
@.planning/phases/34-differential-fuzz-schema-extension/34-01-SUMMARY.md
@tools/ivm-parity/SKILL.md

<interfaces>
Existing harness primitives to reuse — DO NOT modify:

From tools/ivm-parity/ast-fuzz.ts:
- adaptSchema, opsForColumn, valuesForOp, canonicalKey, normalizeAST, topOptionsForTable
- Bounded-BFS uses MAX_WHERE_DEPTH=3, MAX_BRANCHES_PER_NODE=3, MAX_RELATED_DEPTH=2 — apply same caps in fast-check arbitraries to avoid pathological combinatorial blowup.

From tools/ivm-parity/harness-advance-coverage.ts:
- MUTATIONS array (17 entries with run/undo) — BatchedRunner consumes; mutation pruning examines `MUTATIONS[i].run` SQL to determine which tables are touched.
- BATCH_SIZE current default 30; harness-fuzz.ts can set this via env BATCH_SIZE.

fast-check 3.23.2 capabilities (already installed):
- fc.assert, fc.asyncProperty, fc.property — drivers
- fc.oneof (with weights), fc.constantFrom, fc.tuple, fc.dictionary, fc.record, fc.array
- fc.letrec — recursive arbitraries
- fc.option(arb, opts) — optional fields
- fc.pre(condition) — discard predicate inside property
- fc.integer, fc.string
- Default shrinker is structural and traverses combinator output recursively.

AST type from packages/zero-protocol/src/ast.ts:
  type AST = {
    table: string;
    alias?: string;
    schema?: string;
    where?: Condition;
    related?: CorrelatedSubquery[];
    start?: { row: Row; exclusive: boolean };
    limit?: number;
    orderBy?: Ordering;
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend arb-ast.ts to full operator surface plus targeted B1/B2/B3 arbitraries</name>
  <files>tools/ivm-parity/arb-ast.ts</files>
  <read_first>
    - tools/ivm-parity/arb-ast.ts (current Wave 0 skeleton from 34-01)
    - tools/ivm-parity/ast-fuzz.ts (full — for opsForColumn/valuesForOp/topOptionsForTable/relatedShapes; especially lines 601-725 for relatedShapes Category C orderBy+limit pattern that triggers B3)
    - packages/zero-protocol/src/ast.ts (full — AST + Condition + CorrelatedSubquery types)
    - packages/zero-cache/src/services/view-syncer/fuzz-ivm.test.ts (lines 1-150 — existing fast-check generator pattern in repo)
    - .planning/phases/34-differential-fuzz-schema-extension/34-RESEARCH.md (offset 758, limit 100 — fast-check Generator Design + per-operator arbitraries table + Targeted shapes for B1/B2/B3)
    - .planning/phases/34-differential-fuzz-schema-extension/34-01-SUMMARY.md (current arb-ast.ts capabilities and any helper exports added during Wave 0)
  </read_first>
  <behavior>
    Post-task `arb-ast.ts` exports:
    - `buildArbitraries(adapted)` returning at minimum `{ arbAst, arbB1Targeted, arbB2Targeted, arbB3Targeted, arbAstWithTargeted }` where `arbAstWithTargeted = fc.oneof({weight:7, arbitrary: arbAst}, {weight:1, arbitrary: arbB1Targeted}, {weight:1, arbitrary: arbB2Targeted}, {weight:1, arbitrary: arbB3Targeted})`. random-ast-fuzz.ts uses `arbAstWithTargeted` as the default.
    - All ASTs are valid AST JSON parseable by zero-cache (no schema-rejected shapes).
    - No infinite recursion; depth bounds match ast-fuzz.ts (MAX_WHERE_DEPTH=3, MAX_RELATED_DEPTH=2, MAX_BRANCHES_PER_NODE=3).
    - Targeted arbitraries:
      * `arbB1Targeted`: always emits AST with `start` AND a `where` containing CSQ-EXISTS plus an `orderBy` (Skip semantics require orderBy alongside start).
      * `arbB2Targeted`: always emits AST with `where: { type: 'or', conditions: [simple, csq-exists] }`.
      * `arbB3Targeted`: always emits AST with `related` array containing a subquery with numeric `limit`.
  </behavior>
  <action>
    In `tools/ivm-parity/arb-ast.ts`, extend the Wave 0 skeleton to add:

    1. `arbOrderBy(table)`: emits a 1-2 element array of `[columnName, 'asc'|'desc']` tuples drawn from `table.columns`. Cite RESEARCH per-operator arbitraries table.

    2. `arbStart(table, orderByColumns)`: a record with `row` (object whose keys are exactly the orderBy columns and values drawn from the column's domain) and `exclusive` (boolean). Skip semantics require `row` to cover the orderBy.

    3. `arbRelated(table, depth)`: bounded recursion via `depth` parameter. If `depth >= MAX_RELATED_DEPTH` return null arbitrary. Otherwise pick a relationship from `table.relationships` and emit `{ correlation: {parentField, childField}, subquery: arbSubAst(rel.childTable, depth + 1) }`.

    4. `arbLimit`: integer arbitrary with min 1, max 10.

    5. Cap operator: per CONTEXT.md "out of scope" notes, Cap is dead code (`OperatorConfig::Cap` never emitted). Do NOT generate Cap arbitraries explicitly. Document with comment: `// Cap operator: dead code per IVM-PORT-AUDIT.md Risk #4 — not generated.`

    6. `arbAst` top-level: a record combining table + optional where + optional orderBy + optional start + optional limit + optional related. Use `fc.pre(...)` to drop ASTs where `start` is set without `orderBy` (zero-cache rejects these).

    7. Targeted arbitraries (D-08) — implementation sketch:

       buildTargetedArbitraries(adapted, base) returns three arbs:

       arbB1Targeted: chains an arbitrary table-with-relationships, then constructs an AST with:
         - table: chosen table name
         - where: a CSQ-EXISTS over the chosen relationship
         - orderBy: a single-element array using the table's first column ascending
         - start: a row matching the orderBy columns plus exclusive boolean

       arbB2Targeted: chains an arbitrary table-with-relationships, then constructs an AST with:
         - table: chosen table name
         - where: an OR-condition whose two children are (a) a simple predicate on the table and (b) a correlated-subquery EXISTS condition on the relationship

       arbB3Targeted: chains an arbitrary table-with-relationships, then constructs an AST with:
         - table: chosen table name
         - related: a single-element array containing a CSQ whose subquery has explicit `limit: 5` (this is the headline B3 trigger shape)

       The targeted arbitraries are deterministic in shape but stochastic in details — each iteration draws a different table/columns/values combination, so coverage spans the schema while guaranteeing the bug-triggering pattern.

    8. Compose `arbAstWithTargeted` via `fc.oneof` with weights 7/1/1/1 (base/B1/B2/B3). With 1000 iterations expected ~100 hits per targeted shape — sufficient to catch regressions if Track 2 fixes break.

    9. Cite at file head: `// fast-check generator — full operator surface per FUZZ-01 spec; targeted arbs guarantee B1/B2/B3 shape coverage per CONTEXT.md D-08.`

    Verify with a short tsx one-liner that imports buildArbitraries and prints the keys of the returned object — must include arbAstWithTargeted.
  </action>
  <verify>
    <automated>cd tools/ivm-parity && grep -E "arbB1Targeted|arbB2Targeted|arbB3Targeted|arbAstWithTargeted" arb-ast.ts | wc -l | awk '$1 >= 8 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "arbB1Targeted\|arbB2Targeted\|arbB3Targeted" tools/ivm-parity/arb-ast.ts` returns at least 8 (definition + composition + export references).
    - `grep -q "Cap operator: dead code" tools/ivm-parity/arb-ast.ts` succeeds.
    - `grep -E "arbStart|arbOrderBy|arbRelated|arbLimit" tools/ivm-parity/arb-ast.ts` returns at least 4 lines.
    - `grep -q "MAX_WHERE_DEPTH\|MAX_RELATED_DEPTH" tools/ivm-parity/arb-ast.ts` succeeds (depth bounds present as constants or imports).
    - `tsx --check tools/ivm-parity/arb-ast.ts` exits 0.
    - A short tsx eval that imports `buildArbitraries`, calls it with `adaptSchema` of `zero-schema.ts`, and verifies the returned object has key `arbAstWithTargeted` exits 0.
  </acceptance_criteria>
  <done>arb-ast.ts covers full operator surface per FUZZ-01 spec; targeted arbitraries guarantee B1/B2/B3 shapes per D-08; tsx-check clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend harness-fuzz.ts with mutation pruning and BATCH_SIZE config; add FORCE_DIVERGENCE smoke test to random-ast-fuzz.ts</name>
  <files>tools/ivm-parity/harness-fuzz.ts, tools/ivm-parity/random-ast-fuzz.ts</files>
  <read_first>
    - tools/ivm-parity/harness-fuzz.ts (current Wave 0 skeleton from 34-01)
    - tools/ivm-parity/random-ast-fuzz.ts (current Wave 0 skeleton from 34-01)
    - tools/ivm-parity/harness-advance-coverage.ts (lines 60-200 MUTATIONS structure; lines 614-687 runBatch)
    - .planning/phases/34-differential-fuzz-schema-extension/34-RESEARCH.md (offset 1107, limit 60 — CI Budget Compliance, mitigations 1-4)
    - .planning/phases/34-differential-fuzz-schema-extension/34-01-SUMMARY.md (current BatchedRunner contract from Wave 0)
  </read_first>
  <behavior>
    Post-task:
    - `BATCH_SIZE` is configurable via env BATCH_SIZE (default 30; tested values 30 and 60).
    - Mutation pruning: per RESEARCH mitigation 4, before running MUTATIONS in a batch, compute the set of tables touched by ANY AST's where/related. Only run MUTATIONS whose `run` SQL touches an AST-relevant table. Saves 100-300ms/batch when ASTs are filter-only over a single table.
    - `FORCE_DIVERGENCE=1` mode in random-ast-fuzz.ts: injects a known divergent AST (flip:true CSQ — Phase 35 deferred B7) at iteration 0, runs the harness ONCE on it, and asserts the runner returns outcome `diverge`. Validates the harness can SEE divergences end-to-end before relying on it for real ones. Full shrinking validation is implicit in any future fc.assert run that hits a divergence.
  </behavior>
  <action>
    In `tools/ivm-parity/harness-fuzz.ts`:

    1. Add `BATCH_SIZE = parseInt(process.env.BATCH_SIZE ?? '30', 10)` (top-level constant). Default 30 matches current harness-advance-coverage behavior.

    2. Add `MUTATIONS_TABLE_INDEX` — at module load, parse each `MUTATIONS[i].run` to extract table names. Use a regex matching `INSERT INTO`, `UPDATE`, or `DELETE FROM` followed by a table identifier (quoted or unquoted). Build `Map<string label, Set<string> tables>`. Cite RESEARCH CI Budget Compliance mitigation 4.

    3. In `BatchedRunner.#flush()`, before invoking the mutation block:
       - Walk every queued AST collecting referenced tables (root `ast.table`, `where.related.subquery.table` for any CSQ at any depth, `related[].subquery.table`). Call this `tablesInBatch`.
       - Filter MUTATIONS to those whose label maps to a non-empty intersection with `tablesInBatch`. Run only the filtered MUTATIONS.
       - If env `MUTATION_PRUNING=0` is set, skip pruning and run all MUTATIONS (escape hatch).

    4. Add a verbose mode that logs per-batch timings: `[batch N] tables=<count> mutations_run=X/Y duration=Zms`. Critical for the timing probe (Task 3).

    In `tools/ivm-parity/random-ast-fuzz.ts`:

    5. Add FORCE_DIVERGENCE mode: when `process.env.FORCE_DIVERGENCE === '1'`, before invoking fc.assert:
       - Construct a known divergent AST (flip:true CSQ — see Phase 35 deferred B7 note in CONTEXT.md). Concretely: `{ table: 'issues', where: { type: 'or', conditions: [{ simple-pred }, { type: 'correlatedSubquery', op: 'EXISTS', flip: true, related: { correlation, subquery: { table: 'comments', alias: 'force_div_c' } } }] } }`.
       - Call `runner.enqueueAndMaybeFlush(divergentAst)` then `runner.finalFlush()`.
       - Log outcome and a truncated diff (first 200 chars).
       - Exit 0 if outcome is `diverge`; exit 1 if outcome is `ok` (the harness failed to detect a known divergence).
       - Skip the fc.assert run in this mode.

    6. Compose the property body to use `arbAstWithTargeted` (from arb-ast.ts) as the default arbitrary unless `FUZZ_ARB=base` selects plain `arbAst` (escape hatch).

    7. Add cleanup-skip optimization (RESEARCH mitigation 2): if previous batch's `cleanupDb` returned on first poll, set a flag so the next batch's pre-mutate step skips the verification poll.

    Verify by running with both caches up: `FUZZ_NUM_RUNS=10 BATCH_SIZE=30 npm run fuzz-check` runs cleanly. `FORCE_DIVERGENCE=1 npm run fuzz-check` exits 0 with a logged divergence.
  </action>
  <verify>
    <automated>cd tools/ivm-parity && grep -c "BATCH_SIZE\|MUTATIONS_TABLE_INDEX\|MUTATION_PRUNING" harness-fuzz.ts | awk '$1 >= 4 {exit 0} {exit 1}' && grep -q "FORCE_DIVERGENCE" random-ast-fuzz.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BATCH_SIZE\|MUTATIONS_TABLE_INDEX\|MUTATION_PRUNING" tools/ivm-parity/harness-fuzz.ts` returns at least 4.
    - `grep -q "FORCE_DIVERGENCE" tools/ivm-parity/random-ast-fuzz.ts` succeeds.
    - `grep -q "arbAstWithTargeted\|FUZZ_ARB" tools/ivm-parity/random-ast-fuzz.ts` succeeds.
    - `tsx --check tools/ivm-parity/harness-fuzz.ts` exits 0.
    - `tsx --check tools/ivm-parity/random-ast-fuzz.ts` exits 0.
    - Manual (with caches running): `cd tools/ivm-parity && FORCE_DIVERGENCE=1 timeout 60 npm run fuzz-check` exits 0 with stdout containing `outcome=diverge`.
  </acceptance_criteria>
  <done>harness-fuzz.ts has BATCH_SIZE config + mutation pruning; random-ast-fuzz.ts has FORCE_DIVERGENCE smoke test mode; tsx-check clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run 100-iteration timing probe; record results and projected 1k cost</name>
  <files>tools/ivm-parity/timing-probe.md</files>
  <read_first>
    - .planning/phases/34-differential-fuzz-schema-extension/34-RESEARCH.md (offset 1107, limit 80 — CI Budget Compliance section, full cost model and mitigations)
    - tools/ivm-parity/random-ast-fuzz.ts (post Tasks 1+2)
    - tools/ivm-parity/harness-fuzz.ts (post Tasks 1+2)
    - tools/ivm-parity/SKILL.md (full — confirms run procedure for both caches)
  </read_first>
  <behavior>
    Run `FUZZ_NUM_RUNS=100 BATCH_SIZE=30 WAVE_0_SKELETON=1 npm run fuzz-check` against both running caches against the CURRENT schema (FUZZ-02 not yet landed). Capture wall-clock time, divergence count, batch count, and per-batch timings. Project 1k iteration cost as `(measured_100_iter_seconds) * 10`. If projection > 150 seconds, run a second probe with BATCH_SIZE=60 plus MUTATION_PRUNING=1 (already default) and project again. Record both probes plus final mitigation decision in `tools/ivm-parity/timing-probe.md`.
  </behavior>
  <action>
    1. Boot both caches (TS via `bash run.sh` or whatever harness boot doc says — see 34-01-SUMMARY.md for the exact commands; DO NOT modify TS source per SKILL.md hard rule 4).

    2. Run probe A: `time FUZZ_NUM_RUNS=100 BATCH_SIZE=30 WAVE_0_SKELETON=1 FUZZ_SEED=1 npm run fuzz-check 2>&1 | tee /tmp/probe-a.log`. Capture wall-clock (real time), batch count, mean batch duration, divergence count.

    3. Compute projection: `1k_projected = (probe_a_seconds) * 10`. If `projected < 150s`, no further mitigation needed.

    4. If projection >= 150s, run probe B: `time FUZZ_NUM_RUNS=100 BATCH_SIZE=60 WAVE_0_SKELETON=1 FUZZ_SEED=1 npm run fuzz-check 2>&1 | tee /tmp/probe-b.log`. Compute new projection.

    5. If probe B projection still >= 150s, document in timing-probe.md as a phase risk and add a note to PARITY_STATUS.md (D-20 carry-forward) flagging that the 1k <2 minute budget may not hold post FUZZ-02 schema extension; surface to user during 34-04 planning.

    6. Create `tools/ivm-parity/timing-probe.md` with this structure:

       Header section: probe date, hardware (uname -a), zero-cache versions, schema state ("pre-FUZZ-02" since 34-04 hasn't landed).

       Probe A row: NUM_RUNS=100, BATCH_SIZE=30, MUTATION_PRUNING=on, wall-clock seconds, batches run, mean batch duration, divergence count, projected 1k seconds.

       Probe B row (if executed): same columns, BATCH_SIZE=60.

       Mitigations applied: bullets listing which RESEARCH mitigations 1-4 were enabled.

       Decision section: "1k budget achievable: YES/NO (projected: Xs)". If NO, link to PARITY_STATUS.md entry.

       Notes: any divergences observed during the probe (these are likely Track 2 B-shape divergences expected to disappear after 34-02/34-05/34-06 — log them as expected red).

    7. Per D-19, this probe runs against the current (broken-Track-2) baseline. Divergences observed here are EXPECTED on B1/B2/B3 shapes; the fix verification happens in 34-07 with the post-Track-2 baseline.

    8. Commit timing-probe.md.
  </action>
  <verify>
    <automated>test -f tools/ivm-parity/timing-probe.md && grep -E "Probe A|projected|BATCH_SIZE" tools/ivm-parity/timing-probe.md | wc -l | awk '$1 >= 3 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - File `tools/ivm-parity/timing-probe.md` exists.
    - `grep -q "Probe A" tools/ivm-parity/timing-probe.md` succeeds.
    - `grep -E "[Pp]rojected.*[0-9]+\s*s" tools/ivm-parity/timing-probe.md` returns at least 1 line.
    - `grep -E "1k budget achievable" tools/ivm-parity/timing-probe.md` returns at least 1 line.
    - If decision is NO (projection >= 150s), then `grep -q "PARITY_STATUS" tools/ivm-parity/timing-probe.md` succeeds (cross-reference to status doc).
  </acceptance_criteria>
  <done>timing-probe.md committed with measured 100-iter wall-clock, projected 1k cost, mitigation log, and explicit YES/NO budget-achievable decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arb-ast.ts → zero-cache | random AST JSON crosses WS into both caches; malformed shapes must be filtered via fc.pre to avoid surface-level rejections that aren't real divergences |
| harness-fuzz BatchedRunner → PG | mutation block writes to shared parity DB; cleanup must be reliable to avoid leaking state between batches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-06 | DoS | pathological AST (deeply nested CSQ) hangs one cache | mitigate | Depth bounds (MAX_WHERE_DEPTH=3, MAX_RELATED_DEPTH=2) inherited from ast-fuzz.ts; HYDRATE_TIMEOUT 5s for fuzz mode |
| T-34-07 | Information Disclosure (false negative) | mutation pruning skips a mutation that would have surfaced a real divergence | accept | Mitigation pruning is conservative: any AST referencing the mutation's table forces the mutation to run; documented in harness-fuzz.ts |
| T-34-08 | DoS | Postgres connection-pool exhaustion under BATCH_SIZE=60 | mitigate | MAX_PARALLEL=8 cap kept; no concurrent batches; documented in RESEARCH pitfall 3 |
</threat_model>

<verification>
- `cd tools/ivm-parity && tsx --check arb-ast.ts harness-fuzz.ts random-ast-fuzz.ts` exits 0.
- `cd tools/ivm-parity && FORCE_DIVERGENCE=1 npm run fuzz-check` (caches up) exits 0 with logged `outcome=diverge`.
- `cd tools/ivm-parity && FUZZ_NUM_RUNS=10 npm run fuzz-check` (caches up) exits 0 (or non-zero only on Track-2 expected-red shapes; document in SUMMARY).
- timing-probe.md committed with measured + projected numbers.
</verification>

<success_criteria>
- arb-ast.ts emits ASTs covering full operator surface per FUZZ-01 spec; targeted arbs guarantee B1/B2/B3 shapes per D-08.
- harness-fuzz.ts has configurable BATCH_SIZE plus mutation pruning per RESEARCH mitigations.
- random-ast-fuzz.ts has FORCE_DIVERGENCE smoke test mode for shrinking-pipeline validation.
- timing-probe.md records 100-iter probe results and projection to 1k; decision YES/NO documented.
- All 3 modified files clean under `tsx --check`.
</success_criteria>

<output>
After completion, create `.planning/phases/34-differential-fuzz-schema-extension/34-03-SUMMARY.md` documenting:
  - Final BATCH_SIZE chosen (30 or 60).
  - Mutation pruning enabled by default (yes/no).
  - 100-iter probe wall-clock + projected 1k seconds + decision.
  - Any pre-Track-2 divergences observed (expected red — should drop to 0 + allow-list after 34-07).
  - List of all new env vars (FUZZ_NUM_RUNS, FUZZ_SEED, BATCH_SIZE, MUTATION_PRUNING, FORCE_DIVERGENCE, FUZZ_ARB, WAVE_0_SKELETON).
</output>
