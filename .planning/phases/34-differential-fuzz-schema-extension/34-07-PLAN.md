---
phase: 34-differential-fuzz-schema-extension
plan: 07
type: execute
wave: 3
depends_on: [02, 03, 04, 05, 06]
files_modified:
  - tools/ivm-parity/random-ast-fuzz.ts
  - tools/ivm-parity/package.json
  - tools/ivm-parity/PARITY_STATUS.md
  - PARITY_STATUS.md
  - .planning/milestones/v5.0-bench-results.md
autonomous: true
requirements: [FUZZ-01, FUZZ-02, B1, B2, B3, B11]
tags: [phase-34, verification, fuzz, gate, wave-3]

must_haves:
  truths:
    - "FUZZ_NUM_RUNS=1000 npm run fuzz-check completes in under 2 minutes — D-21 #3 hard gate; machine-checked via WALL_MS log line."
    - "Per-table iteration instrumentation proves FUZZ-02 schema (events, big_id_records, event_tags) is actually exercised — each new table sees ≥10 hits in a 1k run (1% floor)."
    - "Zero unexpected divergences — only allow-listed shapes (5 catalogued + 4 deferred) appear; everything else is green."
    - "Phase 33 benches re-run and PASS thresholds (TTFB <1.5×, MemPeak ≥4×) — D-18 #6 / D-21 #4."
    - "Full cargo test green for zero-ivm-rs and zqlite-rs."
    - "Full vitest suite green for packages/zero-cache/src/services/view-syncer/."
    - "streaming-vs-buffered-parity.fuzz.test.ts passes 1k iterations — D-18 #5."
    - "tools/ivm-parity/ npm test (full sweep with new fuzz-check integrated) exits 0."
    - "Any new divergence found by 1k fuzz becomes a regression test in tools/ivm-parity/ and a PARITY_STATUS.md entry per D-20 — even if Phase 35-deferred."
    - "Bench results appended to .planning/milestones/v5.0-bench-results.md."
  artifacts:
    - path: "tools/ivm-parity/random-ast-fuzz.ts"
      provides: "instrumented fuzz driver — emits PER_TABLE_HITS=<json> and WALL_MS=<ms> for machine-checkable assertions"
      contains: "PER_TABLE_HITS"
    - path: "tools/ivm-parity/package.json"
      provides: "test script aggregates BFS sweep + new fuzz-check; npm test runs the complete Phase 34 verification"
      contains: "fuzz-check"
    - path: ".planning/milestones/v5.0-bench-results.md"
      provides: "appended Phase 34 bench section with TTFB and MemPeak post-Track-2 numbers"
      contains: "Phase 34"
    - path: "PARITY_STATUS.md"
      provides: "updated post-Phase-34 status — 4 fixes shipped (B1/B2/B3/B11); any new divergences logged for Phase 35"
      contains: "Phase 34"
  key_links:
    - from: "tools/ivm-parity/package.json"
      to: "tools/ivm-parity/random-ast-fuzz.ts"
      via: "fuzz-check script in npm test aggregation"
      pattern: "fuzz-check"
    - from: "tools/ivm-parity/random-ast-fuzz.ts"
      to: "tools/ivm-parity/zero-schema.ts"
      via: "per-table hit counter increments on each AST.table arbitrary draw — covers FUZZ-02 events/big_id_records/event_tags tables"
      pattern: "PER_TABLE_HITS"
    - from: ".planning/milestones/v5.0-bench-results.md"
      to: "packages/zero-cache/src/services/view-syncer/rust-ivm-streaming-bench.test.ts"
      via: "bench output appended after Track 2 fixes"
      pattern: "TTFB"
---

<objective>
Verification gate per D-21 (#1 through #5). Run 1k iteration fast-check fuzz against post-Track-2 baseline; confirm <2 minutes (machine-checked via WALL_MS); confirm FUZZ-02 schema is actually exercised (machine-checked via PER_TABLE_HITS — addresses checker BLOCKER); confirm zero unexpected divergences; re-run Phase 33 benches and confirm no regression; integrate fuzz-check into `tools/ivm-parity/` `npm test` aggregate; update PARITY_STATUS.md per D-20.

Purpose: Per D-21, this is the phase's hard gate. All four BLOCKING fixes (B1/B2/B3/B11) have Rust unit tests AND `tools/ivm-parity/` differential tests committed (verified in 34-02/34-05/34-06). This plan runs the integrated end-to-end check.

Output:
  - `random-ast-fuzz.ts` instrumented with per-table hit counter + WALL_MS timer (Task 1a — closes BLOCKER and timing warning).
  - 1k fast-check fuzz run completes in <2 min with allow-list filtering — exit 0.
  - Per-table hit counts logged; each FUZZ-02 table sees ≥10 hits.
  - Phase 33 bench re-run with post-Track-2 numbers; appended to v5.0-bench-results.md.
  - tools/ivm-parity/ `npm test` updated to include fuzz-check (BFS first per D-04, then fast-check).
  - PARITY_STATUS.md updated with Phase 34 status and any new divergences.
  - Any new divergences found get regression-test stubs per D-20.
</objective>

<execution_context>
@/Users/kartik.parsoya/Documents/Zero/mono-rs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kartik.parsoya/Documents/Zero/mono-rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-differential-fuzz-schema-extension/34-CONTEXT.md
@.planning/phases/34-differential-fuzz-schema-extension/34-RESEARCH.md
@.planning/phases/34-differential-fuzz-schema-extension/34-VALIDATION.md
@.planning/phases/34-differential-fuzz-schema-extension/34-01-SUMMARY.md
@.planning/phases/34-differential-fuzz-schema-extension/34-02-SUMMARY.md
@.planning/phases/34-differential-fuzz-schema-extension/34-03-SUMMARY.md
@.planning/phases/34-differential-fuzz-schema-extension/34-04-SUMMARY.md
@.planning/phases/34-differential-fuzz-schema-extension/34-05-SUMMARY.md
@.planning/phases/34-differential-fuzz-schema-extension/34-06-SUMMARY.md
@PARITY_STATUS.md
@.planning/milestones/v5.0-bench-results.md

<interfaces>
Verification gate inputs (read-only references):

From .planning/phases/34-differential-fuzz-schema-extension/34-VALIDATION.md:
- "Before /gsd-verify-work" section lists the full no-regression battery
- Phase 33 bench thresholds: TTFB ratio < 1.5×; MemPeak ratio ≥ 4× (Phase 33 carry-forward)

From CLAUDE.md verification gate:
1. npx vitest run packages/zero-cache/src/services/view-syncer/pipeline-driver.*.test.ts
2. FUZZ_NUM_RUNS=1000 streaming-vs-buffered-parity.fuzz.test.ts
3. cargo test in packages/zqlite-rs and packages/zero-ivm-rs
4. No signature change to existing buffered methods + no change to encode_advance_result_buf / decodeAdvanceResultBuf format

Allow-list per D-21 #3 = 5 catalogued (PARITY_STATUS.md) + 4 deferred (B5, B6, B7, B12). File: tools/ivm-parity/parity-allowlist.json (created in 34-01).

Bench file format from Phase 33: append-only Markdown sections with date, hardware, before/after numbers, threshold pass/fail.

FUZZ-02 schema tables to instrument (added in 34-04): events, big_id_records, event_tags. Each MUST see ≥10 hits in a 1k run (1% floor) to prove the fast-check arb actually targets them.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Instrument random-ast-fuzz.ts with PER_TABLE_HITS counter and WALL_MS timer; run full Phase 34 verification battery (cargo + vitest + 1k fuzz with coverage gate + Phase 33 bench); record bench numbers and any new divergences</name>
  <files>tools/ivm-parity/random-ast-fuzz.ts, .planning/milestones/v5.0-bench-results.md, tools/ivm-parity/PARITY_STATUS.md, PARITY_STATUS.md</files>
  <read_first>
    - tools/ivm-parity/random-ast-fuzz.ts (current post-34-01 — fc.assert driver; need to add per-table counter + WALL_MS log line)
    - tools/ivm-parity/arb-ast.ts (post-34-01 — confirms `arbAst` table arb is `fc.constantFrom(...tableNames)`; instrumentation hooks the picked table name)
    - tools/ivm-parity/zero-schema.ts (post-34-04 — confirms `events`, `big_id_records`, `event_tags` exist as table names)
    - .planning/phases/34-differential-fuzz-schema-extension/34-VALIDATION.md (full — sampling rate, full suite commands)
    - .planning/phases/34-differential-fuzz-schema-extension/34-RESEARCH.md (offset 1480, limit 50 — Validation Architecture: Phase requirements → test map; Sampling Rate)
    - .planning/milestones/v5.0-bench-results.md (full — append-only format from Phase 33; the structure to follow)
    - PARITY_STATUS.md (full — current 1079/1084 OK status)
    - tools/ivm-parity/PARITY_STATUS.md (full if exists; else PARITY_STATUS.md is at repo root)
    - tools/ivm-parity/parity-allowlist.json (post-34-01 — current 9 entries)
    - .planning/phases/34-differential-fuzz-schema-extension/34-03-SUMMARY.md (timing-probe results — projects 1k cost)
    - tools/ivm-parity/timing-probe.md (post-34-03 — confirms <150s probe projection)
  </read_first>
  <behavior>
    Post-task:
    - `random-ast-fuzz.ts` maintains a per-table hit counter `Record<string, number>`. Each fc.assert iteration that picks a table increments `hits[ast.table] += 1`. On exit, the driver emits to stdout: `PER_TABLE_HITS=<JSON>` (single line, parseable), e.g. `PER_TABLE_HITS={"events":42,"big_id_records":29,"event_tags":18,"users":151,...}`.
    - Same driver also emits `WALL_MS=<integer>` on exit (computed from `Date.now() - startMs`).
    - Full Rust suite green: `cargo test --release -p zero-ivm-rs && cargo test --release -p zqlite-rs --lib -- --test-threads=1`. Includes the 5 previously-ignored Wave 0 stubs (now flipped green) plus 3-4 new focused tests added in 34-02/34-05/34-06.
    - Full vitest suite green: `cd packages/zero-cache && npx vitest run src/services/view-syncer/`.
    - 1k iteration streaming-vs-buffered fuzz: `cd packages/zero-cache && FUZZ_NUM_RUNS=1000 npx vitest run src/services/view-syncer/streaming-vs-buffered-parity.fuzz.test.ts` — passes.
    - 1k iteration random-AST fuzz: `cd tools/ivm-parity && FUZZ_NUM_RUNS=1000 npm run fuzz-check 2>&1 | tee /tmp/fuzz-1k.log` — exits 0; reports `unexpected_divergences == 0`; emits `WALL_MS=<n>` with `n<120000`; emits `PER_TABLE_HITS={...}` with `events>=10 && big_id_records>=10 && event_tags>=10`.
    - Coverage gate explicitly asserted: each FUZZ-02 table sees ≥10 hits in a 1k run (1% floor of 1000 iterations). Closes checker BLOCKER (FUZZ-02 schema actually exercised by FUZZ-01 fuzz).
    - Full parity sweep: `cd tools/ivm-parity && npm test` — exits 0.
    - Phase 33 bench re-run: TTFB ratio <1.5×, MemPeak ratio ≥4× thresholds met.
    - Bench results appended to `.planning/milestones/v5.0-bench-results.md`.
    - PARITY_STATUS.md updated with Phase 34 ship summary.
    - Any NEW divergences found by the 1k fuzz that are NOT in the allow-list AND NOT one of B1/B2/B3/B11 (those should be GREEN now) become regression-test stubs in `tools/ivm-parity/ast_corpus.regressions.json` (or equivalent) per D-20, plus a PARITY_STATUS.md entry, plus a deferred-fix planned for Phase 35.
  </behavior>
  <action>
    1. **INSTRUMENT random-ast-fuzz.ts** (closes checker BLOCKER + WARNING #4):

       Locate the fc.assert call in `tools/ivm-parity/random-ast-fuzz.ts`. Wrap the property body with two pieces of instrumentation:

       At module top (after env reads):
       ```ts
       const startMs = Date.now();
       const tableHits: Record<string, number> = {};
       ```

       Inside the fc.asyncProperty body (first line, before runner.enqueueAndMaybeFlush):
       ```ts
       // Coverage instrumentation per Phase 34 checker BLOCKER fix:
       // proves FUZZ-02 schema (events / big_id_records / event_tags) is
       // actually targeted by the fast-check arb. Each iteration logs which
       // table the AST landed on; on exit we assert each new table sees
       // ≥10 hits per 1000 iterations (1% floor).
       tableHits[ast.table] = (tableHits[ast.table] || 0) + 1;
       ```

       At the end of the script, AFTER fc.assert resolves and BEFORE process.exit:
       ```ts
       const wallMs = Date.now() - startMs;
       // Single-line, machine-parseable. tail | grep WALL_MS recovers the value.
       console.log(`WALL_MS=${wallMs}`);
       console.log(`PER_TABLE_HITS=${JSON.stringify(tableHits)}`);
       ```

       Cite at file head update: `// Coverage + timing instrumentation — PER_TABLE_HITS proves FUZZ-02 table coverage (events/big_id_records/event_tags >=10 hits per 1k iter); WALL_MS provides machine-checkable <120s timing gate per D-21 #3.`

    2. Pre-flight check: confirm all dependent plans (02 through 06) committed and the SUMMARY files exist.

    3. Boot both caches per 34-01-SUMMARY procedure:
       - TS reference: `bash run.sh` from `tools/ivm-parity/` (or whatever the README/SKILL says).
       - RS: `npm run start-rs` from `tools/ivm-parity/`.
       - Verify both responding via `curl http://localhost:4858/...` and `curl http://localhost:4868/...` (or WS handshake; use the existing harness liveness probe).

    4. Run Rust suite in two parallel terminals:
       ```bash
       cd packages/zero-ivm-rs && cargo test --release --lib --quiet 2>&1 | tee /tmp/cargo-zero-ivm.log
       cd packages/zqlite-rs && cargo test --release --lib -- --test-threads=1 2>&1 | tee /tmp/cargo-zqlite.log
       ```
       Capture wall time of each. Confirm zero failures.

    5. Run vitest suites:
       ```bash
       cd packages/zero-cache && npx vitest run src/services/view-syncer/pipeline-driver.test.ts src/services/view-syncer/pipeline-driver.streaming.test.ts src/services/view-syncer/parity-check.test.ts 2>&1 | tee /tmp/vitest-pipeline.log
       cd packages/zero-cache && FUZZ_NUM_RUNS=1000 npx vitest run src/services/view-syncer/streaming-vs-buffered-parity.fuzz.test.ts 2>&1 | tee /tmp/vitest-stream-fuzz.log
       ```

    6. Run the 1k fast-check fuzz with instrumentation:
       ```bash
       cd tools/ivm-parity && FUZZ_NUM_RUNS=1000 FUZZ_SEED=$(date +%s) npm run fuzz-check 2>&1 | tee /tmp/fuzz-1k.log
       ```
       Capture from /tmp/fuzz-1k.log:
       - WALL_MS line — must be < 120000.
       - PER_TABLE_HITS line — parse JSON; assert `events`, `big_id_records`, `event_tags` each have count ≥ 10.
       - divergences-count (filtered against allow-list) — must be 0 unexpected.

       Machine-checkable assertions (run after fuzz completes):
       ```bash
       # Timing gate (closes checker WARNING #4):
       time_ms=$(grep '^WALL_MS=' /tmp/fuzz-1k.log | tail -1 | sed 's/WALL_MS=//')
       test "$time_ms" -lt 120000 || { echo "FAIL: 1k fuzz took ${time_ms}ms (>120000)"; exit 1; }

       # Coverage gate (closes checker BLOCKER):
       hits_json=$(grep '^PER_TABLE_HITS=' /tmp/fuzz-1k.log | tail -1 | sed 's/PER_TABLE_HITS=//')
       node -e "
         const h = JSON.parse(process.argv[1]);
         const required = ['events', 'big_id_records', 'event_tags'];
         const FLOOR = 10;
         for (const t of required) {
           if ((h[t] || 0) < FLOOR) {
             console.error('FAIL: table ' + t + ' had ' + (h[t]||0) + ' hits (<' + FLOOR + '); FUZZ-02 schema not exercised');
             process.exit(1);
           }
         }
         console.log('OK: FUZZ-02 coverage — ' + required.map(t => t + '=' + h[t]).join(', '));
       " "$hits_json"
       ```
       If either gate fails: STOP and either (a) raise FUZZ_NUM_RUNS, (b) re-weight the table arb in `arb-ast.ts`, or (c) fix the timing regression. Do NOT silently lower the floor.

    7. Run Phase 33 bench:
       ```bash
       cd packages/zero-cache && npx vitest run src/services/view-syncer/rust-ivm-streaming-bench.test.ts -t "TTFB" 2>&1 | tee /tmp/bench-ttfb.log
       cd packages/zero-cache && npx vitest run src/services/view-syncer/rust-ivm-streaming-bench.test.ts -t "memory" 2>&1 | tee /tmp/bench-mempeak.log
       ```
       Extract TTFB and MemPeak ratios. Confirm thresholds met.

    8. Append Phase 34 section to `.planning/milestones/v5.0-bench-results.md` (append-only). Format:
       ```markdown
       ## Phase 34 — Differential Fuzz + Schema Extension (DATE)

       Hardware: <output of `uname -a`>
       Cargo: <cargo --version>
       Node: <node --version>

       ### Verification Gate Status

       | Gate | Threshold | Result | Status |
       |------|-----------|--------|--------|
       | cargo test zero-ivm-rs | 0 failures | <count> | PASS |
       | cargo test zqlite-rs | 0 failures | <count> | PASS |
       | vitest pipeline-driver | 0 failures | <count> | PASS |
       | vitest streaming-vs-buffered fuzz 1k | 0 failures | <count> | PASS |
       | tools/ivm-parity npm test | exit 0 | <result> | PASS |
       | fast-check fuzz 1k WALL_MS | <120000 ms | <ms> | PASS/FAIL |
       | FUZZ-02 coverage (events) | ≥10 hits | <count> | PASS/FAIL |
       | FUZZ-02 coverage (big_id_records) | ≥10 hits | <count> | PASS/FAIL |
       | FUZZ-02 coverage (event_tags) | ≥10 hits | <count> | PASS/FAIL |
       | fast-check fuzz 1k unexpected | 0 | <count> | PASS/FAIL |
       | Phase 33 TTFB | <1.5× ratio | <ratio> | PASS/FAIL |
       | Phase 33 MemPeak | ≥4× ratio | <ratio> | PASS/FAIL |

       ### Bench Numbers
       <TTFB raw + ratio>
       <MemPeak raw + ratio>

       ### Per-Table Hit Distribution (1k iter)
       <full PER_TABLE_HITS JSON>

       ### Track 2 Shipped
       - B1 (Skip ordering) — <commit hash>
       - B2 (parent_sizes max(1)) — <commit hash>
       - B3 (Take partition_key threading) — <commit hash>
       - B11 (cascade-delete prev snapshot) — <commit hash>

       ### Allow-List State
       - Catalogued (5): unchanged from PARITY_STATUS.md baseline
       - Deferred (4): B5, B6, B7, B12 — Phase 35

       ### New Divergences Found by 1k Fuzz
       <list, or "None">
       ```

    9. Update `PARITY_STATUS.md` (and `tools/ivm-parity/PARITY_STATUS.md` if it exists separately):
       - Update header status: "Post-Phase-34: B1/B2/B3/B11 shipped; <X>/<total> sweep OK + <N> known divergences."
       - For each new divergence found in step 6:
         - Add an entry with shrunken canonical AST, suspected root cause (or "unknown — analyze in Phase 35"), and a deferred-fix plan reference.
         - Per D-20: regression test in `tools/ivm-parity/ast_corpus.regressions.json` (or equivalent — implementer creates if not exists).
       - If no new divergences, document "no new divergences beyond allow-list" explicitly.

    10. Per D-22, do NOT yet add CI integration. The fuzz-check script exists, runs locally, but is not gated in GitHub Actions. Document in SUMMARY that Phase 35 owns CI integration.

    11. If ANY gate fails:
        - Step back to determine whether it's a regression in the just-landed Track 2 fix, an environmental issue (cache restart needed), or a genuinely new divergence.
        - For regressions: file a fix-up plan or revert.
        - For environmental: re-run after wipe.
        - For new divergences: capture as Phase 35 backlog per D-20; Phase 34 STILL ships if the new divergence isn't in B1/B2/B3/B11 (those are the gate items).
        - For coverage failure: see step 6 above — DO NOT silently lower the FUZZ-02 floor; instead re-weight the table arb.

    12. Verification gate decision: explicitly state in 34-07-SUMMARY whether all D-21 sub-conditions are met:
        - D-21 #1: Track 2 fixes have unit + diff tests committed: YES/NO with refs.
        - D-21 #2: tools/ivm-parity npm test exits 0: YES/NO.
        - D-21 #3: 1k fuzz <2 min, 0 unexpected, FUZZ-02 coverage gate met: YES/NO with WALL_MS + PER_TABLE_HITS evidence.
        - D-21 #4: Phase 33 benches PASS: YES/NO with ratios.
        - D-21 #5: Full cargo + vitest green: YES/NO.

    13. Commit all changes (instrumented random-ast-fuzz.ts, bench results, PARITY_STATUS updates, regression-test stubs if any).
  </action>
  <verify>
    <automated>grep -q "PER_TABLE_HITS" tools/ivm-parity/random-ast-fuzz.ts && grep -q "WALL_MS" tools/ivm-parity/random-ast-fuzz.ts && cd packages/zero-ivm-rs && cargo test --release --lib --quiet 2>&1 | tail -5 | grep -E "test result: ok\\." && cd ../zqlite-rs && cargo test --release --lib --quiet -- --test-threads=1 2>&1 | tail -5 | grep -E "test result: ok\\." && grep -q "Phase 34" .planning/milestones/v5.0-bench-results.md && grep -q "Phase 34" PARITY_STATUS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PER_TABLE_HITS\\|tableHits" tools/ivm-parity/random-ast-fuzz.ts` returns at least 2 (declaration + emit).
    - `grep -c "WALL_MS\\|startMs\\|wallMs" tools/ivm-parity/random-ast-fuzz.ts` returns at least 2 (declaration + emit).
    - `grep -c "ast.table\\]" tools/ivm-parity/random-ast-fuzz.ts` returns at least 1 (per-iteration counter increment).
    - `cd packages/zero-ivm-rs && cargo test --release --lib --quiet 2>&1 | grep "FAILED" | wc -l | grep -q "^0$"`.
    - `cd packages/zqlite-rs && cargo test --release --lib --quiet -- --test-threads=1 2>&1 | grep "FAILED" | wc -l | grep -q "^0$"`.
    - `cd packages/zero-cache && npx vitest run src/services/view-syncer/pipeline-driver.test.ts 2>&1 | grep -E "Test Files.*passed" | wc -l | awk '$1 >= 1 {exit 0} {exit 1}'`.
    - 1k fast-check fuzz wall-clock recorded in v5.0-bench-results.md and is <120000ms — verified via: `time_ms=$(grep '^WALL_MS=' /tmp/fuzz-1k.log | tail -1 | sed 's/WALL_MS=//'); test "$time_ms" -lt 120000`.
    - 1k fast-check fuzz PER_TABLE_HITS gate met — verified via: `hits=$(grep '^PER_TABLE_HITS=' /tmp/fuzz-1k.log | tail -1 | sed 's/PER_TABLE_HITS=//'); node -e "const h=JSON.parse(process.argv[1]); ['events','big_id_records','event_tags'].forEach(t => { if ((h[t]||0) < 10) { process.exit(1); } });" "$hits"`.
    - 1k fast-check fuzz reports `unexpected_divergences = 0` (or equivalent log line) — verified via grep against /tmp/fuzz-1k.log.
    - `grep -E "Phase 34" .planning/milestones/v5.0-bench-results.md` returns at least 1 line.
    - `grep -E "Phase 34" PARITY_STATUS.md` returns at least 1 line.
    - `grep -E "B1.*shipped|B2.*shipped|B3.*shipped|B11.*shipped|Track 2 Shipped" .planning/milestones/v5.0-bench-results.md` returns at least 4 lines (or 1 line containing all 4 items).
    - `grep -q "Per-Table Hit Distribution\\|PER_TABLE_HITS" .planning/milestones/v5.0-bench-results.md` succeeds (coverage evidence preserved in append-only log).
    - TTFB ratio <1.5× recorded; MemPeak ratio ≥4× recorded.
    - If new divergences found: `grep -E "ast_corpus.regressions|new divergence" tools/ivm-parity/PARITY_STATUS.md PARITY_STATUS.md` returns evidence of capture.
    - 34-07-SUMMARY.md explicitly answers all 5 D-21 sub-conditions YES/NO with evidence (including PER_TABLE_HITS counts and WALL_MS for D-21 #3).
  </acceptance_criteria>
  <done>random-ast-fuzz.ts instrumented (closes checker BLOCKER + WARNING #4); all verification gate items recorded; bench results appended; PARITY_STATUS updated; D-21 conditions answered including FUZZ-02 coverage gate; Phase 34 ready for ship decision.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Integrate fuzz-check into tools/ivm-parity npm test aggregate; freeze allow-list as read-only baseline</name>
  <files>tools/ivm-parity/package.json</files>
  <read_first>
    - tools/ivm-parity/package.json (current — `test` aggregate at line 20)
    - tools/ivm-parity/parity-allowlist.json (post-Phase-34 — should be unchanged from 34-01 unless step 9 in Task 1 added entries)
    - .planning/phases/34-differential-fuzz-schema-extension/34-CONTEXT.md (D-22: no CI integration; this task only updates the local `npm test` aggregate)
    - 34-03-SUMMARY.md (final BATCH_SIZE chosen; mutation pruning state; FUZZ_NUM_RUNS default decision)
  </read_first>
  <behavior>
    Post-task `tools/ivm-parity/package.json` `test` script aggregate runs in this order (per D-04 BFS first then fast-check):

    `npm run fuzz` → `npm run seed-corpus` → `npm run synth-p6` → `npm run refresh-stats` → `npm run sweep:hydrate` → `npm run sweep:advance` → `npm run report` → `npm run fuzz-check` → `cat parity_report.md | head -30`

    The `fuzz-check` step uses default `FUZZ_NUM_RUNS=200` (not 1000) for the integrated `npm test` to keep the local sweep manageable; the gate-mode 1k run is invoked explicitly via `FUZZ_NUM_RUNS=1000 npm run fuzz-check`. Add a separate `npm run fuzz-check:gate` shortcut that pins `FUZZ_NUM_RUNS=1000`.

    No CI integration per D-22. The aggregate is for local developer + manual phase gate runs.
  </behavior>
  <action>
    1. Edit `tools/ivm-parity/package.json` `scripts.test`:
       Before: `"test": "npm run fuzz && npm run seed-corpus && npm run synth-p6 && npm run refresh-stats && npm run sweep:hydrate && npm run sweep:advance && npm run report && cat parity_report.md | head -30"`
       After: `"test": "npm run fuzz && npm run seed-corpus && npm run synth-p6 && npm run refresh-stats && npm run sweep:hydrate && npm run sweep:advance && npm run report && npm run fuzz-check && cat parity_report.md | head -30"`

    2. Add a gate-mode shortcut. New script:
       `"fuzz-check:gate": "FUZZ_NUM_RUNS=1000 npm run fuzz-check"`

    3. Document the addition in 34-07-SUMMARY:
       - Default `FUZZ_NUM_RUNS` for `npm run fuzz-check`: 200 (developer-friendly).
       - Gate mode (`npm run fuzz-check:gate`): 1000.
       - Both modes use the post-Phase-34 baseline (Track 2 fixes landed).
       - Allow-list is read-only post-Phase-34 — adding entries requires explicit user approval (per CD-04 + SKILL.md hard rule 1: never modify the test apparatus to hide a divergence).

    4. Verify by running the integrated test (with caches up):
       ```bash
       cd tools/ivm-parity && timeout 1200 npm test
       ```
       Expect exit 0. The full run can take 5-15 minutes (BFS sweep + fast-check 200 iter).

       If timing budget is too long for an interactive plan iteration, document in SUMMARY that the integrated `npm test` is intentional but takes 5-15 min on local hardware.

    5. Per D-22 do NOT add a GitHub Action workflow file. Phase 35 owns CI integration.

    6. Commit package.json change.
  </action>
  <verify>
    <automated>grep -q "fuzz-check.*report\|report.*fuzz-check" tools/ivm-parity/package.json && grep -q "fuzz-check:gate" tools/ivm-parity/package.json && node -e "const pkg = JSON.parse(require('fs').readFileSync('tools/ivm-parity/package.json','utf8')); if (!pkg.scripts['fuzz-check:gate']) process.exit(1); if (!pkg.scripts.test.includes('fuzz-check')) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const pkg = JSON.parse(require('fs').readFileSync('tools/ivm-parity/package.json','utf8')); if (!pkg.scripts.test.includes('fuzz-check')) process.exit(1)"` exits 0.
    - `node -e "const pkg = JSON.parse(require('fs').readFileSync('tools/ivm-parity/package.json','utf8')); if (!pkg.scripts['fuzz-check:gate']) process.exit(1)"` exits 0.
    - `node -e "const pkg = JSON.parse(require('fs').readFileSync('tools/ivm-parity/package.json','utf8')); if (!pkg.scripts['fuzz-check:gate'].includes('FUZZ_NUM_RUNS=1000')) process.exit(1)"` exits 0.
    - The `test` script's order has `fuzz-check` BEFORE `cat parity_report.md` and AFTER `report` — `node -e "const t = JSON.parse(require('fs').readFileSync('tools/ivm-parity/package.json','utf8')).scripts.test; const i = t.indexOf('fuzz-check'); const r = t.indexOf('npm run report'); const c = t.indexOf('cat'); if (!(r < i && i < c)) process.exit(1)"` exits 0.
    - Manual (with caches running): `cd tools/ivm-parity && timeout 1200 npm test` exits 0 (note: long-running; can be deferred to phase ship gate).
    - No CI workflow file added: `find .github/workflows -name "*ivm-parity*" -o -name "*phase-34*" 2>/dev/null | wc -l | grep -q "^0$"`.
  </acceptance_criteria>
  <done>npm test integrates fuzz-check after report step; fuzz-check:gate shortcut for 1k iter; allow-list is read-only baseline; no CI workflow added (D-22).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 1k fuzz harness → both caches | random AST stress test against post-Track-2 baseline; any new divergence surfaces here |
| allow-list entries | trust boundary on what counts as "expected" — must be explicit + reviewed per CD-04 |
| bench results → milestones log | append-only; historical perf record cannot be rewritten |
| PER_TABLE_HITS counter | additive instrumentation — output to stdout only; no test apparatus mutation per SKILL.md hard rule 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-19 | Information Disclosure (silent gate failure) | 1k fuzz finds new divergence; operator silently adds to allow-list to make gate green | mitigate | Task 1 acceptance: every new divergence must have a regression test stub AND a PARITY_STATUS.md entry AND a deferred-fix plan reference; allow-list expansion requires explicit user approval per CD-04 + SKILL.md hard rule 1 |
| T-34-20 | DoS (timeout) | 1k fuzz runs over 2 minutes due to schema growth from FUZZ-02 | mitigate | 34-03 timing probe pre-validated; if breached, Task 1 step 6 fails the WALL_MS check explicitly; document in SUMMARY and escalate to 34-04 schema scope review (don't silence by reducing NUM_RUNS) |
| T-34-21 | Tampering (bench manipulation) | bench numbers selectively reported to make gate pass | mitigate | Task 1 specifies append-only format with both raw numbers AND ratios; full /tmp/*.log files preserved for audit |
| T-34-22 | Tampering (coverage masking) | operator silently lowers FUZZ-02 hit floor below 10 to make gate pass while schema is not actually exercised | mitigate | Acceptance criteria pin floor at 10 (≥1% of 1k); failure path in step 6 explicitly says "Do NOT silently lower the floor" — instead re-weight arb-ast.ts table arb |
</threat_model>

<verification>
- All 5 D-21 sub-conditions answered YES with evidence in 34-07-SUMMARY.
- PER_TABLE_HITS coverage gate met: events, big_id_records, event_tags each ≥10 hits in 1k run.
- WALL_MS timing gate met: <120000ms.
- v5.0-bench-results.md has Phase 34 section with concrete numbers including per-table hit distribution.
- PARITY_STATUS.md updated with Phase 34 ship status and any new divergences.
- tools/ivm-parity/ npm test integrates fuzz-check.
- No CI workflow added (D-22 honored).
</verification>

<success_criteria>
- random-ast-fuzz.ts emits machine-parseable WALL_MS and PER_TABLE_HITS lines.
- 1k fast-check fuzz <120 seconds (machine-checked), 0 unexpected divergences (allow-list filters 5 catalogued + 4 deferred), FUZZ-02 schema exercised (each new table ≥10 hits).
- Phase 33 benches PASS thresholds (TTFB <1.5×, MemPeak ≥4×) post-Track-2.
- Full cargo test green for both Rust crates.
- Full vitest green for view-syncer plus 1k streaming-vs-buffered fuzz.
- tools/ivm-parity npm test exits 0.
- All artifacts committed: instrumented driver, bench results appended, PARITY_STATUS updated, npm scripts wired.
- 34-07-SUMMARY answers D-21 #1-5 explicitly with evidence references.
</success_criteria>

<output>
After completion, create `.planning/phases/34-differential-fuzz-schema-extension/34-07-SUMMARY.md` documenting:
  - All wall-clock timings (cargo test, vitest, 1k fuzz WALL_MS, bench).
  - 1k fuzz PER_TABLE_HITS distribution — explicitly note events/big_id_records/event_tags counts vs floor.
  - 1k fuzz divergence count + which are allow-listed + which are NEW (with regression-test stubs filed).
  - Phase 33 bench raw numbers + ratios + PASS/FAIL.
  - D-21 sub-condition answers (5 explicit YES/NO with evidence).
  - Track 2 ship summary (B1/B2/B3/B11 commit hashes).
  - Phase 35 backlog seed: list of new divergences logged plus any deferred items still pending.
  - Decision: PHASE 34 SHIP (yes/no) + reasoning.
</output>
</content>
</invoke>