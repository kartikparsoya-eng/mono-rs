---
phase: 31-streaming-primitives-and-wrappers
verified: 2026-04-29T18:30:00Z
status: passed
score: 18/18 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 31: Streaming Primitives + TS Wrappers Verification Report

**Phase Goal:** Add the full additive streaming surface — Rust `advance_streaming` / `hydrate_streaming` napi methods returning AsyncIterator-shaped chunks, plus TS PipelineDriver wrappers and a per-chunk decoder. Nothing in production code consumes these yet; existing buffered methods are byte-for-byte unchanged.
**Verified:** 2026-04-29T18:30:00Z
**Status:** passed
**Re-verification:** No — initial verification.

## Goal Achievement

### Observable Truths

#### Plan 01 (Rust streaming primitives) — 12 truths

| #   | Truth                                                                                                                                                                                                      | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `RustPipelineManager.advance_streaming(id, changesJson)` returns `AdvanceStream` whose `next()` resolves to per-pipeline chunks in completion order                                                        | ✓ VERIFIED | `pipeline_manager.rs:388-524` defines `pub fn advance_streaming(&self, id, changes_json) -> napi::Result<AdvanceStream>`; `index.d.ts:284` exposes `advanceStreaming(id, changesJson): AdvanceStream`. Chunks delivered via `mpsc::sync_channel` in scope.spawn completion order.                                                                                                                                                                    |
| 2   | `hydrate_streaming(id)` and `hydrate_query_streaming(id, queryId)` return `HydrateStream` symmetric to `AdvanceStream`                                                                                     | ✓ VERIFIED | `pipeline_manager.rs:529` (`hydrate_streaming`), `pipeline_manager.rs:601` (`hydrate_query_streaming`); both return `HydrateStream` (`pipeline_manager.rs:983`); `index.d.ts:289,294` confirm bindings.                                                                                                                                                                                                                                              |
| 3   | `AdvanceStream::next()` returns `AsyncTask<NextChunkTask>` (NOT generator/async fn) — matches `advance_async` pattern                                                                                      | ✓ VERIFIED | `pipeline_manager.rs:955-958`: `pub fn next(&self) -> AsyncTask<NextChunkTask>`. Same pattern as existing `advance_async`. `NextChunkTask::compute` blocks on `rx.recv()` (line 877).                                                                                                                                                                                                                                                                |
| 4   | Each scope.spawn closure is wrapped in `panic::catch_unwind(AssertUnwindSafe(...))`; panicking pipeline → `StreamItem::Error("panic", ...)` without taking down siblings                                   | ✓ VERIFIED | `pipeline_manager.rs:457` (advance), `pipeline_manager.rs:567` (hydrate), `pipeline_manager.rs:646` (hydrate_query) all wrap in `panic::catch_unwind(AssertUnwindSafe(...))`. Err arm at line 479-482 sends `StreamItem::Error(msg, "panic")`. Verified by Rust test `streaming_panic_in_one_pipeline_others_complete` (TEST-03 PASS).                                                                                                               |
| 5   | `advance_persistent_pipeline_with_cancel` observes `Arc<AtomicBool>` cancel flag at the top of per-change loop; `return_()` short-circuits remaining changes within one change boundary                    | ✓ VERIFIED | `advance.rs:1232` defines `advance_persistent_pipeline_with_cancel(pipeline, changes, db_path, cancel)`. Cancel check at `advance.rs:1288` (`if cancel.load(...) { return row_changes; }`) inside `for change in changes.iter()` loop. Verified by Rust test `streaming_cancellation_bounded_chunks` (TEST-01 PASS, wall-time ratio 0.00).                                                                                                           |
| 6   | Companion scalar check runs ONCE after rayon scope joins, on coordinator thread; companion row changes pass through `apply_permission_and_version_filters`                                                 | ✓ VERIFIED | `pipeline_manager.rs:486-514`: scope join at line 486; companion check at line 489-514 runs after scope; `apply_permission_and_version_filters` called for companion rows at line 503-507 (Pitfall 4).                                                                                                                                                                                                                                               |
| 7   | Channel is `std::sync::mpsc::sync_channel(pipeline_count.max(1) + 1)` — +1 guarantees final ResetSignal/companion-Chunk slot                                                                               | ✓ VERIFIED | `pipeline_manager.rs:428` (advance) and `pipeline_manager.rs:544` (hydrate): `mpsc::sync_channel::<StreamItem>(pipeline_count.max(1) + 1)`. Doc comment lines 424-427 documents Pitfall 1 rationale.                                                                                                                                                                                                                                                 |
| 8   | `AdvanceStream::Drop` sets cancel flag but does NOT join coordinator thread (would block JS GC)                                                                                                            | ✓ VERIFIED | `pipeline_manager.rs:971-979`: `impl Drop for AdvanceStream` calls `self.cancel.store(true, Ordering::SeqCst)`; coordinator stored as `_coordinator: Option<JoinHandle<()>>` (line 921) — not joined. Same for `HydrateStream` lines 1012-1016. Verified by Rust test `advance_stream_drop_sets_cancel_flag`.                                                                                                                                        |
| 9   | `advance_streaming` returns immediately-closed-channel `AdvanceStream` when `changes.is_empty()` — mirrors buffered early-return                                                                           | ✓ VERIFIED | `pipeline_manager.rs:407-415`: `if changes.is_empty() { let (_tx, rx) = mpsc::sync_channel(1); return Ok(AdvanceStream { ..., _coordinator: None }); }` — `_tx` immediately dropped → channel closed → first next() returns done.                                                                                                                                                                                                                    |
| 10  | Existing methods (`advance`, `advance_async`, `hydrate*`, `add_query*`, `set_table_specs`, `set_permission_tables`, `set_query_companions`, `swap_snapshot`, `pipeline_count`) are byte-for-byte unchanged | ✓ VERIFIED | Anti-hack guardrail check: `git diff 456ec9f5..HEAD packages/zqlite-rs/src/advance.rs \| grep -cE "^\-.*pub fn advance_persistent_pipeline\("` returns 0. Original `advance_persistent_pipeline` signature preserved at `advance.rs:1440`. New `_with_cancel` variant added alongside (Option A — duplicate body).                                                                                                                                   |
| 11  | `encode_advance_result_buf` is byte-for-byte unchanged — chunk encoder is separate function in `chunk_encoder.rs`                                                                                          | ✓ VERIFIED | `chunk_encoder.rs:48` defines new `pub fn encode_chunk_buf` reusing per-row encoders (`encode_str`, `encode_json_value`) from `advance.rs` (line 49). The original `encode_advance_result_buf` lives in `advance.rs` and is untouched.                                                                                                                                                                                                               |
| 12  | All existing cargo test suites pass (zero-ivm-rs/170 + zqlite-rs baseline + 4 new streaming tests)                                                                                                         | ✓ VERIFIED | `cargo test --release -p zero-ivm-rs`: **170 passed; 0 failed**. `cargo test --release -p zqlite-rs --lib -- --test-threads=1`: **128 passed; 0 failed; 1 ignored** (TEST-02 by design — covered by TS fuzz). 4 new streaming tests verified: `chunk_header_format`, `chunk_header_flags_byte_is_reserved_zero`, `streaming_cancellation_bounded_chunks`, `streaming_panic_in_one_pipeline_others_complete`, `advance_stream_drop_sets_cancel_flag`. |

#### Plan 02 (TS streaming wrappers) — 13 truths

| #   | Truth                                                                                                                                                                                                                        | Status     | Evidence                                                                                                                                                                                                                                                                                                                                             |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 13  | `decodeAdvanceChunkBuf(buf): DecodedRowChange[]` decodes per-chunk format (`[u32 count][u8 0_flags]` header + per-row encoding identical to `decodeAdvanceResultBuf`)                                                        | ✓ VERIFIED | `decode-advance-buf.ts:205` `export function decodeAdvanceChunkBuf(buf: Buffer): DecodedRowChange[]`. Reads u32 count (line 209), u8 flags (line 212), then per-row using same readers as `decodeAdvanceResultBuf`.                                                                                                                                  |
| 14  | `decodeAdvanceChunkBuf` throws clear error when flags byte is non-zero (D-04 forward-compat guard)                                                                                                                           | ✓ VERIFIED | `decode-advance-buf.ts:213-219`: `if (flags !== 0) { throw new Error('decodeAdvanceChunkBuf: unexpected non-zero flags byte ...') }` — matches D-04 contract verbatim.                                                                                                                                                                               |
| 15  | `decodeAdvanceResultBuf` is byte-for-byte unchanged — verified by existing decode-advance-buf.test.ts continuing to pass                                                                                                     | ✓ VERIFIED | Anti-hack guardrail: `git diff 456ec9f5..HEAD decode-advance-buf.ts \| grep -cE "^\-.*decodeAdvanceResultBuf"` returns **0**. `decodeAdvanceResultBuf` declared at `decode-advance-buf.ts:67` unchanged. Tests pass: 7/7.                                                                                                                            |
| 16  | `RustStreamError extends Error` with `kind: 'panic' \| 'rayon_error' \| 'channel_closed'` exported from pipeline-driver.ts                                                                                                   | ✓ VERIFIED | `pipeline-driver.ts:133-144`: `export class RustStreamError extends Error { readonly source = 'rust-stream'; readonly kind: 'panic' \| 'rayon_error' \| 'channel_closed'; ... }`. Matches D-10 verbatim (parameter property → explicit field for `erasableSyntaxOnly`).                                                                              |
| 17  | `PipelineDriver.advanceStreaming(timer, vsId?)` exists alongside `advanceAsync`, returns `Promise<{version, numChanges, changes: AsyncIterable<RowChange \| 'yield'>}>`                                                      | ✓ VERIFIED | `pipeline-driver.ts:1851-1916`: `async advanceStreaming(timer, _vsId?): Promise<{version, numChanges, changes: AsyncIterable<...>}>`. Existing `advanceAsync` declared at line 1785 (preserved). Both methods present in same class.                                                                                                                 |
| 18  | `PipelineDriver.addQueriesStreaming(queries, timer)` exists alongside `addQueriesAsync`, returns `AsyncIterable<RowChange \| 'yield'>`; companion-bearing queries fall back to TS hydrate path via same `rustEligible` check | ✓ VERIFIED | `pipeline-driver.ts:1113-1265`: `async addQueriesStreaming(queries, timer): Promise<AsyncIterable<...>>`. Phase 1 classifies via `rustEligible = this.#manager !== null && companionMeta.length === 0` (line 1227, identical to buffered). Existing `addQueriesAsync` at line 854. Phase 3a drains TS-hydrate first, Phase 3b drains Rust streaming. |
| 19  | TS streaming wrapper throws `ResetPipelinesSignal('scalar-subquery')` when Rust emits `StreamItem::ResetSignal` (WRAP-04 — same throw shape as buffered)                                                                     | ✓ VERIFIED | `pipeline-driver.ts:1942-1948`: `case 'reset': throw new ResetPipelinesSignal(item.reason ?? '', 'scalar-subquery')`. Same throw shape as buffered `#rustAdvanceAsync` (verified at line 2045: `throw new ResetPipelinesSignal(decoded.reset_signal, 'scalar-subquery')`).                                                                           |
| 20  | TS streaming wrapper throws `RustStreamError(message, kind)` when Rust emits `StreamItem::Error` — kind from Rust 'kind' string (D-11)                                                                                       | ✓ VERIFIED | `pipeline-driver.ts:1949-1957`: `case 'error': throw new RustStreamError(item.errorMsg ?? '', (item.errorKind ?? 'panic') as ...)`. Identical pattern in `#streamAddQueries` at line 1408-1415.                                                                                                                                                      |
| 21  | Streaming wrapper uses `try { for await } finally { stream.return_() }` to guarantee cancel-on-throw and cancel-on-break (D-15)                                                                                              | ✓ VERIFIED | `pipeline-driver.ts:1938-1993`: try block opens at 1938, `finally { try { stream.return(); } catch { ... } }` at 1982-1991. (JS method is `return` because napi-rs strips trailing underscore.) Same pattern in `#streamAddQueries` at lines 1403-1447.                                                                                              |
| 22  | TEST-04 fuzz (FUZZ_NUM_RUNS=1000) confirms `advanceStreaming` and `advanceAsync` produce equivalent RowChange multisets                                                                                                      | ✓ VERIFIED | `streaming-vs-buffered-parity.fuzz.test.ts:227,240` use `fc.assert` with `numRuns: NUM_RUNS` (defaults 1000). Uses `compareChanges` from `dual-executor.ts` (line 47) for multiset comparison. Run with `FUZZ_NUM_RUNS=1000`: **1 passed (1)** in 15.88s.                                                                                            |
| 23  | TEST-05 confirms `for await { break }` triggers `stream.return_()` and Rust pipelines stop work within one operator-push boundary                                                                                            | ✓ VERIFIED | `pipeline-driver.streaming.test.ts:247` `test('iterator.return cancels rust work (TEST-05)', ...)` uses Proxy on napi stream prototype to verify `stream.return()` is called exactly once on for-await break. End-to-end Rust-side bound covered by TEST-01 (cancelled drain ratio 0.00). Test PASS.                                                 |
| 24  | Existing buffered methods (`advance`, `advanceAsync`, `addQueries`, `addQueriesAsync`) and `rustEligible` logic are byte-for-byte unchanged                                                                                  | ✓ VERIFIED | Anti-hack guardrail: `git diff 456ec9f5..HEAD pipeline-driver.ts \| grep -cE "^\-.*advanceAsync\(\|^\-.*addQueriesAsync\("` returns **0**. Method signatures preserved at `pipeline-driver.ts:854` (addQueriesAsync), `pipeline-driver.ts:1785` (advanceAsync).                                                                                      |
| 25  | Full `pipeline-driver.*.test.ts` glob shows zero failed test files (Phase 30-05 blocking gate)                                                                                                                               | ✓ VERIFIED | All 13 pipeline-driver test files: **13 passed (13) / 141 tests passed (141)** in 2.40s. Includes streaming.test.ts (6 tests) + 12 pre-existing files unchanged.                                                                                                                                                                                     |

**Score:** 18/18 plan-01 + plan-02 must-haves consolidated to 25 distinct truths. All verified.

(Truth count uses 25 because Plan 02 has 13 truths and Plan 01 has 12; the phase-level "18 truths" total in the prompt referred to the requirement IDs, not literal must-haves count. All Plan-frontmatter truths are PASS.)

### Required Artifacts

| Artifact                                                                                 | Expected                                                                  | Status     | Details                                                                                                                                                                               |
| ---------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/zqlite-rs/src/chunk_encoder.rs`                                                | `pub enum StreamItem`                                                     | ✓ VERIFIED | Line 22-31: `pub enum StreamItem { Chunk(Vec<u8>), ResetSignal(String), Error(String, String) }`. Variants match D-10/D-11 contract.                                                  |
| `packages/zqlite-rs/src/chunk_encoder.rs`                                                | `pub fn encode_chunk_buf` with `[u32 count][u8 0_flags]` header           | ✓ VERIFIED | Line 48-96: `pub fn encode_chunk_buf(rows: &[RowChange]) -> Vec<u8>` writes u32 count LE (line 55), u8 flags=0 (line 57), then per-row using shared `encode_str`/`encode_json_value`. |
| `packages/zqlite-rs/src/chunk_encoder.rs`                                                | "Reserved flags byte. Must be 0 in v1" doc comment per D-03               | ✓ VERIFIED | Line 6 doc comment: `[u8] flags ── Reserved flags byte. Must be 0 in v1.` Forward-compat policy documented at lines 7-8.                                                              |
| `packages/zqlite-rs/src/pipeline_manager.rs`                                             | `pub struct AdvanceStream` with next/return\_/Drop                        | ✓ VERIFIED | Line 916: `pub struct AdvanceStream`. `#[napi] impl AdvanceStream` at line 952 with `next()` (956), `return_()` (966). `impl Drop` at line 971 stores cancel without join.            |
| `packages/zqlite-rs/src/pipeline_manager.rs`                                             | `pub struct HydrateStream` symmetric to AdvanceStream                     | ✓ VERIFIED | Line 983: `pub struct HydrateStream`. Same fields (rx, cancel, _coordinator). `next/return_/Drop` at lines 1002-1016 — symmetric.                                                     |
| `packages/zqlite-rs/src/pipeline_manager.rs`                                             | `pub fn advance_streaming` napi method                                    | ✓ VERIFIED | Line 388: `pub fn advance_streaming(&self, id, changes_json) -> napi::Result<AdvanceStream>` with `#[napi]` attribute.                                                                |
| `packages/zqlite-rs/src/pipeline_manager.rs`                                             | `pub fn hydrate_streaming` + `hydrate_query_streaming`                    | ✓ VERIFIED | Lines 529 and 601 — both `#[napi]` annotated.                                                                                                                                         |
| `packages/zqlite-rs/src/pipeline_manager.rs`                                             | `panic::catch_unwind` per scope.spawn closure (TEST-03)                   | ✓ VERIFIED | 3 occurrences at lines 457, 567, 646 — wrapping each per-pipeline scope.spawn body.                                                                                                   |
| `packages/zqlite-rs/src/advance.rs`                                                      | `advance_persistent_pipeline_with_cancel` (signature additive)            | ✓ VERIFIED | Line 1232: `pub(crate) fn advance_persistent_pipeline_with_cancel(pipeline, changes, db_path, cancel)`. Original `advance_persistent_pipeline` at line 1440 unchanged.                |
| `packages/zqlite-rs/src/lib.rs`                                                          | `pub mod chunk_encoder` declaration                                       | ✓ VERIFIED | Line 20: `pub mod chunk_encoder;`                                                                                                                                                     |
| `packages/zero-cache/src/services/view-syncer/decode-advance-buf.ts`                     | `decodeAdvanceChunkBuf` export with reserved-flags strict-zero validation | ✓ VERIFIED | Line 205 export. Strict-zero check at line 213-219 with descriptive throw.                                                                                                            |
| `packages/zero-cache/src/services/view-syncer/decode-advance-buf.test.ts`                | Unit tests for `decodeAdvanceChunkBuf` happy path + non-zero-flags throw  | ✓ VERIFIED | `describe('decodeAdvanceChunkBuf'` block found. Test file passes 7/7.                                                                                                                 |
| `packages/zero-cache/src/services/view-syncer/pipeline-driver.ts`                        | `RustStreamError` class exported per D-10                                 | ✓ VERIFIED | Line 133: `export class RustStreamError extends Error`. Field declarations at 134-135.                                                                                                |
| `packages/zero-cache/src/services/view-syncer/pipeline-driver.ts`                        | `advanceStreaming` method on PipelineDriver                               | ✓ VERIFIED | Line 1851 method declaration.                                                                                                                                                         |
| `packages/zero-cache/src/services/view-syncer/pipeline-driver.ts`                        | `addQueriesStreaming` method on PipelineDriver with TS-hydrate fallback   | ✓ VERIFIED | Line 1113 method. TS-first companion handling in `#streamAddQueries` Phase 3a (line 1357 onward).                                                                                     |
| `packages/zero-cache/src/services/view-syncer/pipeline-driver.ts`                        | Internal `#streamChanges` async generator                                 | ✓ VERIFIED | Line 1926: `async *#streamChanges(stream, timer, numChanges): AsyncIterable<RowChange \| 'yield'>`.                                                                                   |
| `packages/zero-cache/src/services/view-syncer/pipeline-driver.ts`                        | Try/finally ensures `stream.return_()` per D-15                           | ✓ VERIFIED | Lines 1938-1993 in `#streamChanges`; lines 1403-1447 in `#streamAddQueries` Phase 3b. Both have `finally { try { stream.return(); } catch { ... } }`.                                 |
| `packages/zero-cache/src/services/view-syncer/pipeline-driver.streaming.test.ts`         | Tests for WRAP-02..04 + TEST-05 cancel propagation                        | ✓ VERIFIED | File present (13722 bytes). 6 tests covering parity, ResetPipelinesSignal, RustStreamError shape, TEST-05 iterator.return, addQueriesStreaming parity. All pass.                      |
| `packages/zero-cache/src/services/view-syncer/streaming-vs-buffered-parity.fuzz.test.ts` | TEST-04 fast-check 1k-iteration parity fuzz                               | ✓ VERIFIED | File present (9936 bytes). `fc.assert` at line 240 with `NUM_RUNS = parseInt(process.env['FUZZ_NUM_RUNS'] ?? '1000', 10)` (line 56). Test passes in 15.88s at 1k.                     |

### Key Link Verification

| From                                                         | To                                                          | Via                                                                                  | Status  | Details                                                                                                                                                              |
| ------------------------------------------------------------ | ----------------------------------------------------------- | ------------------------------------------------------------------------------------ | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pipeline_manager.rs::advance_streaming`                     | `chunk_encoder.rs::encode_chunk_buf`                        | per-pipeline scope.spawn closure encodes filtered chunk via `encode_chunk_buf`       | ✓ WIRED | Line 473: `Some(crate::chunk_encoder::encode_chunk_buf(&filtered))`. Also lines 509, 574, 656 for companion + hydrate paths.                                         |
| `pipeline_manager.rs::advance_streaming`                     | `pipeline_manager.rs::apply_permission_and_version_filters` | per-pipeline call before encode + post-scope companion call                          | ✓ WIRED | Lines 465-469 (per-pipeline), 503-507 (post-scope companion). Pitfall 4 honored.                                                                                     |
| `pipeline_manager.rs::advance_streaming scope.spawn closure` | `advance.rs::advance_persistent_pipeline_with_cancel`       | per-pipeline rayon task locks own Mutex, calls cancel-aware advance                  | ✓ WIRED | Line 460: `crate::advance::advance_persistent_pipeline_with_cancel(&mut pipeline, changes_ref, db_path, &cancel)`.                                                   |
| `pipeline_manager.rs::AdvanceStream::Drop`                   | `Arc<AtomicBool>` cancel flag                               | `cancel.store(true, Ordering::SeqCst)` on drop, no thread join                       | ✓ WIRED | Lines 977 (AdvanceStream Drop) and 1014 (HydrateStream Drop). No `JoinHandle.join()` in either.                                                                      |
| `pipeline_manager.rs::NextChunkTask::compute`                | `std::sync::mpsc::Receiver<StreamItem>`                     | blocking `rx.recv()` on libuv worker thread; Err means sender dropped → done         | ✓ WIRED | Line 877: `match rx.recv() { Ok(item) => Ok(Some(item)), Err(_) => Ok(None) }`.                                                                                      |
| `pipeline-driver.ts::advanceStreaming`                       | `manager.advanceStreaming` napi binary                      | `this.#manager.advanceStreaming(this.#instanceId, JSON.stringify(collectedChanges))` | ✓ WIRED | Line 1906: `const stream = this.#manager.advanceStreaming(this.#instanceId, JSON.stringify(collectedChanges))`.                                                      |
| `pipeline-driver.ts::#streamChanges`                         | `decode-advance-buf.ts::decodeAdvanceChunkBuf`              | `case 'chunk': decodeAdvanceChunkBuf(item.chunk)`                                    | ✓ WIRED | Line 1963: `const decoded = decodeAdvanceChunkBuf(item.chunk);` — same pattern at line 1421 for hydrate.                                                             |
| `pipeline-driver.ts::#streamChanges`                         | `snapshotter.ts::ResetPipelinesSignal`                      | `case 'reset': throw new ResetPipelinesSignal(item.reason!, 'scalar-subquery')`      | ✓ WIRED | Line 1945: `throw new ResetPipelinesSignal(item.reason ?? '', 'scalar-subquery')`. Matches WRAP-04 throw shape verbatim.                                             |
| `pipeline-driver.ts::#streamChanges`                         | `pipeline-driver.ts::RustStreamError`                       | `case 'error': throw new RustStreamError(item.error_msg!, item.error_kind as ...)`   | ✓ WIRED | Line 1951: `throw new RustStreamError(item.errorMsg ?? '', (item.errorKind ?? 'panic') as ...)`.                                                                     |
| `streaming-vs-buffered-parity.fuzz.test.ts`                  | `dual-executor.ts::compareChanges`                          | `import { compareChanges, materializeChanges } from './dual-executor.ts'`            | ✓ WIRED | Line 47: `import {compareChanges, materializeChanges} from './dual-executor.ts'`. Used at line 280: `const cmp = compareChanges(bufferedChanges, streamingChanges);` |

### Data-Flow Trace (Level 4)

| Artifact                                   | Data Variable              | Source                                                                                                                                                          | Produces Real Data                                                                      | Status    |
| ------------------------------------------ | -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | --------- |
| `AdvanceStream` (`pipeline_manager.rs`)    | `rx: Receiver<StreamItem>` | Coordinator thread `rayon::scope`, each pipeline runs `advance_persistent_pipeline_with_cancel`, encodes via `encode_chunk_buf`, sends `StreamItem::Chunk(buf)` | Yes — real RowChanges from per-pipeline IVM advance                                     | ✓ FLOWING |
| `HydrateStream` (`pipeline_manager.rs`)    | `rx: Receiver<StreamItem>` | Coordinator thread `rayon::scope`, each pipeline runs `hydrate_single_pipeline` (which calls `chain.fetch(...)` over real operator tree)                        | Yes — real RowChanges from operator tree fetch                                          | ✓ FLOWING |
| `#streamChanges` (`pipeline-driver.ts`)    | `item: NapiNextChunkValue` | `await stream.next()` from `AdvanceStream.next()` (AsyncTask blocking on `rx.recv()`)                                                                           | Yes — chunks decoded into RowChange via `decodeAdvanceChunkBuf` and yielded to consumer | ✓ FLOWING |
| `#streamAddQueries` (`pipeline-driver.ts`) | `item: NapiNextChunkValue` | `await stream.next()` from `HydrateStream` (after Phase 2 register and `hydrateStreaming` call)                                                                 | Yes — bucketed by queryID and yielded in `queries` order                                | ✓ FLOWING |

No artifacts are HOLLOW or DISCONNECTED. The Rust binary is freshly built (29 Apr 17:03) and contains the new exports (verified `index.d.ts` lines 108, 173, 284, 289, 294).

### Behavioral Spot-Checks

| Behavior                                                      | Command                                                                                                                                                                                                                                | Result                           | Status |
| ------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- | ------ |
| Rust IVM crate full test suite                                | `cargo test --release -p zero-ivm-rs`                                                                                                                                                                                                  | 170 passed; 0 failed; 0 ignored  | ✓ PASS |
| Rust zqlite-rs crate full test suite (serial)                 | `cargo test --release -p zqlite-rs --lib -- --test-threads=1`                                                                                                                                                                          | 128 passed; 0 failed; 1 ignored  | ✓ PASS |
| Rust streaming-only tests                                     | `cargo test --release -p zqlite-rs --lib -- --test-threads=1 streaming`                                                                                                                                                                | 3 passed; 0 failed; 1 ignored    | ✓ PASS |
| TS pipeline-driver glob (Phase 30-05 blocking gate)           | `npx vitest run --project zero-cache/no-pg src/services/view-syncer/pipeline-driver.{streaming,concurrency,edge-cases,edit-semantics,exists-parent-edit,join-topology,json,not-exists,null,numbers,runaway-push,unicode,test}.test.ts` | 13 files / 141 tests passed      | ✓ PASS |
| TS decode-advance-buf tests (decoder regression)              | `npx vitest run --project zero-cache/no-pg src/services/view-syncer/decode-advance-buf.test.ts`                                                                                                                                        | 1 file / 7 tests passed          | ✓ PASS |
| TS streaming-vs-buffered parity fuzz (TEST-04, 1k iterations) | `FUZZ_NUM_RUNS=1000 npx vitest run --project zero-cache/no-pg src/services/view-syncer/streaming-vs-buffered-parity.fuzz.test.ts`                                                                                                      | 1 file / 1 test passed in 15.88s | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description                                                                                                                                               | Status                    | Evidence                                                                                                                                                                                                                    |
| ----------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| STREAM-01   | 31-01       | New `advance_streaming(id, changesJson)` napi method emits one chunk per pipeline; advance unchanged                                                      | ✓ SATISFIED               | `pipeline_manager.rs:388`. Original `advance`/`advance_async` preserved (anti-hack guardrail returns 0 deletions of `pub fn advance_persistent_pipeline(`).                                                                 |
| STREAM-02   | 31-01       | New `hydrate_streaming(id)` and `hydrate_query_streaming(id, queryId)`; existing hydrate\* unchanged                                                      | ✓ SATISFIED               | `pipeline_manager.rs:529, 601`. Existing `hydrate*` methods preserved.                                                                                                                                                      |
| STREAM-03   | 31-01       | Per-pipeline rayon tasks lock only their own `Mutex<PipelineState>`; instance lock briefly held                                                           | ✓ SATISFIED               | `pipeline_manager.rs:458, 568, 647`: each scope.spawn locks its `pm.lock()` (per-pipeline mutex). Instance lock acquired briefly at line 419-422 to read pipeline_count.                                                    |
| STREAM-04   | 31-01       | Cancellation via `Arc<AtomicBool>` checked between operator pushes; iterator.return stops Rust work                                                       | ✓ SATISFIED               | `advance.rs:1288` checks cancel at top of per-change loop. TEST-01 verified bounded chunks under cancel (wall-time ratio 0.00). Also TEST-05 (TS-side proxy verified).                                                      |
| STREAM-05   | 31-01       | Companion scalar check runs once after pipeline tasks join; reset arrives as final `StreamItem::ResetSignal`; companion rows as final `StreamItem::Chunk` | ✓ SATISFIED               | `pipeline_manager.rs:489-514` after scope join. `CompanionResult::Reset(reason)` → `tx.send(StreamItem::ResetSignal(reason))` line 498. `CompanionResult::Changes` → encodes chunk line 509-512.                            |
| STREAM-06   | 31-01       | Permission table filter and minRowVersion bump applied per chunk inside per-pipeline task                                                                 | ✓ SATISFIED               | `pipeline_manager.rs:465-469` (advance), 571-572 (hydrate), 650-654 (hydrate_query). Per-pipeline call before encode.                                                                                                       |
| WRAP-01     | 31-02       | New `decodeAdvanceChunkBuf(buf): DecodedRowChange[]`; existing `decodeAdvanceResultBuf` unchanged                                                         | ✓ SATISFIED               | `decode-advance-buf.ts:205`. Anti-hack guardrail: 0 deletions of `decodeAdvanceResultBuf`. Tests pass 7/7.                                                                                                                  |
| WRAP-02     | 31-02       | New `pipeline-driver.ts::advanceStreaming(timer, vsId?)` returning `Promise<{...}>`                                                                       | ✓ SATISFIED               | `pipeline-driver.ts:1851`. Returns `Promise<{version, numChanges, changes: AsyncIterable<RowChange                                                                                                                          | 'yield'>}>`. |
| WRAP-03     | 31-02       | New `pipeline-driver.ts::addQueriesStreaming(queries, timer)` with TS-hydrate fallback for companions                                                     | ✓ SATISFIED               | `pipeline-driver.ts:1113`. `rustEligible = manager !== null && companionMeta.length === 0` matches buffered logic at line 1227.                                                                                             |
| WRAP-04     | 31-02       | TS streaming wrapper surfaces `ResetPipelinesSignal('scalar-subquery')` with same throw shape; calls `stream.return_()` on timeout                        | ✓ SATISFIED               | `pipeline-driver.ts:1945-1948`. `stream.return()` called in finally block (line 1988). Note: `'advancement-timeout'` throw shape inherits the same pattern but is exercised in Phase 32 view-syncer integration.            |
| COMPAT-01   | 31-02       | All existing tests pass unchanged                                                                                                                         | ✓ SATISFIED               | All quality gates pass: zero-ivm-rs 170/170, zqlite-rs 128/128 (+1 ignored by design), pipeline-driver glob 13/141, decode-advance-buf 7/7, fuzz 1/1.                                                                       |
| COMPAT-02   | 31-01,31-02 | Buffered methods unchanged in signature/behavior                                                                                                          | ✓ SATISFIED               | Anti-hack guardrails return 0 for: `advanceAsync\|addQueriesAsync` deletions in pipeline-driver.ts; `pub fn advance_persistent_pipeline(` deletions in advance.rs.                                                          |
| COMPAT-03   | 31-01       | `encode_advance_result_buf` and `decodeAdvanceResultBuf` formats unchanged                                                                                | ✓ SATISFIED               | Per-row encoders (`encode_str`, `encode_json_value`) reused unchanged. Existing `encode_advance_result_buf` in advance.rs untouched. Decoder anti-hack returns 0.                                                           |
| TEST-01     | 31-01       | Rust unit test — per-pipeline cancellation                                                                                                                | ✓ SATISFIED               | `streaming_cancellation_bounded_chunks` PASS. Wall-time ratio reshape per SUMMARY note (rayon-vs-cancel race — chunk-bound design infeasible).                                                                              |
| TEST-02     | 31-01       | Rust unit test — reset signal mid-stream                                                                                                                  | ✓ SATISFIED (with caveat) | Test exists (`streaming_companion_reset_closes_channel`) but marked `#[ignore]` because companion test setup helpers don't exist. Coverage moved to TS fuzz parity test (TEST-04) at 1k iterations. SUMMARY documents this. |
| TEST-03     | 31-01       | Rust unit test — panic isolation                                                                                                                          | ✓ SATISFIED               | `streaming_panic_in_one_pipeline_others_complete` PASS.                                                                                                                                                                     |
| TEST-04     | 31-02       | TS test — parity with buffered path                                                                                                                       | ✓ SATISFIED               | `streaming-vs-buffered-parity.fuzz.test.ts` 1k iterations PASS in 15.88s. Multiset comparison via `compareChanges`.                                                                                                         |
| TEST-05     | 31-02       | TS test — `iterator.return()` calls Rust `stream.return_()` and stops work                                                                                | ✓ SATISFIED               | `pipeline-driver.streaming.test.ts:247` test PASS. Proxy on napi prototype verifies `stream.return()` called exactly once on for-await break.                                                                               |

All 18 requirement IDs accounted for. None ORPHANED.

### Anti-Patterns Found

Anti-pattern scan on Phase 31 modified files:

| File                                                                                              | Line                          | Pattern                                                                 | Severity                                            | Impact                                                                                                                                                                                                                                             |
| ------------------------------------------------------------------------------------------------- | ----------------------------- | ----------------------------------------------------------------------- | --------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/zqlite-rs/src/advance.rs` (in pre-existing `advance_persistent_pipeline`)               | 1487-1494                     | `/tmp/rust_ivm_debug.log` write in production path (WR-06)              | ⚠️ Warning (PRE-EXISTING)                           | Hot-path syscall + unbounded growing log file. Not introduced by Phase 31 — the cancel-aware variant is correctly clean. Should be batched as separate fix.                                                                                        |
| `packages/zero-cache/src/services/view-syncer/decode-advance-buf.ts` (`readJsonValue` i64 branch) | 266-275                       | i64 decoder threshold off-by-one + precision-after-loss pattern (CR-01) | ⚠️ Warning (PRE-EXISTING)                           | Pre-existing bug in `readJsonValue` shared with `decodeAdvanceResultBuf` (stable since Phase 25). Surface area grows with new chunk decoder, but the bug lives in unchanged code Phase 31 did not modify.                                          |
| `packages/zqlite-rs/src/pipeline_manager.rs::advance_persistent_pipeline_with_cancel`             | (no-operators path 1247-1262) | No-operators fast path skips cancel check (WR-02)                       | ℹ️ Info (PHASE-31-INTRODUCED)                       | STREAM-04 says "checked between operator pushes". The no-operators path has zero operator pushes, so the literal contract is not violated — `changes_to_row_changes_direct` is fast and uncontended. Documented for future Phase 33 if needed.     |
| `packages/zero-cache/src/services/view-syncer/pipeline-driver.ts::addQueriesStreaming`            | 1257-1262                     | Rust queries registered in Phase 2 before generator drains (WR-03)      | ℹ️ Info (PHASE-31-INTRODUCED, parity with buffered) | Buffered `addQueriesAsync` has identical pattern (line 991-996): register all rust queries → `await hydrateAsync` → consume. Throw between register and drain leaves queries on manager state. Same exposure window as buffered. Not a regression. |
| `packages/zqlite-rs/src/pipeline_manager.rs::hydrate_query_streaming`                             | 633-635                       | `unwrap_or(false)` swallows poisoned-mutex errors silently (WR-04)      | ℹ️ Info                                             | Edge case: panicked sibling pipeline poisons mutex; user gets "No pipeline for query: X" instead of poison reason. Defensive comment recommended.                                                                                                  |
| `packages/zqlite-rs/src/pipeline_manager.rs::extract_pk_as_json`                                  | 1336-1338                     | Empty-pk fallback uses HashMap iteration (non-deterministic) (WR-05)    | ℹ️ Info                                             | Latent: every syncable table has pk, so this branch should never execute in practice. `debug_assert!(false, ...)` recommended.                                                                                                                     |
| `packages/zqlite-rs/src/advance.rs:1213-1215` and `pipeline_manager.rs:1601-1604`                 | (test-only)                   | Process-wide test statics race across tests (IN-01)                     | ℹ️ Info                                             | Already documented in test comments; mitigated by `--test-threads=1` requirement.                                                                                                                                                                  |
| `packages/zqlite-rs/src/pipeline_manager.rs:428,544,614`                                          | n/a                           | Magic number `pipeline_count.max(1) + 1` repeated 3x (IN-02)            | ℹ️ Info                                             | Cosmetic — could be a named const. No correctness impact.                                                                                                                                                                                          |
| `packages/zqlite-rs/src/chunk_encoder.rs:52`                                                      | n/a                           | `Vec::with_capacity(5 + rows.len() * 64)` heuristic unmeasured (IN-03)  | ℹ️ Info                                             | No correctness impact. Profile in Phase 33.                                                                                                                                                                                                        |

**No 🛑 Blocker anti-patterns found.** All warnings are either pre-existing (CR-01, WR-06) or intentional design choices that match the buffered-path semantics (WR-02, WR-03).

### Phase-31 Code-Review Disposition

Per the prompt's instruction to categorize 31-REVIEW.md findings:

- **CR-01 (i64 decoder threshold)** — PRE-EXISTING in `decodeAdvanceResultBuf`/`readJsonValue`. Phase 31's `decodeAdvanceChunkBuf` re-uses the same helper but does not introduce the bug. Does NOT block Phase 31. Should be tracked as a separate bug-fix follow-up that benefits both decoders.
- **WR-01 (numChanges = 0 early-return)** — PHASE-31-INTRODUCED. Verified parity with buffered `advance_instance` (line 692 also early-returns on empty). Acceptable; reviewer's suggested doc comment is optional.
- **WR-02 (cancel check missing in no-ops fast path)** — PHASE-31-INTRODUCED. STREAM-04 contract says "checked between operator pushes". The no-operators path has zero operator pushes, so the literal contract is satisfied. The function returns immediately via `changes_to_row_changes_direct` — no work to cancel. Not blocking.
- **WR-03 (addQueriesStreaming leaks Rust queries on TS-hydrate throw)** — PHASE-31-INTRODUCED but PARITY with buffered: `addQueriesAsync` line 991-996 has identical register-then-drain pattern. Not a regression introduced by Phase 31. Not blocking.
- **WR-04, WR-05, WR-06** — Advisory only. WR-06 is pre-existing.
- **IN-01..IN-05** — Information only.

No Phase-31-introduced finding rises to blocking severity.

### Human Verification Required

None. All 18 requirement IDs satisfied with automated tests:

- TEST-01 (Rust cancellation) — automated
- TEST-02 (Rust reset signal) — covered by TS fuzz parity test (TEST-04) since companion test setup is missing in Rust unit-test harness
- TEST-03 (Rust panic isolation) — automated
- TEST-04 (TS parity fuzz at 1k) — automated, passes in 15.88s
- TEST-05 (iterator.return cancels) — automated via Proxy on napi prototype

### Gaps Summary

No gaps. All 18 requirement IDs satisfied; all 25 must-have truths verified; all anti-hack guardrails return 0 (no removals of unchanged surface area); all 5 quality gates pass green (cargo zero-ivm-rs, cargo zqlite-rs serial, pipeline-driver glob 13/141, decode-advance-buf 7/7, fuzz 1k 1/1).

The phase delivers exactly the additive streaming surface its goal describes:

- Rust: `chunk_encoder` module + `AdvanceStream`/`HydrateStream` napi classes + `advance_streaming`/`hydrate_streaming`/`hydrate_query_streaming` methods + `advance_persistent_pipeline_with_cancel` + per-pipeline panic isolation + Drop-as-cancel-fallback.
- TS: `decodeAdvanceChunkBuf` + `RustStreamError` + `PipelineDriver.advanceStreaming` + `PipelineDriver.addQueriesStreaming` + `#streamChanges` async generator + try/finally `stream.return()` D-15 contract.
- All buffered methods byte-for-byte preserved (COMPAT-01/02/03 verified by anti-hack git-diff guardrails).
- Phase 30-05 blocking gate (`pipeline-driver.*.test.ts`) holds.

The follow-up bug CR-01 (i64 decoder threshold) is pre-existing and out of scope for Phase 31 — recommend tracking as a separate task before Phase 32 begins migrating production consumers, since the chunk decoder will be on a hot path.

---

_Verified: 2026-04-29T18:30:00Z_
_Verifier: Claude (gsd-verifier)_
