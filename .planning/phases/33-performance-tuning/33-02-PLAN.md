---
phase: 33-performance-tuning
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/zero-ivm-rs/src/or_exists_op.rs
autonomous: true
requirements: [HARDEN-02]
requirements_addressed: [HARDEN-02]
tags: [test-coverage, or-exists, ivm, rust, hardening]
must_haves:
  truths:
    - "or_exists_op.rs total test count is ≥22 (and aspirationally up to ~30) — verified by `cargo test --release -p zero-ivm-rs --lib or_exists_op:: -- --list | grep -c '^or_exists_op::tests::test_'`."
    - 'All 5 pre-existing or_exists tests still pass unchanged: test_or_exists_reentrancy_panics, test_or_exists_edit_both_pass, test_or_exists_edit_old_only, test_or_exists_edit_new_only, test_or_exists_edit_neither.'
    - 'Categories covered (mirroring exists_op.rs): fetch (≥3), parent push add/remove (≥3), child push add/remove/edit (≥3), edit no-or_predicate (≥1), edit with-or_predicate (≥4 existing transitions), in-push re-entrancy (1 existing), cache invalidation/recovery (≥2), different-relationship passthrough (≥1), OR-branch combinations (≥2 NEW for D-09), in-push flag cleared (≥1).'
    - 'Production code in or_exists_op.rs is byte-for-byte unchanged outside the tests module (anti-hack guardrail D-28: implementation already correct from Phase 30-03/30-04, this plan is pure test addition).'
    - 'Existing 22 tests in exists_op.rs still pass (we do not refactor any shared helpers in a way that breaks them).'
    - 'All new tests use only Phase-30-04 `force_in_push_for_test` helper or local fixtures — no new test-only public API exposed in production.'
  artifacts:
    - path: packages/zero-ivm-rs/src/or_exists_op.rs
      provides: 'OrExistsOperator unit tests at ≥22 (target ~25–28 per D-11 escape clause)'
      contains: 'test_or_exists_fetch'
  key_links:
    - from: packages/zero-ivm-rs/src/or_exists_op.rs::tests
      to: OrExistsOperator::new
      via: 'Tests construct OrExistsOperator via the existing public constructor with branches: Vec<(Box<dyn Operator>, String, bool, Vec<String>, Vec<String>)>'
      pattern: "OrExistsOperator::new\\("
    - from: packages/zero-ivm-rs/src/or_exists_op.rs::test_or_exists_in_push_flag_cleared_after_push
      to: OrExistsOperator::force_in_push_for_test
      via: 'Direct call after constructing fixture; only #[cfg(test)] gated'
      pattern: 'force_in_push_for_test'
---

<objective>
Close the 4.4× test-breadth gap between `exists_op.rs` (22 tests) and `or_exists_op.rs` (5 tests). Mirror the exists_op.rs categorical structure plus add the OR-specific multi-branch combination tests called out by CONTEXT D-09.

This plan adds tests ONLY (D-28 anti-hack guardrail). The OrExistsOperator implementation is already correct from Phases 30-03 (AUDIT-04 4-transition fix) and 30-04 (AUDIT-03 debug_assert promotion at or_exists_op.rs:170). We are filling the verification deficit, not fixing bugs.

Per RESEARCH §1.7 the actual current counts are 22 (exists) and 5 (or_exists), not 44 and 10 as CONTEXT.md originally stated. The "≥22" target now means parity with `exists_op.rs` test count; the aspirational "≥30" remains stretch (per CONTEXT D-11 escape clause: stop at 25-28 if reaching higher requires contrived padding).

Output: ~17–25 new test functions in `or_exists_op.rs::tests` covering the 9 categories enumerated in `<behavior>` of Tasks 1–3.
</objective>

<execution_context>
@/Users/kartik.parsoya/Documents/Zero/mono-rs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kartik.parsoya/Documents/Zero/mono-rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-performance-tuning/33-CONTEXT.md
@.planning/phases/33-performance-tuning/33-RESEARCH.md
@.planning/phases/33-performance-tuning/33-VALIDATION.md
@.planning/phases/30-audit-fixes/30-03-SUMMARY.md
@.planning/phases/30-audit-fixes/30-04-SUMMARY.md
@./CLAUDE.md
@./AGENTS.md
@packages/zero-ivm-rs/src/or_exists_op.rs
@packages/zero-ivm-rs/src/exists_op.rs

<interfaces>
<!-- Test fixture contracts the executor needs. Already present unless noted. -->

From packages/zero-ivm-rs/src/or_exists_op.rs (current public surface):

```rust
pub struct OrExistsOperator { /* fields private */ }

impl OrExistsOperator {
    pub fn new(
        input: Box<dyn Operator>,
        branches: Vec<(
            Box<dyn Operator>, // child
            String,            // relationship_name
            bool,              // not_exists
            Vec<String>,       // parent_key
            Vec<String>,       // child_key
        )>,
        or_predicate: Option<Predicate>,
    ) -> Self;

    #[cfg(test)]
    pub(crate) fn force_in_push_for_test(&mut self);
}

impl Operator for OrExistsOperator {
    fn fetch(&mut self, req: &FetchRequest) -> Vec<Node>;
    fn push(&mut self, change: Change) -> Vec<Change>;
    fn op_type(&self) -> &'static str;
}
```

From packages/zero-ivm-rs/src/types.rs:

```rust
pub struct Node { pub row: Row, pub relationships: HashMap<String, Vec<Node>> }
pub enum Change {
    Add(Node),
    Remove(Node),
    Edit { node: Node, old_node: Node },
    Child { node: Node, child: ChildData },
}
pub struct ChildData { pub relationship_name: String, pub change: Box<Change> }
pub struct FetchRequest { pub constraint: Option<Constraint>, pub start: Option<Start>, pub reverse: bool }
```

From packages/zero-ivm-rs/src/filter.rs:

```rust
pub enum Predicate { Eq(String, Value), Ne(String, Value), /* … */ }
pub enum Value { String(String), Number(f64), Bool(bool), Null }
```

Test helpers ALREADY in or_exists_op.rs::tests (DO NOT redefine):

- `struct MockInput { nodes: Vec<Node> }` impl Operator
- `fn make_node(id: i64) -> Node`
- `fn make_status_edit(id, old_status, new_status) -> Change` (status-edit fixture)
- `fn build_or_exists_with_status_predicate() -> OrExistsOperator` (single-branch with status predicate)

Test helpers in exists_op.rs::tests to MIRROR (copy/adapt to or_exists, not import):

- `fn make_node_with_parent(id: i64, parent_id: i64) -> Node` — needed for child rows in fetch/push tests
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fetch + parent-push + child-push tests (categories 1–4: fetch, parent add/remove, child push)</name>
  <files>packages/zero-ivm-rs/src/or_exists_op.rs</files>
  <read_first>
    - packages/zero-ivm-rs/src/or_exists_op.rs full file (existing test module structure, helpers)
    - packages/zero-ivm-rs/src/exists_op.rs lines 408-560 (helpers + fetch/parent/child push tests to mirror)
    - .planning/phases/33-performance-tuning/33-RESEARCH.md §4 (test list with 30 suggested names)
  </read_first>
  <behavior>
    All tests are pure additions inside the existing `mod tests` block (line 449 of or_exists_op.rs). No production-code touches.

    **Category: Fetch (~3 tests)**
    - `test_or_exists_fetch_filters_parents_without_children`: 3 parents, no matching children, no or_predicate → fetch returns rows where `MockInput`'s lack of constraint filtering means all parents come through (mirror exists_op.rs:441-464 caveat in comment).
    - `test_or_exists_fetch_or_predicate_short_circuits`: 1 parent with `status=active`, no matching children, `or_predicate = Eq("status", "active")` → fetch returns the parent (or_predicate matched, child count irrelevant). 1 parent with `status=inactive` → does NOT pass (no predicate match, no children).
    - `test_or_exists_fetch_with_parent_constraint`: fetch is called with `FetchRequest { constraint: Some(Constraint::from_pairs([("id", json!(1))])), .. }` — verify operator forwards the constraint and returns only the constrained parent (note: `MockInput` ignores constraints, so this primarily verifies the call doesn't panic and the returned vec length matches MockInput.nodes.len() — same caveat as exists_op).

    **Category: Parent push add/remove (~3 tests)**
    - `test_or_exists_push_parent_add_blocked_when_no_branches_pass`: no children in branch, no or_predicate → `push(Add(parent))` returns `vec![]` (parent doesn't pass any check).
    - `test_or_exists_push_parent_add_passes_via_or_predicate`: no children, but or_predicate matches → `push(Add(parent_with_status_active))` returns `vec![Change::Add(...)]`.
    - `test_or_exists_push_parent_remove_emits_when_was_passing`: 1 parent with matching child (or any branch passing) was previously fetched → push `Remove(parent)` emits `Remove`.

    **Category: Child push add/remove/edit (~5 tests)**
    - `test_or_exists_push_child_add_0_to_1_transition`: no children, then `push(Child { node: parent, child: Add(child_with_parent_id) })` triggers a 0→1 transition → emits `Change::Add(parent)`.
    - `test_or_exists_push_child_remove_1_to_0_transition`: 1 child exists, push child remove triggers 1→0 → emits `Change::Remove(parent)`.
    - `test_or_exists_push_different_relationship_child_passthrough`: child push with `relationship_name = "unrelated"` (doesn't match any branch) → emits the child change unchanged (mirror exists_op.rs:704-737).
    - `test_or_exists_push_child_edit_passthrough`: child Edit (not add/remove) → emits the child change unchanged.
    - `test_or_exists_push_child_add_other_branch_already_passing`: 2 branches, branch A has a matching child (passes), branch B has none. Push child add to B → no parent emit (parent already passing via A; no transition).

  </behavior>
  <action>
    1. **Edit** `packages/zero-ivm-rs/src/or_exists_op.rs` inside `mod tests` (after line 595, the closing `}` of `test_or_exists_edit_neither`):
       a. Add helper `fn make_node_with_parent(id: i64, parent_id: i64) -> Node` (mirror exists_op.rs:431-439).
       b. Add helper `fn build_or_exists_simple_branch(parents: Vec<Node>, children: Vec<Node>) -> OrExistsOperator` constructing a single-branch OrExistsOperator with parent_key=["id"] and child_key=["parent_id"] (no or_predicate).
       c. Add helper `fn build_or_exists_two_branches(parents: Vec<Node>, ch_a: Vec<Node>, ch_b: Vec<Node>) -> OrExistsOperator` for OR-combination tests in Task 3.
       d. Add the **8 tests** described above:
          - `test_or_exists_fetch_filters_parents_without_children`
          - `test_or_exists_fetch_or_predicate_short_circuits`
          - `test_or_exists_fetch_with_parent_constraint`
          - `test_or_exists_push_parent_add_blocked_when_no_branches_pass`
          - `test_or_exists_push_parent_add_passes_via_or_predicate`
          - `test_or_exists_push_parent_remove_emits_when_was_passing`
          - `test_or_exists_push_child_add_0_to_1_transition`
          - `test_or_exists_push_child_remove_1_to_0_transition`
       Each follows the pattern: build fixture → call `op.fetch(&FetchRequest::default())` to populate `parent_sizes` cache → call `op.push(<change>)` → assert `result.len()` and `matches!(&result[0], Change::Add(n) if ...)`. Mirror the exact assertion shape from the corresponding exists_op.rs test for predictability.
    2. Run `cargo test --release -p zero-ivm-rs --lib or_exists_op::tests::test_or_exists_fetch -- --test-threads=1` — all 3 fetch tests pass.
    3. Run `cargo test --release -p zero-ivm-rs --lib or_exists_op::tests::test_or_exists_push -- --test-threads=1` — all 5 push-related new tests pass.
    4. **Per D-28:** verify no production-code lines changed. Run `git diff packages/zero-ivm-rs/src/or_exists_op.rs | grep -E "^[-+]" | head -20` and confirm all `+` lines are inside `mod tests { ... }` (line 449+) and all `-` lines are zero (no deletions in production code).
  </action>
  <verify>
    <automated>cd packages/zero-ivm-rs && cargo test --release --lib or_exists_op::tests -- --test-threads=1 2>&1 | tail -10</automated>
  </verify>
  <done>8 new fetch+parent+child tests added; all pass under cargo test --release; production code (lines 1-448) byte-for-byte unchanged from pre-plan state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add edit-no-or_predicate + cache invalidation + in_push flag tests (categories 5–8)</name>
  <files>packages/zero-ivm-rs/src/or_exists_op.rs</files>
  <read_first>
    - packages/zero-ivm-rs/src/or_exists_op.rs (now updated by Task 1)
    - packages/zero-ivm-rs/src/exists_op.rs lines 670-740 (edit no-or_predicate), 893-968 (in_push flag, cache invalidation)
  </read_first>
  <behavior>
    **Category: Push edit, no or_predicate (~2 tests)**
    - `test_or_exists_edit_count_change_no_predicate`: no or_predicate, parent has 1 matching child cached. Push `Edit{node: same_id, old_node: same_id}` (id unchanged). Result: emit `Edit` (count unchanged across rows of same id) — mirror exists_op.rs:670-703.
    - `test_or_exists_edit_no_or_predicate_passes_when_children_exist`: no or_predicate, 1 child exists. Push `Edit` — emits `Edit` (passes filter for both old and new under no-predicate path).

    **Category: Cache invalidation / fetch state (~2 tests)**
    - `test_or_exists_cache_cleared_on_fetch`: after a fetch, `parent_sizes` cache is populated. Mutate the underlying `MockInput.nodes` to remove children. Call fetch again. Cache must be cleared so the new fetch returns 0 children for the parent (mirror exists_op.rs:913-935).
    - `test_or_exists_child_add_without_prior_fetch_recomputes`: construct OrExistsOperator, do NOT call fetch first. Push child add directly. Operator must recompute count from scratch (no stale cache hit) and emit correctly (mirror exists_op.rs:936-968).

    **Category: In-push flag (~1 test)**
    - `test_or_exists_in_push_flag_cleared_after_push`: drive a normal push that completes successfully. Then call `force_in_push_for_test()` and immediately `push(Add(...))` — should panic with `"Unexpected re-entrancy"` (proves the flag was actually cleared at end of the previous push and is now being forced back to true). Use `#[should_panic(expected = "Unexpected re-entrancy")]`. Mirror exists_op.rs:893-911.

    **Category: Different-relationship passthrough (~1 test) — already in Task 1; skip if duplicate.**
    - Skip — covered by `test_or_exists_push_different_relationship_child_passthrough` in Task 1.

  </behavior>
  <action>
    1. **Edit** `packages/zero-ivm-rs/src/or_exists_op.rs` inside `mod tests` (after Task 1's additions):
       a. Add the **5 tests** described above:
          - `test_or_exists_edit_count_change_no_predicate`
          - `test_or_exists_edit_no_or_predicate_passes_when_children_exist`
          - `test_or_exists_cache_cleared_on_fetch`
          - `test_or_exists_child_add_without_prior_fetch_recomputes`
          - `test_or_exists_in_push_flag_cleared_after_push` (with `#[should_panic(expected = "Unexpected re-entrancy")]`)
       b. For `test_or_exists_cache_cleared_on_fetch`: this requires mutating `MockInput.nodes` between fetches. Add a helper `struct MutableMockInput { nodes: Arc<Mutex<Vec<Node>>> }` impl `Operator` — OR (simpler) construct two separate fixtures with different child sets and verify the second fetch's count is independent of the first. Pick whichever is cleaner; prefer the second (simpler) approach.
    2. Run `cargo test --release -p zero-ivm-rs --lib or_exists_op::tests -- --test-threads=1` — all (5 existing + 8 from Task 1 + 5 from Task 2) = 18 tests pass.
    3. Run `cargo test --release -p zero-ivm-rs --lib or_exists_op:: -- --list` and confirm `test_or_exists_*` count is now ≥18.
  </action>
  <verify>
    <automated>cd packages/zero-ivm-rs && cargo test --release --lib or_exists_op::tests -- --test-threads=1 2>&1 | grep -E "test result|FAILED" | tail -5</automated>
  </verify>
  <done>5 new tests added; combined or_exists_op test count is now ≥18 and all pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add OR-branch combination tests + builder-spec parity test (D-09 OR-specific category) and verify ≥22 total</name>
  <files>packages/zero-ivm-rs/src/or_exists_op.rs</files>
  <read_first>
    - packages/zero-ivm-rs/src/or_exists_op.rs (now updated by Tasks 1 + 2)
    - packages/zero-ivm-rs/src/ast_to_config.rs (search for OrExists construction path) — needed for builder-spec parity test
    - .planning/phases/33-performance-tuning/33-RESEARCH.md §4 tests #25, #26, #27, #28, #29
  </read_first>
  <behavior>
    **Category: OR-branch combinations (≥2 tests, D-09 EXPLICIT requirement)**
    - `test_or_exists_two_branches_both_pass`: 2 branches, both have matching children. Push `Add(parent)` → emits `Add(parent)` (any-branch-passes is true).
    - `test_or_exists_two_branches_one_passes`: 2 branches, only branch A has matching children. Push `Add(parent)` → emits `Add(parent)` (one branch passes is sufficient for OR semantics).
    - `test_or_exists_mixed_exists_not_exists_branches` (OPTIONAL — drop if hits time): 2 branches: branch A is EXISTS (not_exists=false), branch B is NOT EXISTS (not_exists=true). Parent passes if EXISTS has children OR NOT-EXISTS has zero children. Construct fixture where EXISTS has 0 children but NOT-EXISTS branch (different child table) is empty → parent passes via NOT-EXISTS branch.

    **Category: Builder-spec parity (D-09 EXPLICIT requirement) — 1 test**
    - `test_or_exists_ast_to_config_matches_direct_construction`: take a small AST shaped like `where: { type: 'or', conditions: [{type: 'correlatedSubquery', op: 'EXISTS', ... related: ... }, ...] }`, call `ast_to_config::build_pipeline_state` (or whatever the entry point is — verify via grep), then call `op_type()` and assert it returns `"or_exists"`. Compare to a hand-built OrExistsOperator with the same parent_key/child_key/relationship_name and assert both produce the same op_type and same fetch behavior on identical input. NOTE: if the construction-path entry point is not easily callable from a unit test (requires SQLite + connection pool), simplify to a smoke test that just constructs OrExistsOperator via direct `new()` and asserts `op.op_type() == "or_exists"`. Document the simplification in the SUMMARY.

    **Final count check:**
    - `test_or_exists_*` count in `or_exists_op.rs::tests` must be ≥22 (parity with exists_op.rs); aspiration ~25-28.
    - If reaching 22 is awkward (e.g., builder-spec test requires too much scaffolding to be a true parity assertion), document in SUMMARY and stop at the highest meaningful count (per D-11 escape clause).

  </behavior>
  <action>
    1. **Edit** `packages/zero-ivm-rs/src/or_exists_op.rs` inside `mod tests`:
       a. Add helper `fn build_or_exists_two_branches_with_children(parents, ch_a, ch_b)` if not already added in Task 1.
       b. Add tests:
          - `test_or_exists_two_branches_both_pass`
          - `test_or_exists_two_branches_one_passes`
          - `test_or_exists_mixed_exists_not_exists_branches` (OPTIONAL — include if straightforward)
          - `test_or_exists_ast_to_config_matches_direct_construction` (or simplified smoke per <behavior> above)
    2. Run `cargo test --release -p zero-ivm-rs --lib or_exists_op:: -- --list | grep -c '^or_exists_op::tests::test_'` and confirm count is **≥22** (target was 30 per D-11; if at 22-25 with no easy additional tests, document why in SUMMARY per D-11 escape clause).
    3. Run `cargo test --release -p zero-ivm-rs --lib or_exists_op::tests -- --test-threads=1` — all tests pass.
    4. Run full Rust crate suite: `cargo test --release -p zero-ivm-rs --lib -- --test-threads=1` — total count must be **≥170** (Phase 31 baseline + new or_exists tests). Confirm zero regressions in other test modules.
    5. **Per D-28:** verify no production-code touches via `git diff packages/zero-ivm-rs/src/or_exists_op.rs -- ':(exclude)mod tests'` style check; manually confirm only lines inside `mod tests` were modified.
  </action>
  <verify>
    <automated>cd packages/zero-ivm-rs && N=$(cargo test --release --lib or_exists_op:: -- --list 2>/dev/null | grep -c '^or_exists_op::tests::test_'); echo "or_exists test count = $N"; test "$N" -ge 22</automated>
  </verify>
  <done>or_exists_op.rs::tests count ≥22; full zero-ivm-rs cargo test passes; production code unchanged.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                                                                                                              |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Test → production code | Tests use only the public `OrExistsOperator::new()` + `Operator` trait + `#[cfg(test)]`-gated `force_in_push_for_test`. No new public-API surface added. |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                             | Disposition | Mitigation Plan                                                                                                                                                                                 |
| ---------- | ---------------------- | --------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-33-02-01 | Tampering              | Test additions modify production code path (D-28 anti-hack guardrail) | mitigate    | Each task ends with a `git diff` verification step asserting that all `+` lines lie inside `mod tests {}` (line 449 onward). The verifier rejects the plan if production lines (1-448) changed. |
| T-33-02-02 | Information Disclosure | New tests expose internal state via test-only API                     | accept      | `force_in_push_for_test` is `#[cfg(test)]` gated and was added in Phase 30-04 — already in the public-test-API budget for this crate. No new test-only public API needed.                       |

</threat_model>

<verification>
**Per-task verification:** see `<verify>` blocks.

**Plan-level verification (run all in order):**

1. `cd packages/zero-ivm-rs && cargo test --release --lib or_exists_op::tests -- --test-threads=1` — all tests pass.
2. `cd packages/zero-ivm-rs && cargo test --release --lib or_exists_op:: -- --list | grep -c '^or_exists_op::tests::test_'` — output ≥22.
3. `cd packages/zero-ivm-rs && cargo test --release --lib -- --test-threads=1` — full crate suite passes; total count ≥170 (Phase 31 baseline) + new tests.
4. `git diff packages/zero-ivm-rs/src/or_exists_op.rs --stat` — confirm changes are localized to the file (no other files touched by this plan).
5. **Manual diff check:** scroll the diff and confirm every `+` line is inside `mod tests { ... }` and there are zero `-` lines outside trivial whitespace (no production-code modifications). Per D-28 anti-hack guardrail.
   </verification>

<success_criteria>

- `or_exists_op.rs::tests` test count ≥22 (target ~25–28; per D-11 stop at lower count if reaching higher is contrived, with rationale in SUMMARY).
- All new tests follow the categorical structure mirroring `exists_op.rs::tests` plus the OR-specific multi-branch combination tests required by D-09.
- Pre-existing 5 tests pass unchanged: `test_or_exists_reentrancy_panics`, `test_or_exists_edit_both_pass`, `test_or_exists_edit_old_only`, `test_or_exists_edit_new_only`, `test_or_exists_edit_neither`.
- Full `zero-ivm-rs` cargo test suite passes; no regressions in other test modules.
- HARDEN-02 requirement closed: OrExists test breadth gap closed.
- Anti-hack guardrail D-28 honored: zero changes to production code in `or_exists_op.rs` (lines 1-448 byte-for-byte identical to pre-plan).
  </success_criteria>

<output>
After completion, create `.planning/phases/33-performance-tuning/33-02-SUMMARY.md` containing:

- Final test count (`cargo test -- --list | grep -c` output).
- Categorical breakdown: `Fetch: N | Parent push: N | Child push: N | Edit no-pred: N | Edit with-pred: N | Cache: N | In-push: N | OR-branch: N | Builder-spec: N | TOTAL: N`.
- Any tests omitted from RESEARCH §4's suggested 30-test list, with rationale (D-11 escape clause).
- Confirmation that production code (lines 1-448) is unchanged from the pre-plan state.
  </output>
