---
phase: 19
plan: 2
title: 'Vitest Integration Tests for Edit Stream Correctness'
wave: 2
depends_on: [19-01]
files_modified:
  - packages/zero-cache/src/services/view-syncer/pipeline-driver.edit-semantics.test.ts
requirements_addressed: [EDI-01, EDI-02]
autonomous: true
---

<objective>
Create integration tests verifying the Rust advance path produces correct edit
stream output (change_type + row data) matching TS behavior. Uses dual-path
comparison (D-80) with set equality (D-91) and strict type matching (D-90).
</objective>

## Tasks

<task id="19-02-01">
<title>Create edit-semantics test file with dual-path helper</title>
<read_first>
- packages/zero-cache/src/services/view-syncer/pipeline-driver.fixtures.ts
- packages/zero-cache/src/services/view-syncer/pipeline-driver.edge-cases.test.ts (pattern reference)
- packages/zero-cache/src/services/view-syncer/pipeline-driver.test.ts (first 100 lines for imports/setup pattern)
</read_first>
<action>
Create `packages/zero-cache/src/services/view-syncer/pipeline-driver.edit-semantics.test.ts`.

File structure:

1. Import test infrastructure from pipeline-driver.test.ts pattern (PipelineDriver, TestLogSink, etc.)
2. Import schemas from fixtures
3. Add a new table schema: `editItems` with columns {id: string(), val: number(), active: string()} primaryKey('id')
4. Create a `editClientSchema` using createSchema
5. Setup describe block with beforeEach/afterEach for DB lifecycle
6. Helper: `getChanges(sink)` that extracts change_type and row from the sink's recorded pushes

The test file tests edit stream semantics end-to-end through the Rust advance path.
</action>
<acceptance_criteria>

- File exists at `packages/zero-cache/src/services/view-syncer/pipeline-driver.edit-semantics.test.ts`
- Imports compile without error
- describe block with setup/teardown present
  </acceptance_criteria>
  </task>

<task id="19-02-02">
<title>Test: edit where row crosses filter boundary (visible→hidden = remove)</title>
<read_first>
- packages/zero-cache/src/services/view-syncer/pipeline-driver.edge-cases.test.ts (for push/advance pattern)
- packages/zqlite-rs/src/advance.rs lines 280-289 (remove on filter boundary cross)
</read_first>
<action>
Add test: "edit crossing filter boundary emits remove"

Setup:

- Table `edit_items` with filter `active = 'yes'`
- Insert row {id:'e1', val:10, active:'yes'} → advance (produces add)
- Update row to {id:'e1', val:10, active:'no'} → advance

Assert (D-90 strict type + D-91 set equality):

- Second advance produces exactly 1 change with change_type "remove"
- Run with ZERO_DISABLE_RUST_IVM=1 and compare TS output matches (dual-path D-80)
  </action>
  <acceptance_criteria>
- Test "edit crossing filter boundary emits remove" passes with `npx vitest run pipeline-driver.edit-semantics.test.ts`
- Asserts change_type is "remove" (not "edit")
  </acceptance_criteria>
  </task>

<task id="19-02-03">
<title>Test: edit where row becomes visible (hidden→visible = edit/add)</title>
<read_first>
- packages/zqlite-rs/src/advance.rs lines 272-279 (edit when new passes filter with PK match)
</read_first>
<action>
Add test: "edit making row visible emits edit (PK match)"

Setup:

- Table `edit_items` with filter `active = 'yes'`
- Insert row {id:'e1', val:10, active:'no'} → advance (produces nothing — filtered out)
- Update row to {id:'e1', val:20, active:'yes'} → advance

Assert:

- Second advance produces 1 change with change_type "edit" (PK matched, new passes filter)
- Row data contains {id:'e1', val:20, active:'yes'}
- Dual-path comparison matches TS
  </action>
  <acceptance_criteria>
- Test passes in vitest
- change_type verified as "edit" with correct row data
  </acceptance_criteria>
  </task>

<task id="19-02-04">
<title>Test: insert→update within same push produces [add] then [edit]</title>
<read_first>
- packages/zero-cache/src/services/view-syncer/pipeline-driver.test.ts (for multi-row push patterns)
</read_first>
<action>
Add test: "insert then update same row across two pushes"

Setup:

- Table `edit_items` with no filter
- Push 1: Insert {id:'e1', val:1, active:'yes'} → advance → expect [add]
- Push 2: Update to {id:'e1', val:2, active:'yes'} → advance → expect [edit]

Assert:

- Push 1 changes: set contains {change_type:"add", row:{id:'e1',val:1,active:'yes'}}
- Push 2 changes: set contains {change_type:"edit", row:{id:'e1',val:2,active:'yes'}}
- Dual-path matches
  </action>
  <acceptance_criteria>
- Test passes verifying add→edit sequence across pushes
- Both Rust and TS paths produce identical output
  </acceptance_criteria>
  </task>

<task id="19-02-05">
<title>Test: update→delete produces [remove]</title>
<read_first>
- packages/zqlite-rs/src/advance.rs lines 318-331
</read_first>
<action>
Add test: "update then delete produces remove"

Setup:

- Table `edit_items` with no filter
- Push 1: Insert {id:'e1', val:1, active:'yes'} → advance
- Push 2: Update {id:'e1', val:2, active:'yes'} → advance
- Push 3: Delete row e1 → advance

Assert:

- Push 3 changes: exactly [{change_type:"remove"}]
- Dual-path matches
  </action>
  <acceptance_criteria>
- Test passes verifying delete after update produces "remove" change_type
  </acceptance_criteria>
  </task>

## Verification

```bash
npx vitest run packages/zero-cache/src/services/view-syncer/pipeline-driver.edit-semantics.test.ts
```

All 4 tests pass. Dual-path comparison confirms Rust matches TS for all edit stream scenarios.

## must_haves

- Each test verifies exact change_type string (D-90)
- Set equality comparison used (D-91)
- Dual-path Rust vs TS comparison (D-80)
- Filter boundary crossing (visible→hidden, hidden→visible) tested
- Type transitions (add→edit, edit→remove) verified across pushes
