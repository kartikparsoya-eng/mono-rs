---
phase: 36-flipped-join-family
plan: 03
type: execute
wave: 2
depends_on: [36-02]
files_modified:
  - packages/zero-ivm-rs/src/union_fan_out_op.rs
  - packages/zero-ivm-rs/src/union_fan_in_op.rs
  - packages/zero-ivm-rs/src/push_accumulated.rs
  - packages/zero-ivm-rs/src/pipeline.rs
  - packages/zqlite-rs/src/hydrate.rs
autonomous: true
requirements: [B7, UNION-PORT]
tags: [phase-36, union-fan-in, union-fan-out, wave-2]

must_haves:
  truths:
    - 'UnionFanIn dedup-by-PK uses a streaming priority-queue merge over Vec<Node> from each branch.'
    - 'UnionFanOut/UnionFanIn protocol (fanOutStartedPushing → accumulate → fanOutDonePushing) is preserved.'
    - 'Internal-vs-external push distinction (TS union-fan-in.ts:114-181) is correctly implemented: ChildChange forwarded; Add/Remove suppressed if any other branch has the row.'
    - '≥30 unit tests cover dedup, reorder, internal/external push, accumulator flush, multi-branch overlap.'
  artifacts:
    - path: 'packages/zero-ivm-rs/src/union_fan_in_op.rs'
      provides: 'UnionFanInOperator with full TS-parity push/fetch/protocol semantics; mergeFetches Rust port'
      min_lines: 330
    - path: 'packages/zero-ivm-rs/src/union_fan_out_op.rs'
      provides: 'UnionFanOutOperator with protocol handoff and per-output forwarding'
      min_lines: 100
    - path: 'packages/zero-ivm-rs/src/push_accumulated.rs'
      provides: "push_accumulated_changes + merge_relationships + make_add_empty_relationships helpers (extends Wave 1's stub)"
      contains: 'merge_relationships'
  key_links:
    - from: 'packages/zero-ivm-rs/src/union_fan_out_op.rs'
      to: 'packages/zero-ivm-rs/src/union_fan_in_op.rs'
      via: 'Arc<Mutex<UnionFanInOperator>> for protocol-handoff'
      pattern: 'fan_out_started_pushing'
    - from: 'packages/zero-ivm-rs/src/union_fan_in_op.rs'
      to: 'packages/zero-ivm-rs/src/push_accumulated.rs'
      via: 'push_accumulated_changes call from fan_out_done_pushing'
      pattern: 'push_accumulated_changes'
---

<objective>
Wave 2 of Phase 36 — port UnionFanOut + UnionFanIn (the dedup+reorder
variants used by `applyFilterWithFlips` for OR conditions). This is the
highest-risk wave per CONTEXT R-01 — generator-to-synchronous translation
for the accumulator + protocol handoff.

Output:

- `packages/zero-ivm-rs/src/union_fan_out_op.rs` (~100 LOC + tests)
- `packages/zero-ivm-rs/src/union_fan_in_op.rs` (~330 LOC + tests)
- Extended `push_accumulated.rs` (merge_relationships, make_add_empty_relationships)
- `OperatorConfig::UnionFanOut` and `OperatorConfig::UnionFanIn` variants in `pipeline.rs`
- ≥30 unit tests across both operators
  </objective>

<execution_context>
@/Users/kartik.parsoya/Documents/Zero/mono-rs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kartik.parsoya/Documents/Zero/mono-rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-flipped-join-family/36-CONTEXT.md
@.planning/phases/36-flipped-join-family/36-RESEARCH.md
@packages/zql/src/ivm/union-fan-in.ts
@packages/zql/src/ivm/union-fan-out.ts
@packages/zql/src/ivm/union-fan-in.test.ts
@packages/zql/src/ivm/union-fan-out.test.ts
@packages/zql/src/ivm/push-accumulated.ts
@packages/zero-ivm-rs/src/operator.rs
@packages/zero-ivm-rs/src/exists_op.rs
@packages/zero-ivm-rs/src/or_exists_op.rs
@packages/zero-ivm-rs/src/fan_out_op.rs
@packages/zero-ivm-rs/src/fan_in_op.rs

<interfaces>
From packages/zql/src/ivm/union-fan-in.ts:
- export class UnionFanIn implements Operator { constructor(fanOut, inputs) }
- fetch(req): Stream<Node | 'yield'>                           // line 103
- *push(change, pusher)                                         // line 114
- *#pushInternalChange(change, pusher)                          // line 143
- fanOutStartedPushing()                                        // line 183
- *fanOutDonePushing(fanOutChangeType)                          // line 191
- export function* mergeFetches(fetches, comparator)            // line 222

From packages/zql/src/ivm/union-fan-out.ts:

- export class UnionFanOut implements Operator { constructor(input) }
- \*push(change) // line 27
- setFanIn(fanIn) // line 22
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement UnionFanOut operator</name>
  <files>packages/zero-ivm-rs/src/union_fan_out_op.rs</files>
  <read_first>
    - packages/zql/src/ivm/union-fan-out.ts (full)
    - packages/zql/src/ivm/union-fan-out.test.ts (full — test fixtures)
    - packages/zero-ivm-rs/src/fan_out_op.rs (just-written for pattern)
  </read_first>
  <behavior>
    - `union_fan_out.push(change)` calls `fan_in.fan_out_started_pushing()` first; pushes to all outputs; then calls `fan_in.fan_out_done_pushing(change.change_type())` and appends its result.
    - `set_fan_in(fan_in)` — assert not already set (TS line 23: assert).
    - `destroy()` refcount pattern (same as FanOut).
    - `fetch(req)` delegates to input.
  </behavior>
  <action>
    Implement `UnionFanOutOperator`:
      - Fields: `input: Arc<dyn Operator>`, `outputs: Vec<Arc<Mutex<dyn Operator>>>`, `fan_in: Option<Arc<Mutex<UnionFanInOperator>>>`, `destroy_count: AtomicUsize`.
      - Cite TS lines on each method.
      - `push(change)` — TS line 27: must().fanOutStartedPushing(); for_each output push(change, this); must().fanOutDonePushing(change.changeType()).
    Add ≥10 tests:
      - `test_union_fan_out_calls_started_pushing_first`
      - `test_union_fan_out_propagates_to_all_outputs`
      - `test_union_fan_out_calls_done_pushing_with_change_type`
      - `test_union_fan_out_destroy_refcount`
      - `test_union_fan_out_set_fan_in_idempotent` (asserts on second call)
      - `test_union_fan_out_zero_outputs_done_pushing_still_called`
      - `test_union_fan_out_fetch_delegates_to_input`
      - `test_union_fan_out_push_accumulates_in_fan_in`
      - `test_union_fan_out_protocol_per_change_idempotent` (multiple pushes; phase resets to Idle each time)
      - `test_union_fan_out_destroy_overflow_panics`
  </action>
  <verify>
    <automated>cd packages/zero-ivm-rs && cargo test union_fan_out_op 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p zero-ivm-rs union_fan_out_op` reports ≥10 tests passing.
    - `wc -l packages/zero-ivm-rs/src/union_fan_out_op.rs` ≥ 200 (incl. tests).
  </acceptance_criteria>
  <done>UnionFanOut operator landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement UnionFanIn dedup-merge fetch + push protocol</name>
  <files>packages/zero-ivm-rs/src/union_fan_in_op.rs, packages/zero-ivm-rs/src/push_accumulated.rs</files>
  <read_first>
    - packages/zql/src/ivm/union-fan-in.ts (full)
    - packages/zql/src/ivm/union-fan-in.test.ts (full)
    - packages/zql/src/ivm/push-accumulated.ts (full)
    - .planning/phases/36-flipped-join-family/36-RESEARCH.md §UnionFanIn TS↔Rust intent map (full)
  </read_first>
  <behavior>
    - **fetch:** dedup-merge across `inputs.iter().map(|i| i.fetch(req))`. Use a binary-heap k-way merge with `compareRows` ordering. Skip emit if last_yielded compares equal to current min.
    - **push (external, when phase == Idle):** dispatch to `push_internal_change` (TS pushInternalChange).
    - **push (internal, when phase == InFanOut):** accumulate.
    - **push_internal_change:**
      - If change is Child → forward.
      - If Add/Remove → for each input ≠ pusher, fetch by primary-key constraint; if any input has a matching row, suppress (return `vec![]`); else forward.
    - **fan_out_started_pushing:** assert phase == Idle; flip to InFanOut.
    - **fan_out_done_pushing(change_type):** flush accumulated via `push_accumulated_changes` with `merge_relationships` + `make_add_empty_relationships`; reset phase to Idle.
  </behavior>
  <action>
    Extend `packages/zero-ivm-rs/src/push_accumulated.rs`:
      - `pub fn push_accumulated_changes<F1, F2>(changes: &[Change], change_type: ChangeType, schema: &SourceSchema, merge_fn: F1, make_fn: F2) -> Vec<Change> where F1: Fn(&Change, &Change) -> Change, F2: Fn(&Change) -> Change` — group by PK, fold per-PK.
      - `pub fn merge_relationships(a: &Change, b: &Change) -> Change` — combine relationships HashMaps; assert no key collision (TS asserts unique branch relationships).
      - `pub fn make_add_empty_relationships(schema: &SourceSchema) -> impl Fn(&Change) -> Change` — closure that pads missing relationship keys with empty Vec.
    Implement `UnionFanInOperator`:
      - Fields: `inputs: Vec<Arc<Mutex<dyn Operator>>>`, `schema: Arc<SourceSchema>`, `phase: UnionPushPhase`, `accumulated: Vec<(Change, usize)>` (track pusher index for assertion).
      - Cite TS lines.
      - `new(fan_out, inputs)` — TS line 25-95: assert tableName/primaryKey/system/sort match across inputs; build merged schema relationship-by-relationship.
      - `fetch(req)` — TS line 103: collect each input's fetch into `Vec<Vec<Node>>`; run `merge_dedup` (binary-heap k-way merge with dedup-by-PK).
      - `push(change, pusher_idx)` — TS line 114-120: dispatch by phase.
      - `push_internal_change(change, pusher_idx)` — TS line 143-181: as described in behavior.
      - `fan_out_started_pushing` / `fan_out_done_pushing` — TS lines 183, 191.
    Add ≥20 tests:
      - **fetch tests** (10):
        - `test_ufi_fetch_two_branches_no_overlap_emits_all`
        - `test_ufi_fetch_two_branches_full_overlap_dedups`
        - `test_ufi_fetch_two_branches_partial_overlap`
        - `test_ufi_fetch_three_branches_streaming_order`
        - `test_ufi_fetch_with_compound_pk`
        - `test_ufi_fetch_reverse_order`
        - `test_ufi_fetch_empty_branch_skipped`
        - `test_ufi_fetch_all_empty_returns_empty`
        - `test_ufi_fetch_with_constraint`
        - `test_ufi_fetch_dedup_keeps_first_branch_order`
      - **push tests** (8):
        - `test_ufi_push_external_child_forwarded`
        - `test_ufi_push_external_add_no_other_branch_forwarded`
        - `test_ufi_push_external_add_other_branch_has_row_suppressed`
        - `test_ufi_push_external_remove_no_other_forwarded`
        - `test_ufi_push_external_remove_other_has_row_suppressed`
        - `test_ufi_push_in_fan_out_phase_accumulates`
        - `test_ufi_push_done_pushing_flushes_accumulated`
        - `test_ufi_push_done_pushing_zero_inputs_noop`
      - **schema/protocol tests** (2):
        - `test_ufi_schema_mismatch_branch_table_panics`
        - `test_ufi_started_pushing_twice_panics`
  </action>
  <verify>
    <automated>cd packages/zero-ivm-rs && cargo test union_fan_in_op 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p zero-ivm-rs union_fan_in_op` reports ≥20 tests passing.
    - `wc -l packages/zero-ivm-rs/src/union_fan_in_op.rs` ≥ 600 (incl. tests).
    - `push_accumulated.rs` exports `push_accumulated_changes`, `merge_relationships`, `make_add_empty_relationships`.
  </acceptance_criteria>
  <done>UnionFanIn operator landed with full TS-parity behavior. R-01 risk closed unless slip flagged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire OperatorConfig::UnionFanOut and OperatorConfig::UnionFanIn variants</name>
  <files>packages/zero-ivm-rs/src/pipeline.rs, packages/zqlite-rs/src/hydrate.rs</files>
  <read_first>
    - packages/zero-ivm-rs/src/pipeline.rs (full — patterns established by Wave 1)
    - packages/zqlite-rs/src/hydrate.rs (full)
  </read_first>
  <behavior>
    - `OperatorConfig::UnionFanOut { branches: Vec<Vec<OperatorConfig>> }` — analogous to FanOut.
    - `OperatorConfig::UnionFanIn { branch_count: usize }` — analogous to FanIn.
    - `build_operator` constructs UnionFanOutOperator + UnionFanInOperator pair with shared `Arc<Mutex<...>>` references for protocol handoff.
  </behavior>
  <action>
    Add variants and `build_operator` arms (mirror Wave 1 pattern). Add 5 round-trip + builder tests:
      - `test_union_fan_out_json_round_trip`
      - `test_union_fan_in_json_round_trip`
      - `test_union_fan_out_in_pipeline_compiles_to_operator_tree`
      - `test_union_fan_in_protocol_threaded_through_arc`
      - `test_union_fan_out_zero_branches_panics`
  </action>
  <verify>
    <automated>cd packages/zero-ivm-rs && cargo test pipeline 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 5 new pipeline tests pass.
    - `grep -c "UnionFanOut\|UnionFanIn" packages/zero-ivm-rs/src/pipeline.rs` ≥ 4.
  </acceptance_criteria>
  <done>Union variants wired to pipeline + hydrate; round-trip + builder tests pass.</done>
</task>

</tasks>

<risks>
- **R-01 (HIGH)** — Generator-to-synchronous translation for `push_internal_change` lookahead. If `push_internal_change` produces a Vec<Change> that the downstream output expects to consume cooperatively, we may need an explicit step-driver. Mitigation: copy `or_exists_op.rs` synchronous-output pattern. Escalation: if 3+ days of attempts fail, spike on `pushAccumulatedChanges` semantics for 1 day before continuing.
- **R-03 (LOW)** — Schema mismatch assertion runtime — Wave 2 includes a dedicated test that explicitly panics on mismatched branch schema (`test_ufi_schema_mismatch_branch_table_panics`).
</risks>
