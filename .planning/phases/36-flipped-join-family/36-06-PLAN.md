---
phase: 36-flipped-join-family
plan: 06
type: execute
wave: 5
depends_on: [36-05]
files_modified:
  - tools/ivm-parity/parity-allowlist.json
  - tools/ivm-parity/diff-tests-track2.ts
  - .planning/IVM-PORT-AUDIT-DEEP.md
  - PARITY_STATUS.md
autonomous: true
requirements: [B7, FUZZ-PARITY, PLANNER-EMISSION]
tags: [phase-36, parity, allow-list-shrink, wave-5]

must_haves:
  truths:
    - 'tools/ivm-parity/parity-allowlist.json shrinks by exactly 2 keys: fuzz_00132 and fuzz_00133.'
    - 'Deferred pattern B7-flipped-join is removed from the allow-list.'
    - 'diff-tests-track2.ts includes ≥5 new shape-tests covering FlippedJoin distributive expansion.'
    - "Three planner-emission round-trip tests verify that the planner's flip:true emission is exercised end-to-end."
    - 'npm run fuzz-check:gate passes with the smaller allow-list (CI gate green).'
  artifacts:
    - path: 'tools/ivm-parity/parity-allowlist.json'
      provides: 'shrunken allow-list (3 catalogued + 3 patterns instead of 5+4)'
      contains: 'fuzz_00139'
    - path: 'tools/ivm-parity/diff-tests-track2.ts'
      provides: '5 new flipped-join shape tests + 3 planner-emission tests'
      contains: 'flippedJoinShape'
    - path: '.planning/IVM-PORT-AUDIT-DEEP.md'
      provides: 'B7 status flipped from 🟠 OPEN-RISK to ✅ FIXED with commit citation'
      contains: 'B7. ✅ FIXED'
    - path: 'PARITY_STATUS.md'
      provides: 'fuzz_00132/fuzz_00133 removed from catalogued; B7-flipped-join removed from deferred patterns'
      contains: 'Phase 36'
---

<objective>
Wave 5 of Phase 36 — close out parity. Remove the two FlippedJoin allow-list
entries (`fuzz_00132`, `fuzz_00133`) and the deferred pattern
`B7-flipped-join` from `tools/ivm-parity/parity-allowlist.json`. Add ≥5 new
shape-tests to `diff-tests-track2.ts` to lock in coverage. Add 3
planner-emission tests (per CONTEXT R-02 mitigation) that drive ZQL through
the planner and confirm the resulting AST hits the new Rust path. Confirm
`npm run fuzz-check:gate` passes against the smaller allow-list.

Update `IVM-PORT-AUDIT-DEEP.md` to flip B7 to ✅ FIXED. Update
`PARITY_STATUS.md` to reflect the new allow-list state.

Output:

- `tools/ivm-parity/parity-allowlist.json` — 2 entries removed
- `tools/ivm-parity/diff-tests-track2.ts` — ≥5 new shape tests + 3 planner tests
- `.planning/IVM-PORT-AUDIT-DEEP.md` — B7 status update
- `PARITY_STATUS.md` — Phase 36 section
  </objective>

<execution_context>
@/Users/kartik.parsoya/Documents/Zero/mono-rs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kartik.parsoya/Documents/Zero/mono-rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-flipped-join-family/36-CONTEXT.md
@.planning/phases/36-flipped-join-family/36-RESEARCH.md
@.planning/IVM-PORT-AUDIT-DEEP.md
@PARITY_STATUS.md
@tools/ivm-parity/parity-allowlist.json
@tools/ivm-parity/diff-tests-track2.ts
@tools/ivm-parity/PARITY-DIVERGENCES.md
@apps/zbugs/shared/permissions.ts
@packages/zql/src/planner/planner-builder.ts

<interfaces>
From tools/ivm-parity/parity-allowlist.json (current state, post-Phase-34):
- 5 catalogued + 4 deferred patterns. Phase 36 removes fuzz_00132, fuzz_00133, and B7-flipped-join → 3 catalogued + 3 deferred.

From tools/ivm-parity/diff-tests-track2.ts:

- existing helpers: runSubscribeAndCompare, hashRows, etc.
- existing shape tests: b1Ast, b2Ast, b3Ast, b11Ast.

From apps/zbugs/shared/permissions.ts:

- (assumed) at least one rule uses flip:true on EXISTS — to be verified during Task 3.
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 5 new flipped-join shape tests in diff-tests-track2.ts</name>
  <files>tools/ivm-parity/diff-tests-track2.ts</files>
  <read_first>
    - tools/ivm-parity/diff-tests-track2.ts (full)
    - tools/ivm-parity/PARITY-DIVERGENCES.md (full)
    - tools/ivm-parity/SKILL.md (full — hard rules for tooling work)
  </read_first>
  <behavior>
    - Each new test asserts TS↔Rust hydrate parity for a shape that previously hit the FlippedJoin path silently.
    - Tests run via the existing harness primitives (subscribe, hashRows, summarizeDiff).
    - Test names start with `flippedJoin*Ast`.
  </behavior>
  <action>
    Add 5 shape tests:
      - `flippedJoinStandaloneAst` — `messages.where(EXISTS(attachments, flip=true))`. Hydrate; assert TS=RS.
      - `flippedJoinOrSimpleAst` — `messages.where(OR(simple, EXISTS(attachments, flip=true)))`. Hits UnionFanOut/UnionFanIn distributive expansion.
      - `flippedJoinAndSimpleAst` — `messages.where(AND(simple, EXISTS(attachments, flip=true)))`.
      - `flippedJoinNestedOrInAndAst` — `messages.where(AND(simple, OR(simpleB, EXISTS(attachments, flip=true))))`.
      - `flippedJoinTwoFlipsInOrAst` — `messages.where(OR(EXISTS(attachments, flip=true), EXISTS(comments, flip=true)))` — two flipped CSQs in one OR (3 union branches).
    Each test:
      1. Build the AST literal with `flip:true` on the relevant CSQ.
      2. Subscribe both TS and RS caches.
      3. Run a representative MUTATION (`UPDATE attachments SET ... WHERE id='...'`).
      4. Assert TS↔RS row hash equality and patch equality.
      5. On any divergence, log the canonical key and fail.
  </action>
  <verify>
    <automated>cd tools/ivm-parity && grep -c "flippedJoin.*Ast" diff-tests-track2.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "flippedJoin.*Ast" tools/ivm-parity/diff-tests-track2.ts` ≥ 5.
    - Each test cites the AST shape in a comment.
  </acceptance_criteria>
  <done>5 shape tests added; live invocation deferred to Task 4.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 3 planner-emission round-trip tests</name>
  <files>tools/ivm-parity/diff-tests-track2.ts, packages/zqlite-rs/src/ast_to_config.rs</files>
  <read_first>
    - apps/zbugs/shared/permissions.ts (full — for known flip:true usage)
    - packages/zql/src/planner/planner-builder.ts (lines 240-280)
    - packages/zql/src/planner/planner.test.ts (existing planner tests for fixture pattern)
  </read_first>
  <behavior>
    - Tests drive ZQL queries through the planner and assert the resulting AST contains `flip:true`.
    - Then assert the AST is processed by `ast_to_config.rs` and emits a FlippedJoin variant in the OperatorConfig list.
    - This addresses CONTEXT R-02 (planner cost model emission must be exercised in production patterns).
  </behavior>
  <action>
    Add 3 tests. They can live in `tools/ivm-parity/diff-tests-track2.ts` (TS-side, using planner) OR in `packages/zqlite-rs/src/ast_to_config.rs::tests` if the AST is already pinned. Prefer the latter for round-trip determinism.
    Tests:
      - `test_planner_zbugs_permissions_flip_true_round_trip` — load `apps/zbugs/shared/permissions.ts`, find a query that uses `flip:true`, plan it, assert the resulting AST has flip set, push through ast_to_config, assert FlippedJoin is in the configs. **NOTE:** if zbugs no longer uses flip:true (per CONTEXT [ASSUMED] R-02), skip this test and document in the test summary that the assumption was invalidated; produce a synthetic AST instead.
      - `test_planner_cost_model_flips_on_small_parent_large_child` — synthetic ZQL where parent has 5 rows and child has 5000 rows with a selective predicate. Run through planner; assert flip:true is emitted; push through ast_to_config; assert FlippedJoin.
      - `test_planner_manual_flip_false_preserves_exists` — explicit `manualFlip: false`. Assert regular Exists path is taken (regression guard).
  </action>
  <verify>
    <automated>cd packages/zqlite-rs && cargo test test_planner 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - 3 planner round-trip tests pass.
    - `test_planner_zbugs_permissions_flip_true_round_trip` either passes or has explicit fallback document.
  </acceptance_criteria>
  <done>Planner emission verified end-to-end.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Shrink parity-allowlist.json</name>
  <files>tools/ivm-parity/parity-allowlist.json</files>
  <read_first>
    - tools/ivm-parity/parity-allowlist.json (full)
    - tools/ivm-parity/SKILL.md (full — hard rule 1: never modify test apparatus to hide divergence; this is the inverse — REMOVING entries is the desired outcome)
    - PARITY_STATUS.md (current state)
  </read_first>
  <behavior>
    - Remove the entry whose shape == "fuzz_00132".
    - Remove the entry whose shape == "fuzz_00133".
    - Remove the deferred pattern entry whose phase35Issue == "B7" (the `B7-flipped-join` pattern).
    - Keep all other entries (fuzz_00139, fuzz_00140, seed_18, B5/B6/B12 deferred patterns).
    - JSON remains valid; total entries shrink from 9 → 6.
  </behavior>
  <action>
    Edit `tools/ivm-parity/parity-allowlist.json` — remove the 3 entries listed above. Validate with `node -e "JSON.parse(require('fs').readFileSync('tools/ivm-parity/parity-allowlist.json','utf8'))"`.
  </action>
  <verify>
    <automated>node -e "const a = JSON.parse(require('fs').readFileSync('tools/ivm-parity/parity-allowlist.json','utf8')); if (a.length !== 6) { console.error('expected 6 entries got', a.length); process.exit(1); } if (a.some(e => e.shape === 'fuzz_00132' || e.shape === 'fuzz_00133' || e.phase35Issue === 'B7')) { console.error('B7 entries still present'); process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - Allow-list has exactly 6 entries.
    - No entry references B7 or fuzz_00132/fuzz_00133.
    - JSON parses cleanly.
  </acceptance_criteria>
  <done>Allow-list shrunk per Phase 36 closure.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Run npm run fuzz-check:gate to verify CI gate green</name>
  <files>tools/ivm-parity/</files>
  <read_first>
    - tools/ivm-parity/package.json (full — fuzz-check:gate script)
    - tools/ivm-parity/SKILL.md (operational rules)
  </read_first>
  <behavior>
    - `npm run fuzz-check:gate` exits 0.
    - The 5 new shape tests and 3 planner tests pass via the gate.
  </behavior>
  <action>
    Boot the two caches (TS + RS) per the SKILL.md procedure. Run `npm run fuzz-check:gate`. Confirm exit 0. If a divergence emerges, capture the canonical key and either:
      (a) fix the underlying operator bug (preferred), or
      (b) escalate — adding entries to the allow-list is forbidden without explicit user approval per SKILL.md hard rule 1.
  </action>
  <verify>
    <automated>cd tools/ivm-parity && timeout 180 npm run fuzz-check:gate 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npm run fuzz-check:gate` exits 0.
    - Console output contains "0 unexpected divergences".
  </acceptance_criteria>
  <done>CI gate verified green against the smaller allow-list.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Update IVM-PORT-AUDIT-DEEP.md and PARITY_STATUS.md</name>
  <files>.planning/IVM-PORT-AUDIT-DEEP.md, PARITY_STATUS.md</files>
  <read_first>
    - .planning/IVM-PORT-AUDIT-DEEP.md (B7 section, lines 109-118)
    - PARITY_STATUS.md (full)
  </read_first>
  <behavior>
    - B7 status changes from `🟠 OPEN-RISK` to `✅ FIXED`. Add commit reference (will be filled at commit time).
    - PARITY_STATUS.md gets a new "Phase 36 — Shipped" section above the existing Phase 34 section listing the FlippedJoin family port.
    - Allow-list table in PARITY_STATUS.md drops fuzz_00132 and fuzz_00133 rows.
    - Deferred patterns table drops the B7-flipped-join row.
  </behavior>
  <action>
    Edit `.planning/IVM-PORT-AUDIT-DEEP.md`:
      - Line 109: change `### B7. 🟠 OPEN-RISK — \`flip: true\` on a CSQ silently treated as regular Exists` to `### B7. ✅ FIXED — \`flip: true\` on a CSQ now routes to FlippedJoin family`.
      - Update body to reference `flipped_join_op.rs`, `union_fan_in_op.rs`, `union_fan_out_op.rs`, and the `applyFilterWithFlips` Rust equivalent in `ast_to_config.rs`.
      - Add "Tests" subsection citing flipped_join_op::tests count + diff-tests-track2.ts new entries.
    Edit `PARITY_STATUS.md`:
      - Add new section "Phase 36 — FlippedJoin Family Port — Shipped" with table of operators landed (FlippedJoin, UnionFanIn, UnionFanOut, FanIn, FanOut), commit references, test counts.
      - Edit allow-list table: remove fuzz_00132 and fuzz_00133 rows.
      - Edit deferred patterns table: remove B7-flipped-join row.
      - Update the file header to "Post-Phase-36 — B7 closed; FlippedJoin family ported; allow-list = 3 keys + 3 patterns".
  </action>
  <verify>
    <automated>grep -q "B7. ✅ FIXED" .planning/IVM-PORT-AUDIT-DEEP.md && ! grep -q "fuzz_00132" PARITY_STATUS.md && ! grep -q "B7-flipped-join" PARITY_STATUS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "B7. ✅ FIXED" .planning/IVM-PORT-AUDIT-DEEP.md` exits 0.
    - `grep -q "fuzz_00132" PARITY_STATUS.md` exits 1 (string absent).
    - `grep -q "Phase 36" PARITY_STATUS.md` exits 0.
  </acceptance_criteria>
  <done>Audit and parity docs reflect Phase 36 closure.</done>
</task>

</tasks>
