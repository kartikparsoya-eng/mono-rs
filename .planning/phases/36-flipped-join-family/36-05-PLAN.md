---
phase: 36-flipped-join-family
plan: 05
type: execute
wave: 4
depends_on: [36-04]
files_modified:
  - packages/zqlite-rs/src/ast_to_config.rs
autonomous: true
requirements: [B7, AST-FLIP-TRANSLATION]
tags: [phase-36, ast-translation, applyFilterWithFlips, wave-4]

must_haves:
  truths:
    - 'ast_to_config detects CSQ with flip:true and routes to the FanOut→FanIn-with-FlippedJoin distributive expansion.'
    - 'OR-with-mixed-flip, AND-with-flip, and standalone flipped CSQ all translate correctly.'
    - 'NOT EXISTS with flip:true panics (TS planner already rejects this — Rust mirrors).'
    - '≥15 unit tests in ast_to_config::tests cover the translation matrix.'
  artifacts:
    - path: 'packages/zqlite-rs/src/ast_to_config.rs'
      provides: 'append_condition_configs_with_flips function + condition_has_flip walker + partition_branches helper; integrated into ast_to_operator_configs entry point'
      contains: 'append_condition_configs_with_flips'
  key_links:
    - from: 'packages/zqlite-rs/src/ast_to_config.rs'
      to: 'packages/zql/src/builder/builder.ts'
      via: 'TS-spec citation in append_condition_configs_with_flips docstring'
      pattern: 'applyFilterWithFlips.*builder.ts'
---

<objective>
Wave 4 of Phase 36 — implement the AST translation that takes a CSQ with
`flip:true` and emits the FanOut→FanIn-with-FlippedJoin distributive
expansion. Currently `ast_to_config.rs` silently treats `flip:true` as a
regular Exists at three sites (lines 401, 456, 589).

This wave is the integration point — it makes the planner's `flip:true`
flag actually produce the right pipeline. After this wave, the live
differential fuzz can be re-run against shapes that previously hit
`fuzz_00132`/`fuzz_00133` with the allow-list still in place. Wave 5
removes the allow-list entries.

Output:

- `packages/zqlite-rs/src/ast_to_config.rs` — `append_condition_configs_with_flips` function and dispatch logic
- ≥15 unit tests in `ast_to_config::tests`
  </objective>

<execution_context>
@/Users/kartik.parsoya/Documents/Zero/mono-rs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kartik.parsoya/Documents/Zero/mono-rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-flipped-join-family/36-CONTEXT.md
@.planning/phases/36-flipped-join-family/36-RESEARCH.md
@packages/zql/src/builder/builder.ts
@packages/zqlite-rs/src/ast_to_config.rs
@packages/zero-ivm-rs/src/pipeline.rs

<interfaces>
From packages/zql/src/builder/builder.ts:
- function applyFilterWithFlips(input, condition, delegate, name): Input    // line 376-482
- function partitionBranches(conditions, predicate)                          // helper
- function conditionIncludesFlippedSubqueryAtAnyLevel(condition): boolean    // helper

From packages/zqlite-rs/src/ast_to_config.rs (current state):

- pub fn ast_to_operator_configs(schema, ast, primary_key, partition_key) -> Result<Vec<OperatorConfig>, String>
- fn append_condition_configs(schema, configs, cond, primary_key) -> Result<(), String>
- fn append_csq_as_exists(...) -> Result<(), String>
- Three sites at lines 401, 456, 589 currently silently match flip and only skip apply_exists_limit.
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement condition_has_flip walker and partition_branches helper</name>
  <files>packages/zqlite-rs/src/ast_to_config.rs</files>
  <read_first>
    - packages/zql/src/builder/builder.ts (lines 376-482, especially partitionBranches and conditionIncludesFlippedSubqueryAtAnyLevel helpers)
    - packages/zqlite-rs/src/ast_to_config.rs (full)
  </read_first>
  <behavior>
    - `condition_has_flip(cond) -> bool` walks the condition tree and returns true iff any CSQ has `flip == Some(true)`.
    - `partition_branches<F>(conditions: &[Condition], predicate: F) -> (Vec<Condition>, Vec<Condition>)` splits into (matching, non-matching).
  </behavior>
  <action>
    Add private helpers `condition_has_flip` and `partition_branches` near the top of `ast_to_config.rs`.
    Cite TS line `builder.ts:376+` in docstring.
    Add 5 unit tests:
      - `test_condition_has_flip_simple_no_flip` (returns false)
      - `test_condition_has_flip_csq_flip_true` (returns true)
      - `test_condition_has_flip_nested_in_and` (returns true)
      - `test_condition_has_flip_nested_in_or` (returns true)
      - `test_partition_branches_splits_correctly`
  </action>
  <verify>
    <automated>cd packages/zqlite-rs && cargo test ast_to_config::tests::test_condition_has_flip 2>&1 | tail -10 && cargo test ast_to_config::tests::test_partition_branches 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 5 helper tests pass.
    - Helpers are private (`fn`, not `pub fn`).
  </acceptance_criteria>
  <done>Walkers and helpers landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement append_condition_configs_with_flips dispatch</name>
  <files>packages/zqlite-rs/src/ast_to_config.rs</files>
  <read_first>
    - packages/zql/src/builder/builder.ts (lines 376-482 in detail)
    - .planning/phases/36-flipped-join-family/36-RESEARCH.md §Distributive expansion in applyFilterWithFlips
  </read_first>
  <behavior>
    - `append_condition_configs_with_flips(schema, configs, cond, primary_key)` is the new entry point.
    - Fast path: if `!condition_has_flip(cond)` delegate to existing `append_condition_configs`.
    - Recursive cases:
      - `Or { conditions }`: partition into withFlipped/withoutFlipped. Build withoutFlipped as one branch (calling `append_condition_configs`); each flipped CSQ becomes its own branch (recursively `append_condition_configs_with_flips`). Wrap in UnionFanOut → branches → UnionFanIn.
      - `And { conditions }`: partition. Emit withoutFlipped first as a normal AND chain via `append_condition_configs`. Then chain each flipped CSQ via recursion.
      - `CorrelatedSubquery { flip: Some(true), op, related }`: assert op != "NOT EXISTS"; emit `OperatorConfig::FlippedJoin { ... }` directly. Do NOT call `apply_exists_limit` (TS FlippedJoin doesn't downgrade child take).
      - `Simple` with flip:true is impossible (TS asserts `condition.type !== 'simple'`); panic.
    - Replace the three flip-handling sites at lines 401, 456, 589 with calls to the new function (or refactor so they delegate).
  </behavior>
  <action>
    Add `fn append_condition_configs_with_flips` per spec.
    Modify entry point: in `ast_to_operator_configs`, where WHERE is processed, call `append_condition_configs_with_flips` instead of `append_condition_configs` (the new function fast-paths when no flip).
    Update lines 401-432 (top-level CSQ flip site), 456-485 (`append_csq_as_exists`), 589-616 (collect_exists_branches): if flip is true, route to FlippedJoin via the new helper; else preserve existing Exists logic.
    Cite TS file:line on each branch.
  </action>
  <verify>
    <automated>cd packages/zqlite-rs && cargo test ast_to_config 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All pre-existing ast_to_config tests still pass (no regressions).
    - `grep -c "append_condition_configs_with_flips" packages/zqlite-rs/src/ast_to_config.rs` ≥ 4.
    - The function is invoked from `ast_to_operator_configs` for any AST with a WHERE clause.
  </acceptance_criteria>
  <done>Translation dispatch landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add 15 ast_to_config translation tests covering the flip matrix</name>
  <files>packages/zqlite-rs/src/ast_to_config.rs</files>
  <read_first>
    - packages/zqlite-rs/src/ast_to_config.rs (existing tests pattern)
  </read_first>
  <behavior>
    Tests cover the translation matrix:
    - flip:true on standalone CSQ → FlippedJoin emitted.
    - flip:true on CSQ inside AND → AND of non-flipped + flipped (chain).
    - flip:true on CSQ inside OR → UnionFanOut/UnionFanIn with branches.
    - Nested cases (flip inside OR inside AND, etc.).
    - Negative: flip:false → Exists path unchanged.
    - Negative: flip:true on NOT EXISTS panics.
    - apply_exists_limit NOT applied on flipped branches.
  </behavior>
  <action>
    Add ≥15 tests in `ast_to_config::tests`:
      - `test_flip_true_standalone_emits_flipped_join`
      - `test_flip_false_emits_regular_exists`
      - `test_flip_unset_emits_regular_exists`
      - `test_flip_true_in_and_chains_after_filter`
      - `test_flip_true_in_or_emits_union_fan_out_in`
      - `test_flip_true_in_or_with_simple_branch_two_branches`
      - `test_flip_true_in_or_with_two_flips_three_branches`
      - `test_flip_true_in_nested_and_inside_or`
      - `test_flip_true_in_nested_or_inside_and`
      - `test_flip_true_with_compound_key`
      - `test_flip_true_in_csq_subquery_recurses_correctly`
      - `test_flip_true_with_user_take_in_subquery_preserved` (apply_exists_limit not applied)
      - `test_flip_true_on_not_exists_panics`
      - `test_flip_true_on_simple_panics_unreachable` (defensive)
      - `test_no_flips_anywhere_uses_old_path` (regression guard — preserves existing pipeline shape)
  </action>
  <verify>
    <automated>cd packages/zqlite-rs && cargo test ast_to_config::tests::test_flip 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - ≥15 flip-related tests pass.
    - `cargo test -p zqlite-rs` total exit 0 (no regressions).
  </acceptance_criteria>
  <done>Translation matrix tests pass.</done>
</task>

</tasks>

<risks>
- **R-02 (MEDIUM)** — partial coverage of translation; planner-emission tests deferred to Wave 5.
</risks>
