---
phase: 36-flipped-join-family
plan: 02
type: execute
wave: 1
depends_on: [36-01]
files_modified:
  - packages/zero-ivm-rs/src/fan_out_op.rs
  - packages/zero-ivm-rs/src/fan_in_op.rs
  - packages/zero-ivm-rs/src/pipeline.rs
  - packages/zero-ivm-rs/src/lib.rs
  - packages/zqlite-rs/src/hydrate.rs
autonomous: true
requirements: [B7, FANIN-FANOUT-PORT]
tags: [phase-36, fan-in, fan-out, wave-1]

must_haves:
  truths:
    - 'FanOut and FanIn implement the filter-graph fork/merge with branch-OR predicate evaluation, mirroring TS fan-out.ts and fan-in.ts.'
    - 'OperatorConfig::FanOut and OperatorConfig::FanIn variants serialize/deserialize via existing pipeline.rs JSON conventions.'
    - 'Each operator has ≥10 unit tests with explicit expected output (no generator scaffolding).'
  artifacts:
    - path: 'packages/zero-ivm-rs/src/fan_out_op.rs'
      provides: 'FanOutOperator implementing filter-graph fork; push fans out to all outputs and triggers fan_in.fan_out_done_pushing_to_all_branches'
      min_lines: 80
    - path: 'packages/zero-ivm-rs/src/fan_in_op.rs'
      provides: 'FanInOperator implementing filter-graph merge with accumulator; push accumulates, fan_out_done_pushing_to_all_branches flushes'
      min_lines: 120
    - path: 'packages/zero-ivm-rs/src/pipeline.rs'
      provides: 'OperatorConfig::FanOut and OperatorConfig::FanIn variants + build_operator arms'
      contains: 'FanOut'
  key_links:
    - from: 'packages/zero-ivm-rs/src/fan_out_op.rs'
      to: 'packages/zero-ivm-rs/src/fan_in_op.rs'
      via: 'Arc<FanInOperator> field on FanOutOperator'
      pattern: 'fan_in.*Arc'
    - from: 'packages/zero-ivm-rs/src/pipeline.rs'
      to: 'packages/zero-ivm-rs/src/fan_out_op.rs'
      via: 'build_operator match arm'
      pattern: 'OperatorConfig::FanOut'
---

<objective>
Wave 1 of Phase 36 — port the simpler FanOut/FanIn pair from TS to Rust.
These are the filter-graph variants (used in `applyOr` for plain
non-flipped subqueries); they are easier than the Union variants and
establish the pattern for Wave 2.

Output:

- `packages/zero-ivm-rs/src/fan_out_op.rs` (~80 LOC + tests)
- `packages/zero-ivm-rs/src/fan_in_op.rs` (~120 LOC + tests)
- `OperatorConfig::FanOut` and `OperatorConfig::FanIn` variants in `pipeline.rs`
- `build_operator_chain` arms in `hydrate.rs` for the new variants
- ≥20 unit tests across both operators
  </objective>

<execution_context>
@/Users/kartik.parsoya/Documents/Zero/mono-rs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kartik.parsoya/Documents/Zero/mono-rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-flipped-join-family/36-CONTEXT.md
@.planning/phases/36-flipped-join-family/36-RESEARCH.md
@packages/zql/src/ivm/fan-out.ts
@packages/zql/src/ivm/fan-in.ts
@packages/zql/src/ivm/fan-out-fan-in.test.ts
@packages/zero-ivm-rs/src/pipeline.rs
@packages/zero-ivm-rs/src/operator.rs

<interfaces>
From packages/zql/src/ivm/fan-out.ts:
- export class FanOut implements FilterOperator { constructor(input) }
- *push(change)                                                  // line 74
- *filter(node): Generator<'yield', boolean>                     // line 63

From packages/zql/src/ivm/fan-in.ts:

- export class FanIn implements FilterOperator { constructor(fanOut, inputs) }
- \*fanOutDonePushingToAllBranches(fanOutChangeType) // line 76
- push(change) // line 71
- \*filter(node): Generator<'yield', boolean> // line 67

From packages/zero-ivm-rs/src/operator.rs:

- pub trait Operator { fn fetch(&self, req: &FetchRequest) -> Vec<Node>; fn push(&mut self, change: Change) -> Vec<Change>; ... }
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement FanOut operator (Rust port of fan-out.ts)</name>
  <files>packages/zero-ivm-rs/src/fan_out_op.rs</files>
  <read_first>
    - packages/zql/src/ivm/fan-out.ts (full)
    - packages/zero-ivm-rs/src/exists_op.rs (full — for output-fan-out pattern)
    - packages/zero-ivm-rs/src/or_exists_op.rs (full — multi-output coordination)
  </read_first>
  <behavior>
    - `fan_out.push(change)` propagates the change to all outputs (in registration order), then signals the paired FanIn via `fan_in.fan_out_done_pushing_to_all_branches(change_type)`.
    - `fan_out.filter(node)` short-circuits — if any output's filter returns true, return true. Mirrors TS fan-out.ts:64-72.
    - `destroy()` decrements an internal counter; only when all outputs have destroyed does it propagate to the input. Mirrors TS fan-out.ts:36-45.
    - `getSchema()` returns input's schema unchanged.
  </behavior>
  <action>
    Implement `FanOutOperator`:
      - Fields: `input: Arc<dyn Operator>`, `outputs: Vec<Arc<Mutex<dyn Operator>>>`, `fan_in: Option<Arc<Mutex<FanInOperator>>>`, `destroy_count: AtomicUsize`.
      - Cite TS lines in inline comments at each method.
      - `pub fn new(input: Arc<dyn Operator>) -> Self` — TS line 23.
      - `pub fn set_fan_in(&mut self, fan_in: Arc<Mutex<FanInOperator>>)` — TS line 28.
      - `pub fn add_output(&mut self, out: Arc<Mutex<dyn Operator>>)` — TS line 32 (`setFilterOutput`).
      - `pub fn push(&mut self, change: Change) -> Vec<Change>` — TS line 74. Iterate outputs, collect each output's push result. After all branches, call `self.fan_in.unwrap().fan_out_done_pushing_to_all_branches(change.change_type())` and append its result.
      - `pub fn filter(&self, node: &Node) -> bool` — TS line 63. Iterate outputs in order, call each output's filter, return true on first match (short-circuit).
      - `pub fn fetch(&self, req: &FetchRequest) -> Vec<Node>` — TS not present (fan-out is filter-only); Rust mirrors by delegating: `self.input.fetch(req)`.
      - `pub fn destroy(&self) -> Result<(), String>` — TS line 36; if `destroy_count >= outputs.len()` return `Err("FanOut already destroyed once for each output")`; else increment; if reached threshold call `input.destroy()`.
    Add ≥10 unit tests in same file:
      - `test_fan_out_two_outputs_propagates_push` — fan-out has 2 sinks; verify both get a push.
      - `test_fan_out_filter_short_circuit` — first output's filter returns true; verify second is not called.
      - `test_fan_out_filter_no_match` — both outputs return false; verify result is false.
      - `test_fan_out_destroy_refcount_three_outputs` — verify input.destroy is called only on 3rd destroy.
      - `test_fan_out_destroy_overflow_panics` — 4th destroy returns Err.
      - `test_fan_out_calls_fan_in_done_pushing` — assert fan_in.fan_out_done_pushing_to_all_branches is invoked after all outputs.
      - `test_fan_out_propagates_fetch_to_input` — fetch passthrough.
      - `test_fan_out_zero_outputs_push_panics_or_noops` — verify behavior matches TS (no outputs == no propagation, but fan_in MUST exist).
      - `test_fan_out_change_type_passed_to_fan_in` — Add → InFanOut(Add); verify fan_in receives correct ChangeType.
      - `test_fan_out_set_fan_in_idempotent` — calling set_fan_in twice panics (TS: assert).
  </action>
  <verify>
    <automated>cd packages/zero-ivm-rs && cargo test fan_out_op 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p zero-ivm-rs fan_out_op` reports ≥10 tests passing.
    - `wc -l packages/zero-ivm-rs/src/fan_out_op.rs` ≥ 200 (incl. tests).
    - No `unimplemented!()` or `todo!()` in the file.
    - Module compiled with no warnings.
  </acceptance_criteria>
  <done>FanOut operator landed with full TS-parity behavior and ≥10 tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement FanIn operator (Rust port of fan-in.ts)</name>
  <files>packages/zero-ivm-rs/src/fan_in_op.rs</files>
  <read_first>
    - packages/zql/src/ivm/fan-in.ts (full)
    - packages/zql/src/ivm/push-accumulated.ts (full — used by FanIn flush)
    - packages/zero-ivm-rs/src/fan_out_op.rs (just-written; verify integration shape)
  </read_first>
  <behavior>
    - `fan_in.push(change)` does NOT forward immediately — accumulates in `accumulated_pushes: Vec<Change>` (TS fan-in.ts:71-74).
    - `fan_in.fan_out_done_pushing_to_all_branches(change_type)` calls `pushAccumulatedChanges` analogue — `push_accumulated_changes(&self.accumulated_pushes, change_type, identity, identity)` — and emits the result downstream.
    - `fan_in.filter(node)` simply forwards to its output's filter (TS fan-in.ts:67).
    - `getSchema()` is the schema of the paired FanOut (asserted equal to all inputs at construction).
    - Schema mismatch on construction panics (TS fan-in.ts:41).
  </behavior>
  <action>
    Implement `FanInOperator`:
      - Fields: `inputs: Vec<Arc<Mutex<dyn Operator>>>`, `schema: Arc<SourceSchema>`, `accumulated: Vec<Change>`, `output: Option<Arc<Mutex<dyn Operator>>>`.
      - Cite TS lines.
      - `pub fn new(fan_out: Arc<Mutex<FanOutOperator>>, inputs: Vec<Arc<Mutex<dyn Operator>>>) -> Self` — TS line 36-43; assert all inputs share the schema with fan_out.
      - `pub fn push(&mut self, change: Change) -> Vec<Change>` — TS line 71; just appends, returns `vec![]`.
      - `pub fn fan_out_done_pushing_to_all_branches(&mut self, change_type: ChangeType) -> Vec<Change>` — TS line 76; if inputs is empty assert accumulated is empty; else call `push_accumulated_changes(...)` with `identity, identity`.
      - `pub fn filter(&self, node: &Node) -> bool` — TS line 67; delegates to output's filter.
      - `pub fn fetch(&self, req: &FetchRequest) -> Vec<Node>` — fan-in is filter-only; Rust mirrors via `self.inputs[0].fetch(req)` (all inputs share schema).
    Implement helper `push_accumulated_changes` in `push_accumulated.rs` (NEW small file):
      - Group changes by primary key.
      - Per-PK: fold via `merge_fn` (default `identity` for FanIn).
      - Wrap result via `make_fn` (default `identity` for FanIn).
      - Return `Vec<Change>` ready to push downstream.
    Add ≥10 tests:
      - `test_fan_in_accumulates_pushes_until_done`
      - `test_fan_in_done_pushing_flushes_accumulated`
      - `test_fan_in_no_inputs_no_pushes_ok`
      - `test_fan_in_no_inputs_with_pushes_panics`
      - `test_fan_in_filter_delegates_to_output`
      - `test_fan_in_schema_mismatch_panics`
      - `test_fan_in_dedup_same_pk_via_push_accumulated`
      - `test_fan_in_two_branches_one_pk_each`
      - `test_fan_in_change_type_propagated_to_push_accumulated`
      - `test_fan_in_destroy_propagates_to_inputs`
  </action>
  <verify>
    <automated>cd packages/zero-ivm-rs && cargo test fan_in_op 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p zero-ivm-rs fan_in_op` reports ≥10 tests passing.
    - `wc -l packages/zero-ivm-rs/src/fan_in_op.rs` ≥ 250 (incl. tests).
    - `push_accumulated.rs` helper file exists.
  </acceptance_criteria>
  <done>FanIn operator landed with TS-parity behavior, ≥10 tests, helper file in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire OperatorConfig::FanOut and OperatorConfig::FanIn variants</name>
  <files>packages/zero-ivm-rs/src/pipeline.rs, packages/zero-ivm-rs/src/lib.rs, packages/zqlite-rs/src/hydrate.rs</files>
  <read_first>
    - packages/zero-ivm-rs/src/pipeline.rs (full)
    - packages/zqlite-rs/src/hydrate.rs (full — build_operator_chain)
  </read_first>
  <behavior>
    - `OperatorConfig::FanOut { branches: Vec<Vec<OperatorConfig>> }` — each branch is a sub-pipeline.
    - `OperatorConfig::FanIn { branch_count: usize }` — paired with the FanOut. Branch_count must equal preceding FanOut.branches.len().
    - `build_operator` constructs the operator tree: FanOut creates a single FanOutOperator; downstream FanIn paired via `Arc<Mutex<...>>` references.
    - Round-trip JSON serialization tested: parse `{"type":"fan_out","branches":[...]}` → emit Rust ops; same for `fan_in`.
  </behavior>
  <action>
    In `pipeline.rs`:
      - Add `#[serde(rename = "fan_out")] FanOut { branches: Vec<Vec<OperatorConfig>> }` variant.
      - Add `#[serde(rename = "fan_in")] FanIn { branch_count: usize }` variant.
      - Extend `build_operator` match — see Phase 11 Join for the pattern of constructing nested operators.
    In `hydrate.rs::build_operator_chain`:
      - Add arms for the new variants. FanOut needs to construct sub-pipelines for each branch; FanIn pairs with the matching FanOut at the chain level.
    Add 5 round-trip JSON tests in `pipeline.rs::tests`:
      - `test_fan_out_json_round_trip`
      - `test_fan_in_json_round_trip`
      - `test_fan_out_fan_in_pipeline_compiles_to_operator_tree` (build_operator emits Operator that survives a fetch/push smoke test)
      - `test_fan_in_branch_count_mismatch_errors`
      - `test_fan_out_zero_branches_panics`
  </action>
  <verify>
    <automated>cd packages/zero-ivm-rs && cargo test pipeline 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p zero-ivm-rs pipeline` reports the 5 new tests passing.
    - `grep -c "fan_out\|fan_in" packages/zero-ivm-rs/src/pipeline.rs` ≥ 6.
    - `cargo test -p zqlite-rs --test hydrate` exits 0 (pre-existing tests still pass).
  </acceptance_criteria>
  <done>OperatorConfig variants wired; hydrate.rs builds the operator tree; round-trip tests pass.</done>
</task>

</tasks>
