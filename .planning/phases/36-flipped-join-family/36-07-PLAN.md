---
phase: 36-flipped-join-family
plan: 07
type: verify
wave: 6
depends_on: [36-06]
files_modified:
  - .planning/phases/36-flipped-join-family/36-VALIDATION.md
  - .planning/milestones/v6.0-bench-results.md
  - .planning/STATE.md
autonomous: true
requirements: [B7]
tags: [phase-36, verification, gate, wave-6]

must_haves:
  truths:
    - 'All Phase 36 exit criteria from CONTEXT D-14 are independently verified.'
    - 'Phase 33 benches still PASS (TTFB ≤1.5×, MemPeak ≤4×); FlippedJoin path latency baseline measured fresh.'
    - 'Full cargo test + vitest green; no pre-existing test count regression.'
    - 'Allow-list shrinkage confirmed and CI gate green.'
  artifacts:
    - path: '.planning/phases/36-flipped-join-family/36-VALIDATION.md'
      provides: 'Per-criterion verification with evidence (commit hashes, command output snippets)'
      contains: 'Phase 36 Verification Gate'
    - path: '.planning/milestones/v6.0-bench-results.md'
      provides: 'Append-only Phase 36 bench section with TTFB/MemPeak baseline for FlippedJoin path'
      contains: 'Phase 36'
---

<objective>
Wave 6 of Phase 36 — verification gate. Walk through each Phase 36 exit
criterion (CONTEXT D-14), capture evidence per criterion, and write
36-VALIDATION.md. Run Phase 33 benchmarks fresh against the new path.
Confirm full cargo + vitest + fuzz-check:gate are green.

This plan does not produce new operator code — it produces evidence and
documentation for closure.
</objective>

<execution_context>
@/Users/kartik.parsoya/Documents/Zero/mono-rs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kartik.parsoya/Documents/Zero/mono-rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-flipped-join-family/36-CONTEXT.md
@.planning/phases/36-flipped-join-family/36-RESEARCH.md
@.planning/milestones/v5.0-bench-results.md (template for Phase 36 bench section)
@PARITY_STATUS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run full cargo + vitest suite; capture pass counts</name>
  <files>.planning/phases/36-flipped-join-family/36-VALIDATION.md</files>
  <read_first>
    - .planning/phases/36-flipped-join-family/36-CONTEXT.md §Verification Gate
  </read_first>
  <action>
    Run:
      - `cd packages/zero-ivm-rs && cargo test 2>&1 | tee /tmp/zero-ivm-rs.log` — capture total pass count.
      - `cd packages/zqlite-rs && cargo test 2>&1 | tee /tmp/zqlite-rs.log` — capture total pass count.
      - `npm --workspace=zero-cache run test 2>&1 | tee /tmp/zc.log` — capture pass count.
      - `cd packages/zql && npm run test 2>&1 | tee /tmp/zql.log` — capture pass count for the unchanged TS suite.
    Write a section in 36-VALIDATION.md:
      ```
      ## D-14 #2 — cargo test -p zero-ivm-rs ≥110 new tests
      Total tests: <N> (delta from Phase 35: <D>; expected delta ≥110).
      ```
  </action>
  <verify>
    <automated>cd packages/zero-ivm-rs && cargo test 2>&1 | grep -E "test result.*passed" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - zero-ivm-rs test count grew by ≥110.
    - zqlite-rs test count grew by ≥15.
    - vitest passes match Phase 35 baseline (no regressions).
  </acceptance_criteria>
  <done>Full test suites green; counts documented.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run fuzz-check:gate against shrunken allow-list</name>
  <files>.planning/phases/36-flipped-join-family/36-VALIDATION.md</files>
  <action>
    Boot two caches per SKILL.md procedure. Run `cd tools/ivm-parity && time npm run fuzz-check:gate 2>&1 | tee /tmp/fuzz-gate.log`. Capture:
      - Wall time (must be <2 min).
      - Divergence count (must be 0).
      - Per-table hit distribution (no degradation from Phase 34 baseline).
    Append section in 36-VALIDATION.md:
      ```
      ## D-14 #3 — npm run fuzz-check:gate exit 0 with shrunken allow-list
      ```
  </action>
  <verify>
    <automated>tail -1 /tmp/fuzz-gate.log | grep -qE "0 unexpected"</automated>
  </verify>
  <acceptance_criteria>
    - Exit 0.
    - 0 unexpected divergences.
    - Wall time <120s.
  </acceptance_criteria>
  <done>CI gate confirmed green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run Phase 33 benchmarks; record FlippedJoin baseline</name>
  <files>.planning/milestones/v6.0-bench-results.md, .planning/phases/36-flipped-join-family/36-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v5.0-bench-results.md (Phase 34 section as format template)
  </read_first>
  <behavior>
    - Run Phase 33 standard benches (TTFB ratio, MemPeak ratio).
    - Run a FlippedJoin-specific benchmark (new path; baseline is fresh).
  </behavior>
  <action>
    Run:
      - `npm --workspace=zero-cache run test:bench 2>&1 | tee /tmp/bench.log` — captures TTFB + MemPeak for the existing benchmark suite.
      - Author a small new benchmark in `packages/zero-cache/src/services/view-syncer/flipped-join.bench.ts` that exercises a flipped-join path and records p50/p99 hydrate latency. Run; capture median.
    Append to `.planning/milestones/v6.0-bench-results.md` (CREATE the file if it does not yet exist; mirror v5.0 layout):
      ```
      ## Phase 36 — FlippedJoin Family Port

      | Metric | Value | Threshold | Pass? |
      |---|---|---|---|
      | TTFB ratio (Rust:TS) | <X> | ≤1.5× | <Y> |
      | MemPeak ratio (Rust:TS) | <X> | ≤4× | <Y> |
      | FlippedJoin hydrate p50 | <X>ms | new baseline | n/a |
      | FlippedJoin hydrate p99 | <X>ms | new baseline | n/a |
      ```

  </action>
  <verify>
    <automated>test -f .planning/milestones/v6.0-bench-results.md && grep -q "Phase 36" .planning/milestones/v6.0-bench-results.md</automated>
  </verify>
  <acceptance_criteria>
    - TTFB ratio ≤1.5×.
    - MemPeak ratio ≤4×.
    - FlippedJoin baseline numbers recorded.
  </acceptance_criteria>
  <done>Benches run and baseline recorded.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Author 36-VALIDATION.md with full per-criterion table</name>
  <files>.planning/phases/36-flipped-join-family/36-VALIDATION.md</files>
  <action>
    Write 36-VALIDATION.md with this structure:
      ```
      # Phase 36 Verification Gate

      | Criterion | Result | Evidence |
      |---|---|---|
      | D-14 #1: All 5 operators have unit + diff tests | <PASS/FAIL> | <commit refs> |
      | D-14 #2: cargo test -p zero-ivm-rs ≥110 new tests | <PASS/FAIL> | /tmp/zero-ivm-rs.log line N |
      | D-14 #3: parity-allowlist.json shrinks by 2 | <PASS/FAIL> | git diff hash |
      | D-14 #4: cargo test -p zqlite-rs ≥15 new ast_to_config tests | <PASS/FAIL> | /tmp/zqlite-rs.log line N |
      | D-14 #5: Phase 33 benches PASS, fresh FlippedJoin baseline | <PASS/FAIL> | v6.0-bench-results.md Phase 36 section |
      | D-14 #6: TS suite still green (zql vitest) | <PASS/FAIL> | /tmp/zql.log final line |
      ```
    Include a §Risk Closure section confirming R-01 through R-04 status (closed/mitigated/open-with-known-pattern).
    Include a §Phase 35 Carry-Forward Verification — confirm B5/B6/B12 deferred patterns remain in allow-list (we only removed B7).

  </action>
  <verify>
    <automated>test -f .planning/phases/36-flipped-join-family/36-VALIDATION.md && grep -E "^\| D-14 #(1|2|3|4|5|6)" .planning/phases/36-flipped-join-family/36-VALIDATION.md | wc -l | grep -qE '^[ ]*6'</automated>
  </verify>
  <acceptance_criteria>
    - 36-VALIDATION.md contains the 6-criterion table.
    - Every criterion has evidence link (file path + line number).
    - All 6 criteria show PASS.
  </acceptance_criteria>
  <done>Verification document complete.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Update STATE.md and ROADMAP.md to mark Phase 36 complete</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    Edit `.planning/STATE.md` to flip Phase 36 status to "complete" with completion date.
    Edit `.planning/ROADMAP.md` Phase 36 row similarly.
  </action>
  <verify>
    <automated>grep -q "Phase 36.*complete\|Phase 36.*✅\|Phase 36.*shipped" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md shows Phase 36 complete.
    - ROADMAP.md shows Phase 36 complete with link to milestone.
  </acceptance_criteria>
  <done>Phase 36 marked complete in workspace state.</done>
</task>

</tasks>

<commit_message>
docs(phase-36): plan FlippedJoin family port (closes B7 + parity fuzz_00132/133)
</commit_message>
