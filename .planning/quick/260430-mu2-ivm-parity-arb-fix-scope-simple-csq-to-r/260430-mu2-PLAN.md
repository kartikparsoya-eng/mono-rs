---
phase: 260430-mu2-ivm-parity-arb-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/ivm-parity/arb-ast.ts
  - tools/ivm-parity/all-divergences.json
  - tools/ivm-parity/ALL-DIVERGENCES-CATALOG.md
  - tools/ivm-parity/regression-runner-last.json
autonomous: true
requirements:
  - QUICK-MU2-FIX
  - QUICK-MU2-CATALOG
must_haves:
  truths:
    - 'arb-ast.ts no longer generates cross-table column references in simple/csq arbitraries'
    - 'all-divergences.json reports uniqueShapes=6 (down from 56)'
    - 'regression-runner-last.json reports total.diverge=6 with 0/6 hydrate-only pass — clean B5 baseline'
    - '9 out-of-scope files in tools/ivm-parity/ remain untracked after both commits'
    - 'git diff main..HEAD lists ONLY paths under tools/ivm-parity/'
  artifacts:
    - path: 'tools/ivm-parity/arb-ast.ts'
      provides: 'scoped simple+csq arbitraries via makeRecArb(rootTable)'
      contains: 'makeRecArb'
    - path: 'tools/ivm-parity/all-divergences.json'
      provides: 'regenerated divergence catalog with uniqueShapes=6'
    - path: 'tools/ivm-parity/ALL-DIVERGENCES-CATALOG.md'
      provides: 'human-readable catalog of 6 real B5-family divergences'
    - path: 'tools/ivm-parity/regression-runner-last.json'
      provides: '0/6 baseline for the upcoming B5 fix'
  key_links:
    - from: 'Commit 1 (arb-ast.ts fix)'
      to: 'Commit 2 (regenerated catalog)'
      via: 'bisect-friendly ordering — fix lands before its measured artifact'
      pattern: 'fix(ivm-parity).*then.*docs(ivm-parity)'
---

<objective>
Stage and commit pre-existing working-tree changes as 2 atomic, bisect-friendly commits:

1. **fix(ivm-parity)**: scope `arb-ast.ts` simple+csq arbitraries to root table.
2. **docs(ivm-parity)**: regenerate divergence catalog using the fixed arb (56 → 6 real divergences).

Purpose: land the arb fix and its measured outcome as separate, individually revertable commits so a future bisect can isolate either the generator change or the catalog regeneration. Establish a clean 0/6 hydrate-only baseline for the upcoming B5 OrExistsOperator fix (tracked separately).

Output: 2 commits on main, 4 files changed, 9 OUT-OF-SCOPE files preserved untracked.
</objective>

<execution_context>
@/Users/kartik.parsoya/Documents/Zero/mono-rs/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@./AGENTS.md

# Working-tree state confirmed at plan time

# Modified (the 4 commit targets):

# M tools/ivm-parity/ALL-DIVERGENCES-CATALOG.md

# M tools/ivm-parity/all-divergences.json

# M tools/ivm-parity/arb-ast.ts

# M tools/ivm-parity/regression-runner-last.json

#

# Untracked (MUST stay untracked — 9 OUT-OF-SCOPE files):

# ?? tools/ivm-parity/\_gen_prod_gap_asts.py

# ?? tools/ivm-parity/\_gen_prod_shape_asts.py

# ?? tools/ivm-parity/harness-capture-golden.ts

# ?? tools/ivm-parity/harness-golden-shared.ts

# ?? tools/ivm-parity/harness-replay-golden.ts

# ?? tools/ivm-parity/prod_gap_asts.json

# ?? tools/ivm-parity/prod_shape_asts.json

# ?? tools/ivm-parity/run-capture-golden.sh

# ?? tools/ivm-parity/run-replay-golden.sh

#

# Base for cross-check: d6b75515877d834695faa65525b00b36106d86e3

</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit 1 — fix(ivm-parity): scope arb-ast.ts simple+csq arbitraries to root table</name>
  <files>tools/ivm-parity/arb-ast.ts</files>
  <action>
Stage ONLY tools/ivm-parity/arb-ast.ts with explicit pathspec, then commit.

Commands (run from repo root /Users/kartik.parsoya/Documents/Zero/mono-rs):

1. Pre-stage sanity:
   - `git status --short tools/ivm-parity/arb-ast.ts` → must show ` M tools/ivm-parity/arb-ast.ts`
   - `grep -q "makeRecArb" tools/ivm-parity/arb-ast.ts` → must succeed
   - `grep -q "rootTable" tools/ivm-parity/arb-ast.ts` → must succeed
   - `! grep -E "arbTable\\.chain\\(t => arbSimpleConditionForTable\\(t\\)\\)" tools/ivm-parity/arb-ast.ts` → must succeed (old buggy line gone)

2. Stage with explicit pathspec only — NO `git add .`, NO `-A`, NO directory globs:
   `git add tools/ivm-parity/arb-ast.ts`

3. Verify staging is exclusive:
   `git diff --cached --name-only` → must output exactly `tools/ivm-parity/arb-ast.ts` and nothing else.

4. Commit using HEREDOC (do NOT skip hooks, do NOT amend):

```
git commit -m "$(cat <<'EOF'
fix(ivm-parity): scope arb-ast.ts simple+csq arbitraries to root table

Old: recArb.simple and recArb.csq used arbTable.chain(t => ...) which
picked a random table per-condition. For an AST rooted at event_tags,
this generated event_tags WHERE processedAt IS NULL where processedAt
is a column on events, not event_tags. Both TS and RS dutifully ran
these nonsense queries and legitimately diverged on edge cases — pure
arb noise, not real bugs.

New: makeRecArb(rootTable) is invoked once per AST root table inside
arbAst.chain. simple and csq arbitraries draw only from the actual
root table's columns and relationships. CSQ fallback for tables
without relationships is preserved.

Measurement (post-fix):
- Sanity probe: 200 ASTs at SEED=20260430 → 0 cross-table column
  leaks (was many).
- 1000-iter sweep: 12 → 1 unexpected divergence.
- 6 seeds × 300 iter (1800 ASTs total): 5/6 seeds completely clean;
  1 unique divergence shape across the entire run.
- Cache stability: previously died after ~130 random ASTs; now clean
  for 2800+.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

5. Post-commit verification (every check below must pass):
   - `git log -1 --pretty=%s` → must start with `fix(ivm-parity):`
   - `git show --stat HEAD --name-only | grep -E "^tools/ivm-parity/arb-ast\\.ts$"` → must succeed
   - `git show --stat HEAD --name-only | grep -v -E "^(commit|Author|Date|fix|$|tools/ivm-parity/arb-ast\\.ts$)" | grep -v "^ " | wc -l` informational
   - Stricter: `git diff-tree --no-commit-id --name-only -r HEAD` → must output exactly `tools/ivm-parity/arb-ast.ts`
   - 9 untracked files preserved: `git status --short tools/ivm-parity/ | grep "^??" | wc -l` → must report ≥ 9
     </action>
     <verify>
     <automated>cd /Users/kartik.parsoya/Documents/Zero/mono-rs && git diff-tree --no-commit-id --name-only -r HEAD | tr -d ' ' | sort -u > /tmp/mu2-c1-files.txt && echo "tools/ivm-parity/arb-ast.ts" | sort -u > /tmp/mu2-c1-expected.txt && diff /tmp/mu2-c1-files.txt /tmp/mu2-c1-expected.txt && git log -1 --pretty=%s | grep -qE "^fix\\(ivm-parity\\):" && [ "$(git status --short tools/ivm-parity/ | grep -c '^??')" -ge 9 ] && echo OK</automated>
     </verify>
     <done>

- HEAD commit subject begins with `fix(ivm-parity):`
- HEAD commit touches exactly one file: `tools/ivm-parity/arb-ast.ts`
- ≥ 9 untracked files in tools/ivm-parity/ remain untracked
- No hooks skipped; commit is a fresh commit, not an amend
  </done>
  </task>

<task type="auto">
  <name>Task 2: Commit 2 — docs(ivm-parity): regenerate catalog with fixed arb (56 → 6 real divergences)</name>
  <files>tools/ivm-parity/all-divergences.json, tools/ivm-parity/ALL-DIVERGENCES-CATALOG.md, tools/ivm-parity/regression-runner-last.json</files>
  <action>
Stage ONLY the 3 catalog/baseline files with explicit pathspecs, then commit.

Commands (run from repo root /Users/kartik.parsoya/Documents/Zero/mono-rs):

1. Pre-stage sanity (verifies the regenerated artifacts match the documented numbers — uses ONLY grep / test / `node -e JSON.parse`, never `npx tsx -e import(...)` per m6u checkpoint lesson):
   - `test -s tools/ivm-parity/all-divergences.json`
   - `test -s tools/ivm-parity/ALL-DIVERGENCES-CATALOG.md`
   - `test -s tools/ivm-parity/regression-runner-last.json`
   - `node -e "const j=JSON.parse(require('fs').readFileSync('tools/ivm-parity/all-divergences.json'));if(j.uniqueShapes!==6)throw new Error('expected uniqueShapes=6, got '+j.uniqueShapes);console.log('uniqueShapes=6 ok')"`
   - `node -e "const j=JSON.parse(require('fs').readFileSync('tools/ivm-parity/regression-runner-last.json'));if(j.total.diverge!==6)throw new Error('expected total.diverge=6, got '+j.total.diverge);console.log('regression baseline=0/6 ok')"`

2. Stage with explicit pathspecs only — NO `git add .`, NO `-A`, NO directory globs:

   ```
   git add tools/ivm-parity/all-divergences.json \
           tools/ivm-parity/ALL-DIVERGENCES-CATALOG.md \
           tools/ivm-parity/regression-runner-last.json
   ```

3. Verify staging is exclusive (must list exactly these 3 paths and nothing else):
   `git diff --cached --name-only | sort` → must equal:

   ```
   tools/ivm-parity/ALL-DIVERGENCES-CATALOG.md
   tools/ivm-parity/all-divergences.json
   tools/ivm-parity/regression-runner-last.json
   ```

4. Commit using HEREDOC (do NOT skip hooks, do NOT amend):

```
git commit -m "$(cat <<'EOF'
docs(ivm-parity): regenerate catalog with fixed arb (56 → 6 real divergences)

The previous catalog (56 unique shapes) was ~90% arb noise from
cross-table column references generated by the pre-fix simple+csq
arbitraries. Real-bug count was much smaller than the 56 implied —
the catalog overstated the bug surface and obscured the actual
B5 family.

Regenerated catalog after the arb fix:
- uniqueShapes = 6 (down from 56)
- All 6 are fast-check shrinks of the same B5 family —
  AND-inside-OR-of-EXISTS (the documented TODO at
  ast_to_config.rs:587).
- Bucket distribution:
    A-nested-OR-with-EXISTS = 5
    D-simple-OR-with-EXISTS = 1
- Regression baseline: 0/6 pass on hydrate-only — clean,
  reproducible suite for the upcoming B5 fix.

Out-of-scope: the B5 fix itself (8+ hour OrExistsOperator
architectural refactor) belongs in its own phase.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

5. Post-commit verification (every check below must pass):
   - `git log -1 --pretty=%s` → must start with `docs(ivm-parity):`
   - `git diff-tree --no-commit-id --name-only -r HEAD | sort` → must equal exactly:
     ```
     tools/ivm-parity/ALL-DIVERGENCES-CATALOG.md
     tools/ivm-parity/all-divergences.json
     tools/ivm-parity/regression-runner-last.json
     ```
   - 9 untracked files preserved: `git status --short tools/ivm-parity/ | grep "^??" | wc -l` → must report ≥ 9
   - Final cross-check across both commits:
     `git diff --name-only d6b75515877d834695faa65525b00b36106d86e3..HEAD | sort -u` → every line MUST start with `tools/ivm-parity/` (no other paths leaked).
     </action>
     <verify>
     <automated>cd /Users/kartik.parsoya/Documents/Zero/mono-rs && git diff-tree --no-commit-id --name-only -r HEAD | sort > /tmp/mu2-c2-files.txt && printf "tools/ivm-parity/ALL-DIVERGENCES-CATALOG.md\ntools/ivm-parity/all-divergences.json\ntools/ivm-parity/regression-runner-last.json\n" | sort > /tmp/mu2-c2-expected.txt && diff /tmp/mu2-c2-files.txt /tmp/mu2-c2-expected.txt && git log -1 --pretty=%s | grep -qE "^docs\\(ivm-parity\\):" && [ "$(git status --short tools/ivm-parity/ | grep -c '^??')" -ge 9 ] && [ "$(git diff --name-only d6b75515877d834695faa65525b00b36106d86e3..HEAD | grep -vE '^tools/ivm-parity/' | wc -l | tr -d ' ')" = "0" ] && node -e "const j=JSON.parse(require('fs').readFileSync('tools/ivm-parity/all-divergences.json'));if(j.uniqueShapes!==6)throw 0;console.log('uniqueShapes=6 ok')" && node -e "const j=JSON.parse(require('fs').readFileSync('tools/ivm-parity/regression-runner-last.json'));if(j.total.diverge!==6)throw 0;console.log('regression baseline=0/6 ok')" && echo OK</automated>
     </verify>
     <done>

- HEAD commit subject begins with `docs(ivm-parity):`
- HEAD commit touches exactly the 3 catalog/baseline files (no others)
- `all-divergences.json` reports `uniqueShapes === 6`
- `regression-runner-last.json` reports `total.diverge === 6` (0/6 hydrate-only pass)
- ≥ 9 untracked files in tools/ivm-parity/ remain untracked
- `git diff --name-only d6b75...HEAD` lists ONLY paths under `tools/ivm-parity/`
- No hooks skipped; commit is a fresh commit, not an amend
  </done>
  </task>

</tasks>

<verification>
Phase-level verification (after both tasks):

1. Two-commit ordering — fix lands before docs:

   ```
   git log --pretty=%s d6b75515877d834695faa65525b00b36106d86e3..HEAD
   ```

   First (newest) line: `docs(ivm-parity): regenerate catalog ...`
   Second line: `fix(ivm-parity): scope arb-ast.ts simple+csq ...`

2. Total files changed across the 2 commits = exactly 4, all under `tools/ivm-parity/`:

   ```
   git diff --name-only d6b75515877d834695faa65525b00b36106d86e3..HEAD | sort -u
   ```

   Expected output (exact):

   ```
   tools/ivm-parity/ALL-DIVERGENCES-CATALOG.md
   tools/ivm-parity/all-divergences.json
   tools/ivm-parity/arb-ast.ts
   tools/ivm-parity/regression-runner-last.json
   ```

3. 9 OUT-OF-SCOPE files still untracked:

   ```
   git status --short tools/ivm-parity/ | grep "^??" | wc -l
   ```

   Must be ≥ 9. Spot-check the list contains `_gen_prod_gap_asts.py`,
   `harness-capture-golden.ts`, `prod_shape_asts.json`, `run-replay-golden.sh`.

4. No hook bypass: `git log -1 --pretty=%B HEAD` and `git log -1 --pretty=%B HEAD~1`
   must NOT contain `--no-verify`. (Hooks are not invoked in commit messages, but
   confirm the executor did not pass `--no-verify` on the command line by reading
   the action sections — this is enforced procedurally.)

5. No .planning/ modifications: `git diff --name-only d6b75...HEAD | grep "^\\.planning/"`
   must produce no output.
   </verification>

<success_criteria>
All of the following must be true:

- [ ] Exactly 2 new commits on `main` since `d6b75515877d834695faa65525b00b36106d86e3`.
- [ ] Commit 1 (HEAD~1) subject: `fix(ivm-parity): scope arb-ast.ts simple+csq arbitraries to root table`. Touches only `tools/ivm-parity/arb-ast.ts`.
- [ ] Commit 2 (HEAD) subject: `docs(ivm-parity): regenerate catalog with fixed arb (56 → 6 real divergences)`. Touches only the 3 catalog/baseline files.
- [ ] `all-divergences.json.uniqueShapes === 6`.
- [ ] `regression-runner-last.json.total.diverge === 6` (0/6 hydrate-only pass — clean B5 baseline).
- [ ] ≥ 9 untracked files remain in `tools/ivm-parity/` after both commits.
- [ ] `git diff --name-only d6b75...HEAD` contains ONLY paths under `tools/ivm-parity/`.
- [ ] No `--no-verify`, no `--amend`, no `git add .` / `-A` / directory globs were used.
- [ ] No `.planning/` files were modified by the executor.
      </success_criteria>

<output>
This is a `quick` task — no SUMMARY.md required by the orchestrator. Final state is the 2 commits on `main` and the preserved untracked files. The orchestrator owns post-execution updates (if any) under `.planning/quick/260430-mu2-ivm-parity-arb-fix-scope-simple-csq-to-r/`.
</output>
